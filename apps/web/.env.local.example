# 前端环境变量模板
# 复制为 .env.local 并填入实际值（.env.local 已在 .gitignore 中忽略）

# BFF 服务地址（客户端可见，用于 api-client 直连 BFF）
# 未配置时前端走同源 /api，由 Next.js rewrites 代理到 BFF（推荐生产用法）
NEXT_PUBLIC_BFF_URL=http://localhost:3001

# BFF 服务地址（仅服务端可见，用于 next.config.ts rewrites 代理目标）
BFF_URL=http://localhost:3001
