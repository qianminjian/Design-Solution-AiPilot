# 施工图全流程 AI 平台设计明灯

## 目标与成功标准

- 建设覆盖前期策划、概念设计、方案设计、扩初设计、施工图、多专业协同、审查、交付与变更的 AI 平台。
- 原始“境外主创草图到方案深化”流程作为首个业务场景，不限定平台最终范围。
- 唯一设计正文为 D00–D46 各章节文件（见 `INDEX.md` 章节索引），后续详细设计均在对应章节文件内完成。
- 每个板块、组件、模块、接口、流程和界面最终具备可实施、可测试、可追溯的设计。

## 范围边界

**做：** 原始需求澄清、总体设计补充、全流程能力设计、组件技术栈和详细设计任务计划。

**不做：** 当前阶段不编码、不部署、不修改数据库；AI 不替代注册建筑师/工程师的专业审签和监管审批。

## 设计决策

| #   | 决策                                                                                                                 | 理由                                                                                                                                                           | 日期       |
| --- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 1   | 维持“施工图全流程 AI 平台”定位                                                                                       | 原设计初稿已确定，细化不得改变方向                                                                                                                             | 2026-07-16 |
| 2   | 总体能力完整设计、实施分阶段推进                                                                                     | 平衡长期目标与 KISS/YAGNI                                                                                                                                      | 2026-07-16 |
| 3   | 采用 CDE + 工作流 + AI 能力平台 + 专业工具连接器                                                                     | 支撑跨阶段、跨专业、跨工具协同                                                                                                                                 | 2026-07-16 |
| 4   | IFC、IDS、BCF 分别用于模型、信息要求和问题交换                                                                       | 遵循 openBIM 标准职责边界                                                                                                                                      | 2026-07-16 |
| 5   | 所有 AI 结果按风险等级进入人工复核                                                                                   | 保留专业责任和发布控制                                                                                                                                         | 2026-07-16 |
| 6   | D01–D46 为条件性设计基线，未通过开工准入前不称 Implementation Ready                                                  | Open Decision、精确版本与测试证据仍需实例化                                                                                                                    | 2026-07-16 |
| 7   | EVAI、小库 AI、建筑学长默认采用 `ManualHandoff`                                                                      | 未发现公开 API/Key 控制台；正式自动接入须通过供应商资格与书面授权                                                                                              | 2026-07-19 |
| 8   | `施工图全流程AI平台综合建设方案.md` 为人读版派生方案                                                                 | 支撑汇报和跨团队评审，不建立第二套设计事实源                                                                                                                   | 2026-07-19 |
| 9   | V1 技术试点采用通用英文境外、中小型办公、建筑纵向闭环                                                                | 技术验证优先；结构/MEP 先保留交换与协调                                                                                                                        | 2026-07-19 |
| 10  | OD-01 首次产品化：通用英文境外，ISO/EN 优先，公制 SI，境外云 Region 厂商待定                                         | 规范版权清晰；与原始"境外主创草图"业务场景一致；GoldenDataset 标注成本可控                                                                                     | 2026-07-22 |
| 11  | OD-02 首批建筑类型：中小型办公（5–15 层，框架/框剪），排除超高层和医疗/实验室                                        | 结构规整、规则集成熟；碰撞场景典型；标注成本可控                                                                                                               | 2026-07-22 |
| 12  | OD-03 首批专业深度：建筑纵向闭环，结构/给排水/暖通/电气交换与协调，专项不纳入 V1                                     | 与决策 9 一致；建筑验证核心 AI 能力，MEP 保留交换控制成本                                                                                                      | 2026-07-22 |
| 13  | OD-04 工具版本：Revit/AutoCAD 2022/2024、Rhino 7/8、SketchUp 2023/2024 Pro、ArchiCAD 26/27；商用求解器延后至结构闭环 | 双版本覆盖主流客户；版本升级须重跑 D45 金样；V1 结构非闭环故求解器延后                                                                                         | 2026-07-22 |
| 14  | OD-05 外部 AI：EVAI/小库 AI/建筑学长 V1 维持 ManualHandoff，W3 并行启动供应商资格接触                                | V1 不阻塞；B-02 降级为 V2 自动化前置条件；数据处理须境外云+不用于训练+GDPR 兼容                                                                                | 2026-07-22 |
| 15  | OD-06 首个生产画像：Hybrid-Site，RPO≤4h/RTO≤8h 初始值待 Pilot 校准                                                   | D29-D33 连接器需本地 Worker 驱动 CAD/BIM，纯 SaaS 无法满足；与 OD-01+OD-04 一致无冲突                                                                          | 2026-07-22 |
| 16  | 设计正文按 D01-D46 章节拆分为独立文件（D00-D46 共 47 个文件），命名规范 Dxx-小写短横线标题.md                        | 原文件 16090 行过大，IDE 加载慢、搜索效率低；D01-D46 编号体系天然适合拆分；拆分后仍维持唯一事实源原则，不产生第二套设计事实；引用约定更新为 @design/Dxx-xxx.md | 2026-07-22 |

## 当前状态

- 阶段：D01–D46 条件性设计基线完成；R1 冻结完成；R2 主体完成；R3 TEVV 完成；R4 Gate 验证完成；**V0 全栈验证完成**（2026-07-29 更新：治理域前后端全链路 + P01-P04/P07/P09/P11/P12 关键界面 + 运营中心 API 对接预留 + Change 域 Java 后端 V0 基线完成）。远程环境全栈运行：7 服务 running、前端 24+ 页面 200、API 全链路通（含 IAM/CDE/Portfolio/Workflow/TEVV/合规规则/检查/规则集/发现项/治理 10 域）、Auth 登录 code 0 + Cookie 认证就绪、Flyway 14/14 迁移成功、AI DeepSeek 就绪。治理域 Java 后端 V0 基线 230 测试全部通过（含 124 治理域测试），前端 5 个治理中心页面全部接入真实 API。前端 P01-P04 关键界面（我的工作/项目首页/CDE 资产库/需求追踪矩阵）对齐 D37.5-D37.8 设计基线；P07 协调工作台对齐 D37.11；P09 AI/Agent 复核中心对齐 D37.13；P11 专业提交、校审与发布向导对齐 D37.15；P12 变更影响与闭环工作台对齐 D37.16；运营中心（Monitoring/Operations）对齐 D37.17（V0 前端骨架 + V1 API 对接预留）。Change 域 Java 后端 V0 基线完成（2026-07-29）：14 个枚举、5 个子域（ChangeRequest/AffectedItem/TaskPlanItem/ClosureEvidence/ChangeOperation）完整 DDD 五层结构、V18 Flyway 迁移脚本 5 张表、ChangeRequestService 11 个业务方法实现全状态机校验（含 stepUpToken 高风险保护 + 职责分离 + UNKNOWN 影响项阻断关闭），`mvn clean compile` 335 文件 BUILD SUCCESS。Change 域集成测试 4 阶段全部通过（A-19/A-20：Schema IT + Repository IT 17 用例 + Service IT 29 用例 + Controller API IT 14 用例，共 60 个用例 0 失败）。**BFF Change 域代理层端到端联调完成**（2026-07-29 A-21）：修正 `packages/shared/src/contracts/change.contract.ts` 中 `ChangeApiPaths` 6 处路径与后端 `@PostMapping` 不一致（submit-impact-assessment→submit-impact、approve/reject/recall/verify-closure 由冒号语法改为斜杠、task-plans:generate 改为 task-plans/:generate），补充 15 个子实体 CRUD 端点（affected-item/task-plan/closure-evidence 的 detail/create/update/delete + recheck/verify/start 动作）；更新 BFF `ChangeProxyController` 注释列出 5 个后端 Controller 全部端点；补充 BFF 单元测试至 15 个用例（覆盖 5 个状态流转端点 + 3 个子实体动作端点 + traceId fallback + 非 2xx 透传），`pnpm -r typecheck` 0 错误、`pnpm --filter @design-platform/bff lint` 0 错误、`pnpm vitest run change-proxy.controller.test.ts` 15/15 通过。V0 空状态对齐 D37 §空状态红线。尚未达到 Implementation Ready（待合规规则种子数据导入 + 执行引擎实现 + R2 W4-W8 厂商冻结）。
- 最近动作：完成设计正文按 D01-D46 章节拆分。将唯一设计正文 `deep-research-report.md`（16090 行）按章节拆分为 47 个独立文件（D00-D46），命名规范为 `Dxx-小写短横线标题.md`。拆分脚本位于 `scripts/split-design-doc.py`，每个文件添加了统一文档头部（来源、创建/更新日期、原始行范围、引用约定）。同步更新 `INDEX.md`，新增 D01-D46 章节索引表、章节分组统计、命名规范和引用约定。新增决策 16 记录本次拆分决策。原文件归档至 `his_bak/v1.0-Design-deep-research-report-20260722.md`。
- 最近动作：完成 PrismScan L2-project 规则库深度分析与提取，将 38 条通用规则按项目技术栈和设计要求适配后整合到 `.trae/rules/`，规则文件由 8 个扩展至 16 个。新建 8 个规则文件（`security.md` 合并 security+privacy+auth 核心原则、`testing.md` 多语言测试统一标准、`database.md` PostgreSQL 16+Flyway 规范、`api-conventions.md` 跨语言 API 契约对齐 r2-contract-catalog、`observability.md` 合并 observability+error-monitoring 含 Sentry 集成、`deployment.md` Hybrid-Site 灰度发布与回滚、`code-review.md` 含项目特化 Approval 规则、`devex.md` 开发环境标准化含 Husky/lint-staged）；强化 6 个现有规则文件（`coding-standards.md` 补充核心工程原则与代码风格、`frontend.md` 补充 WCAG 2.2 AA+i18n+性能 SLO、`bff.md` 补充 JWT/RBAC+限流熔断+降级策略、`backend-java.md` 补充构造器注入+Saga/Outbox+ArchUnit、`backend-python.md` 补充 AI/ML 实践含 Prompt 结构化+RAG chunking+Guardrails、`git-commit-message.md` 补充分支策略+依赖更新策略）。6 条规则经评估不提取（`reference-driven-development` 为 PrismScan 内部规则、`grpc` 项目用 REST/HTTP、`cli-design` 无 CLI 工具、`seo-foundations`/`seo-ai-overview` V1 无 SEO 页面、`dart-style` 无 Dart/Flutter）。冲突适配：PrismScan `conventions.md` 禁用 Ant Design 推荐 shadcn/ui，但项目 OD 锁定 Ant Design 5，故所有 UI 组件引用适配为 Ant Design 5。同步更新 `AGENTS.md` 规则表从 8 条扩展至 16 条（5 始终生效 + 10 glob 匹配 + 1 场景匹配）。
- 最近动作：将 Qoder 项目 `deep-research-report.md` 中以"业界对标审计补充"为主题的 14 个新增章节（795 行）深度分析后整合到本项目唯一设计正文，涵盖快速导航（角色/冲刺阅读指南、D01-D46 首切片相关性标记）、§7 V0/V1 最小可开发范围裁剪（开发聚焦、明确不实现、简化技术栈、ISO 19650 对标、EDTR 基线、Q-01~~Q-10 决策表）、D12.23.1 施工图自动化现实性校准（2025-2026 能力矩阵）、D20.21.1 RAG 设计对标 2025 最佳实践、D24.21.1 AI 能力现实性校准（V0 最小 AI 集）、D28.23.1 AI 能力分阶段治理（V0 轻量→V3 企业级）、D33.24.1 CAD/BIM 集成与 openBIM 现实性审计、D34.8.1 V0 最小数据模型、D34.24.1 V0 模块化单体部署架构、D35.15.1 V0 关键 API 契约示例（5 个 OpenAPI）、D40.26.1 OWASP LLM Top 10 与 EU AI Act 对标、D46.7.1 V0 技术选型审计与简化决策、D46.13.1 OD 推荐默认值与阻塞性分析、附录 B V0/V1 首切片开发实施指南（7 节含 Sprint 1-10 开发顺序、3-6 人团队分工、架构演进路线图）。三处与 R1-R2 冲突的内容已适配：①D46.13.1 OD 表新增"R1 冻结值"列，并附差异说明（OD-01 推荐中国+境外并行→冻结为纯境外包；OD-06 推荐SaaS-Managed→冻结为 Hybrid-Site）；②§7.5 Q-01~~Q-10 决策表后追加 R1 冻结状态说明，列出 6 项冻结值并引用 R1/R2 交付物；③D46.7.1 V0 技术选型追加 R2 交叉引用，说明 V0 简化技术栈与 Hybrid-Site 生产画像的升级路径关系。原文件 15298 行 → 整合后 16090 行（+792 行）。
- 最近动作：R2 阶段完成四项工作：①将 R1 决策（OD-01 至 OD-06）回写至唯一设计正文 D01.15（待业务决策清单标记 Q-01 至 Q-06 已冻结）和 D02.7/D02.12（版本基线、决策结果表新增冻结状态列）；②输出 R2 Support Matrix 冻结文档（`r2-support-matrix/r2-support-matrix.html`），冻结 5 类工具 10 个版本资格矩阵；③输出 R2 Contract Catalog 分配文档（`r2-contract-catalog/r2-contract-catalog.html`），分配首切片 6 个 API 域 30 个 Operation、18 个 Event Topic、8 个 File Schema 共 48 个契约稳定 ID，定义版本兼容策略和 Consumer Test 计划；④输出 R2 DeploymentProfile 实例化文档（`r2-deployment-profile/r2-deployment-profile.html`），将 OD-06 Hybrid-Site 决策实例化为 Region/Cell/Cluster 冻结参数、9 个信任区流量矩阵、WorkerImageProfile、DR 分层 RPO/RTO、9 个 Registry 冻结状态和 15 项 Runbook Catalog。Gate 4 由"部分满足（契约设计完成待分配）"推进至"部分满足（已分配待 Consumer Test 验证）"；Gate 5 由"未满足"推进至"部分满足（首画像已冻结，Registry 字段待 W4–W8 全量填充）"。
- 最近动作：R3 TEVV 模块开发完成。Java Core 层实现 GoldenDataset 和 VerificationItem 实体/服务/API（10 个单元测试通过）；BFF 层新增 GoldenDatasetProxyController 和 VerificationItemProxyController 代理路由（6 个集成测试通过）；前端层实现 /golden-datasets 数据集管理页和 /golden-datasets/[id] 验证项管理页；shared 包新增 TEVV 契约类型（DTO + 枚举 + API 路径）；OpenAPI 规范 docs/api/tevv.yaml 补齐。R4 Gate 准入验证完成：Gate 2 测试物化（BFF 126/126 测试通过，含 TEVV 代理 6 个 + Gate 6 端到端 10 个）；Gate 3 契约 Consumer Test（shared 契约 + OpenAPI 规范就位）；Gate 4 部署画像（Docker Compose 4 服务编排 + 4 个 Dockerfile 非 root 用户）；Gate 5 运维就绪度（健康检查 + 结构化日志 + 4 个 CI 工作流）；Gate 6 全链路端到端验证（ApiResponse 透传 + 错误格式转换 + traceId 传播 + 租户隔离）。提交记录：fb86f9c（TEVV BFF 代理+前端页面）、499a749（验证项前端页面）、54b55a8（TEVV 契约+OpenAPI+Gate 6 E2E）。
- 最近动作（2026-07-27）：远程测试环境部署完成。在 116.8.109.229 服务器创建 AiDesign 隔离用户，Docker Compose 全栈启动 7 个服务（postgres/minio/chromadb/core/ai/bff/web），Nginx 子路径 `/aidesign/` 反向代理配置完成，修复 Next.js basePath 与 Nginx proxy_pass 前缀冲突导致的 404 问题。前端可通过 `https://yun.gxjugu.com/aidesign/` 访问（Dashboard/Login/Members/Projects 等 21 个页面可正常加载）。提交记录：3c567cb（远程部署配置与 404 修复，含 compose.production.yml、deploy-remote.sh、Nginx 脚本、Dockerfile 优化）。
- 最近动作（2026-07-27）：合规检查 BFF 代理补齐。新增 4 个 BFF 代理控制器：ComplianceRuleProxyController（/api/v1/compliance-rules，含规则 CRUD + 修订管理 + IDS 导入 + 激活）、ComplianceCheckProxyController（/api/v1/compliance-checks，含检查运行创建/执行/查询）、RuleSetProxyController（/api/v1/rule-sets，含规则集管理 + 规则添加/移除）、FindingProxyController（/api/v1/compliance-findings，含发现项查询/更新）。契约验证策略：创建操作使用严格验证（complianceRuleDtoSchema/complianceCheckRunDtoSchema），查询操作使用软验证（仅观察契约漂移不阻断），规则集保持透传。BFF typecheck 通过，42 文件 485 测试通过（5 个 Auth CookieService Mock 历史遗留失败与本次变更无关）。BFF/Web 镜像重新构建并部署到远程，4 个端点 API 认证通过（code=0），外部域名合规页面全部 200。数据通路：前端 → BFF 代理 → Core Service 完整闭环。
- 下一步：配置真实可用的 LLM_API_KEY（DeepSeek 或 Qwen）以解锁 text-generation 和 RAG 查询端点；前端 P08 RAG 知识库管理页面对齐收尾验证（已基本完成，待 LLM_API_KEY 配置后验证检索问答端到端）；危险动作实际执行流程已闭环（A-38 完成），V1 阶段补充完整 Worker/QueueTask 数据后可验证业务执行链路（当前占位 ID 返回 404 为预期行为）。
- 阻塞项：B-01 已解除；B-02 降级为 V2 前置条件；B-03 部分解除（版本基线冻结，资格验证待 W4–W7）；B-04 已解除（R3 TEVV 测试物化完成）；B-05 标记 V1 不适用；B-06 已解除；B-07 部分满足；B-08 部分满足；B-09/B-10 待 W5/W8；B-11/B-12/B-13/B-14 已解除（V0 全栈验证完成）。

## 设计演进日志

- 2026-07-29：A-29 Analysis 域前后端契约对齐与 BFF 代理层补齐完成（移除前端 Mock 数据 + 真实 API 切换 + 14 个 BFF 单元测试通过）。对照 `@design/D37-关键界面-交互状态.md §D37.14 P10 工程分析运行与结果质量`，按"前端切换真实 API + BFF 代理补齐 + 后端 Core Service 实现"分层推进。①更新 `packages/shared/src/contracts/analysis.contract.ts`：补充 `CreateAnalysisProblemRequest`/`InvalidateProblemRequest`/`CancelRunRequest`/`CreateSimulationRunRequest`/`CreateAnalysisScenarioRequest`/`SubmitQualityAssessmentRequest` 6 个请求类型定义，对齐后端 Java DTO；新增 `SubmitQualityAssessmentRequest` 安全红线注释（ACCEPT_AS_REVISION/EXCEPTION 决策需注册师签章 sealId + 高风险决策需 stepUpToken 二次认证）；扩展 `SolverProfileDto` 注释说明 V0 阶段字段映射策略（实体保留新字段 maxConcurrentRuns/maxDurationSec/licensePool/isInternal/region/...，DTO 返回旧字段 licenseType/available/estimatedCost/estimatedDurationMin 对齐前端契约）；扩展 `ConvergenceMetricDto` 注释说明后端 history 字段为 jsonb 字符串、前端按 number[] 处理；扩展 `AnalysisScenarioDto` 注释说明 parameters 为结构化数组、由后端 jsonb 列持久化与序列化。②改造 `apps/web/src/hooks/use-analysis.ts`：删除所有 Mock 数据（MOCK_PROBLEMS/MOCK_SCENARIOS/MOCK_RUNS/MOCK_RESULTS/MOCK_TIMELINE/MOCK_CONVERGENCE/MOCK_SOLVER_PROFILES/MOCK_QUALITY_ASSESSMENT），使用 `apiGet`/`apiPost`/`apiPut`/`apiDelete` 调用真实 BFF 代理；新增 `adaptPage` 工具函数将后端 PageResponse.data（{ list, total, page, pageSize, hasMore }）适配为前端 OffsetPageResponse（{ items, total, page, pageSize, hasMore }），兼容 list/items 两种字段名；实现 `useAnalysisProblems`/`useAnalysisProblem`/`useCreateAnalysisProblem`/`useUpdateAnalysisProblem`/`useDeleteAnalysisProblem`/`useSubmitAnalysisProblem`/`useInvalidateAnalysisProblem`/`useMeshQuality`/`useAnalysisScenarios`/`useCreateAnalysisScenario`/`useDeleteAnalysisScenario`/`useSimulationRuns`/`useSimulationRun`/`useCreateSimulationRun`/`useCancelSimulationRun`/`useRetrySimulationRun`/`useRunTimeline`/`useRunConvergence`/`useRunResults`/`useAnalysisResult`/`useResultQuality`/`useSubmitQualityAssessment`/`useCreateImpactProposal`/`useSolverProfiles`/`useProblemStatusSummary` 25 个 TanStack Query hooks；retry 策略对 404/501（后端未实现或未启动）跳过重试避免浪费请求；`useAnalysisProblem`/`useMeshQuality`/`useAnalysisScenarios`/`useRunTimeline`/`useRunConvergence`/`useRunResults`/`useAnalysisResult`/`useResultQuality`/`useSolverProfiles` 9 个 hook 在 404/501 时返回 null/空数组由组件显示空状态，不抛错。③改造 `apps/web/src/app/(dashboard)/analysis/page.tsx` 列表页：将 `const problems = data ?? []` 修正为 `const problems = data?.items ?? []`，正确处理 `OffsetPageResponse<AnalysisProblemDto>` 类型；更新页面标题副标题与 V0 限制 Alert 文案为"对接后端真实 API"；更新空状态副文案为"可切换筛选条件或检查后端服务状态"。④改造 `apps/web/src/app/(dashboard)/analysis/problems/[problemId]/page.tsx` 详情页：移除 `MOCK_QUALITY_ASSESSMENT` 引用，改为调用 `useResultQuality` hook 获取真实质量评估数据；移除 `latestRunId = problem?.latestRunId ?? "run-003"` Mock fallback，改为 `latestRunId = problem?.latestRunId ?? null`，problem 未关联运行时不查询运行相关数据；更新 V0 限制 Alert 文案为"对接后端真实 API"；修正 `RunMonitorPanel` 子组件签名 `runId: string` → `runId: string | null`，`QualityRailPanel` 子组件签名 `assessment: ResultQualityAssessmentDto | null` → `assessment: ResultQualityAssessmentDto | null | undefined`，兼容 useResultQuality 返回的 undefined 加载态。⑤更新 `services/core/src/main/java/com/platform/core/analysis/solver/dto/SolverProfileDto.java`：重写为 record，仅保留前端契约所需 8 个字段（id/name/version/solverType/licenseType/available/estimatedCost/estimatedDurationMin），添加 V0 字段映射说明注释。⑥更新 `services/core/src/main/java/com/platform/core/analysis/solver/service/SolverProfileService.java` 的 `toDto(SolverProfile entity)` 方法：从实体新字段派生 DTO 旧字段——licenseType（内部求解器默认 floating，外部 Provider 默认 cloud）、estimatedDurationMin（maxDurationSec / 60 向上取整）、estimatedCost（V0 简化为 0，V1 按 licensePool 派生），available 取实体 isActive()，确保前端获取到正确格式的求解器配置数据。⑦新增 `apps/bff/tests/unit/proxy/analysis-proxy.controller.test.ts` 14 个测试用例：GET /v1/analysis/problems 透传查询参数与授权头、POST /v1/analysis/problems 转发 body（创建草稿）、POST /v1/analysis/problems/:id/invalidate 转发高风险动作 body（含 stepUpToken）、POST /v1/analysis/runs/:id/cancel 转发取消运行 body（含 stepUpToken）、POST /v1/analysis/results/:id/quality-assessment 转发质量评估 body（含 sealId）、DELETE /v1/analysis/problems/:id 透传（body 为 undefined）、GET /v1/analysis/problems/:id/scenarios 透传子资源路径、GET /v1/analysis/runs/:id/timeline 透传运行时间线路径、GET /v1/analysis/solver-profiles 透传求解器配置列表路径、traceId fallback 使用 request.traceId、非 2xx 响应原样透传（如 404 后端未实现）、query 数组参数归一化为 string[]、GET /v1/analysis/problems/:id/mesh-quality 透传网格质量路径、POST /v1/analysis/results/:id/impact-proposal 转发变更影响提案 body（含 stepUpToken）。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm --filter @design-platform/bff lint` 通过（exit code 0，14 个历史遗留 warnings 与本次变更无关，0 errors），`pnpm exec vitest run tests/unit/proxy/analysis-proxy.controller.test.ts` 14/14 通过（350ms）。数据通路：前端（use-analysis.ts）→ BFF（AnalysisProxyController @All("*") 纯透传）→ Core Service（AnalysisProblemController/AnalysisScenarioController/SimulationRunController/AnalysisResultController/SolverProfileController/ConvergenceMetricController/RunTimelineEventController/MeshQualityController）完整闭环，路径对齐 D37.14 §D37.14 P10 工程分析运行与结果质量设计基线。V0 差距记录：①Analysis 域 Core Service 端到端联调待 V1 部署阶段后端服务启动后执行；②Analysis 域 Repository/Service/Controller 集成测试（参考 A-19 Change 域模式）待后续阶段实现；③Analysis 域 MeshQuality/RunTimelineEvent/ConvergenceMetric Controller 端点待 V1 实现；④Analysis 域 AI 辅助场景推荐/参数推荐管道待 V1（依赖 LLM Provider 接入）；⑤Analysis 域结果自动 supersede 与 Benchmark 对比待 V1；⑥前端 dashboard/monitoring/app-layout 测试与页面预期不同步问题（7 个失败测试与本次 Analysis 域变更无关）待后续修复。权威源：@design/D37-关键界面-交互状态.md §D37.14、@design/D33-工程分析-求解.md、@design/D35-API-事件契约.md、frontend.md（Next.js 15/React 19/Ant Design 5/WCAG 2.2 AA）、security.md（AI 安全红线、stepUpToken 二次认证、sealId 注册师签章）、design-constraints.md（AI 结果人工复核红线）。

- 2026-07-29：A-30 前端 dashboard/monitoring/app-layout 测试与页面预期同步修复完成（7 个失败 → 0 失败 + typecheck/lint 通过）。针对 A-29 验证中标记的"前端 dashboard/monitoring/app-layout 测试与页面预期不同步问题（7 个失败测试与本次 Analysis 域变更无关）待后续修复"作为独立小任务推进，按"测试预期对齐当前页面真实文案 + 补充 Operations hooks mock"分层修复。①`apps/web/tests/unit/components/app-layout.test.tsx`（1 个失败 → 修复）：菜单项预期 "Dashboard" → "My Work"（对齐 `app-layout.tsx` SIDER_MENU_ITEMS 第 56 行 `// 路由保持 /dashboard 兼容现有跳转，label 对齐 D37.5 P01 "My Work"`），其余菜单项 "Projects/Stage Gate/Documents/Compliance Rules/Settings" 维持不变。②`apps/web/tests/unit/app/dashboard-page.test.tsx`（3 个失败 → 修复）：欢迎标题预期 "欢迎使用施工图全流程 AI 平台" → "我的工作台"（对齐 `dashboard/page.tsx` 第 476 行 `<Title level={3}>我的工作台</Title>`）；项目定位说明预期 "建筑专业纵向闭环 — 境外主创草图到方案深化" → "建筑专业纵向闭环" Tag（对齐第 474 行 `<Tag>建筑专业纵向闭环</Tag>`）；全流程覆盖说明正则 `/覆盖前期策划、概念设计、方案设计、扩初设计、施工图设计/` → `/覆盖前期策划、概念设计、方案设计、扩初设计、施工图/`（页面已扩展为"覆盖前期策划、概念设计、方案设计、扩初设计、施工图、多专业协同、审查、交付与变更"）。③`apps/web/tests/unit/app/monitoring-page.test.tsx`（3 个失败 → 修复）：页面已从简单 health 监控扩展为完整运营中心（D37.17），新增 SLO/Queue/Worker/Connector 4 个 Tab + 5 个 Operations hooks 调用；测试补充 `mockOperationsHooks`（useOperationsOverview/useSlos/useQueueTasks/useWorkers/useConnectors）返回空数据避免真实 API 调用；错误状态预期 "无法获取系统健康状态"/"未知错误" → "系统健康状态加载失败"（对齐 `DataErrorAlert` context="系统健康状态" + describeError() 第 87 行 `${context}加载失败`）；非 Error 对象预期 "未知错误" → "系统健康状态加载失败" + "请稍后重试"（对齐 describeError() 默认分支）；加载态 Spin 类名预期 `.ant-spin-lg` → `.ant-spin-spinning`（页面 `<Spin spinning={healthLoading}>` 未指定 size，Ant Design 5 默认 size 不渲染 .ant-spin-lg 类）。验证结果：`pnpm --filter @design-platform/web exec vitest run tests/unit/app/dashboard-page.test.tsx tests/unit/app/monitoring-page.test.tsx tests/unit/components/app-layout.test.tsx tests/unit/hooks/use-monitoring.test.tsx` 4 个测试文件 26/26 通过（2.37s），`pnpm --filter @design-platform/web typecheck` 通过（exit code 0，tsc --noEmit 无错误），`pnpm --filter @design-platform/web lint` 通过（exit code 0，仅 dashboard/governance 等无关页面 7 个 react-hooks/exhaustive-deps Warning 与本次变更无关，0 errors）。同步更新 BEACON.md "下一步"任务列表移除"修复前端 dashboard/monitoring/app-layout 测试与页面预期不同步问题"。V0 差距记录：①dashboard-page.test.tsx 仅验证顶部欢迎区文案，工作项 DataGrid/SavedView/Quick Preview 三栏布局的交互测试待 V1（依赖 useWorkItems/useQuickAction hooks 真实接入）；②monitoring-page.test.tsx 仅验证 useHealth 状态展示，SLO/Queue/Worker/Connector 4 个 Tab 的内容渲染测试待 V1（需补充各 Tab 的 fixture 数据）；③app-layout.test.tsx 仅验证菜单项渲染，Governance 子菜单展开/移动端 Drawer 折叠菜单交互测试待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.5（P01 My Work）/§D37.17（Operations 监控）、frontend.md（Next.js 15/React 19/Ant Design 5/WCAG 2.2 AA）、testing.md（AAA 模式、Mock 规范、测试命名描述行为）、`apps/web/src/components/common/data-error-alert.tsx`（错误分类展示策略）。

- 2026-07-29：A-31 BFF Operations/Analysis 代理层注释对齐与测试补齐完成（5 文件注释更新 + Operations 测试 8→15 用例 + 全部验证通过）。针对 A-29 验证中标记的"BFF OperationsProxyController 与 Core Service 端到端联调（V1）"和"Analysis 域 Core Service 端到端联调"作为前置任务，发现 BFF 代理控制器注释仍标注"后端未实现"但 Core Service 实际已实现 6 个 Operations Controller + 5 个 Analysis Controller，且 shared 包契约头部和前端 hooks 注释存在相同过时描述。按"注释对齐实际后端实现状态 + 测试覆盖对齐 Change 域 15 用例标准"分层推进。①`apps/bff/src/proxy/operations/operations-proxy.controller.ts`（注释更新）：从"端点 → Core Service（Java，V1 待实现）+ V0 策略：纯透传 + 后端 Operations API（/api/v1/operations/**）尚未实现"更新为"端点 → Core Service（Java，已实现，路径对齐 @OperationsOverviewController / @SloController / @QueueTaskController / @WorkerController / @ConnectorController / @OperationsActionController）"+ 列出 6 个 Controller 完整端点清单（Overview GET / SLO CRUD + Queue CRUD + pause/resume/retry/cancel + Worker CRUD + pause/resume + Connector GET + Action POST）+ V0 策略更新为"BFF 仅做透传，由 Core Service 校验危险动作约束与影响预览 + 后端非 2xx 响应原样透传（保留 errorCode/message/traceId）"。②`apps/bff/src/proxy/analysis/analysis-proxy.controller.ts`（注释更新）：从"端点 → Core Service（Java，AnalysisProblemController / AnalysisScenarioController / SimulationRunController / AnalysisResultController / SolverProfileController 等）+ V0 策略：纯透传 + 后端 API 返回 404/501 时由前端显示空状态"更新为"端点 → Core Service（Java，已实现，路径对齐 @AnalysisProblemController / @AnalysisScenarioController / @SimulationRunController / @AnalysisResultController / @SolverProfileController）"+ V0 策略更新为"后端非 2xx 响应原样透传（保留 errorCode/message/traceId）+ BFF 仅做透传，不解析业务字段"。③`packages/shared/src/contracts/monitoring.contract.ts`（头部注释更新）：从"V0 前端骨架，后端 API 待 V1 实现 + V0 简化（前端骨架 + V1 API 对接预留）+ 后端 Operations API 未实现时（404/501）前端显示空状态"更新为"后端实现状态（2026-07-29）：Core Service 已实现 6 个 Controller + 路径与 BFF OperationsProxyController（@Controller("v1/operations") + 全局前缀 /api）完全对齐 + 前端通过 MonitoringApiPaths 直连 BFF 代理 + 后端非 2xx 响应原样透传"。④`packages/shared/src/contracts/analysis.contract.ts`（头部注释更新）：从"V0 阶段：仅前端骨架，后端 API 未就位 + V0 简化：仅定义类型与 API 路径占位，供前端骨架使用 + 后端 API 在 V1 阶段实现 + 前端使用 Mock 数据展示骨架结构"更新为"后端实现状态（2026-07-29）：Core Service 已实现 5 个 Controller + 路径与 BFF AnalysisProxyController 完全对齐 + 前端通过 AnalysisApiPaths 直连 BFF 代理"。⑤`apps/web/src/hooks/use-monitoring-operations.ts`（注释更新）：从"V0 阶段，D37.17 运营中心 + 后端 Operations API（/api/v1/operations/**）尚未实现 + 前端通过这些 hooks 提供统一查询入口；后端实现后无需修改组件代码 + 当 API 返回 404 / 501（未实现）时组件层显示空状态"更新为"D37.17 运营中心 + 后端实现状态（2026-07-29）：Core Service 已实现 6 个 Controller + 前端通过 MonitoringApiPaths 直连 BFF 代理 + 后端非 2xx 响应由组件层显示空状态或错误提示"。⑥`apps/bff/tests/unit/proxy/operations-proxy.controller.test.ts`（测试补齐 8 → 15 用例）：参照 ChangeProxyController 15 用例标准，新增 7 个测试用例覆盖 Queue 子动作端点（POST /v1/operations/queue/:id/pause 暂停任务 / resume 恢复任务 / retry 重试任务 / cancel 取消任务含 stepUpToken + impactPreviewAcknowledged）+ Worker 子动作端点（POST /v1/operations/workers/:id/pause 暂停 Worker / resume 恢复 Worker）+ query 数组参数归一化（GET /v1/operations/queue?status=running&status=queued → query.status: ["running","queued"]）。验证结果：`pnpm --filter @design-platform/bff typecheck` 通过（exit code 0，tsc --noEmit 无错误）；`pnpm --filter @design-platform/bff lint` 通过（exit code 0，14 个历史遗留 warnings 与本次变更无关，0 errors）；`pnpm --filter @design-platform/shared typecheck` 通过（exit code 0）；`pnpm --filter @design-platform/web typecheck` 通过（exit code 0）；`pnpm --filter @design-platform/bff exec vitest run tests/unit/proxy/operations-proxy.controller.test.ts` 15/15 通过（453ms）；回归验证 `analysis-proxy.controller.test.ts` 14/14 + `change-proxy.controller.test.ts` 15/15 通过（379ms），三个代理控制器总测试数 44/44 通过。数据通路：前端（use-monitoring-operations.ts → MonitoringApiPaths）→ BFF（OperationsProxyController @Controller("v1/operations") + @All("_") + 全局前缀 /api → /api/v1/operations/\**）→ Core Service（6 个 Controller 路径对齐）完整闭环；前端（use-analysis.ts → AnalysisApiPaths）→ BFF（AnalysisProxyController @Controller("v1/analysis") + @All("_") + 全局前缀 /api → /api/v1/analysis/**）→ Core Service（5 个 Controller 路径对齐）完整闭环。下一步：V0 剩余端到端场景验证（文件上传→MinIO 链路）；远程环境部署验证（Change 域 V18 + Operations 域 V19 + Analysis 域 V20 迁移 + Core 重建部署）；BFF OperationsProxyController 与 Core Service 端到端联调（V1，待远程部署 Core 镜像重建）；Analysis 域 Core Service 端到端联调（BFF 代理已就位，前端已切换真实 API，需远程部署后端服务启动后做端到端验证）。权威源：@design/D37-关键界面-交互状态.md §D37.14/§D37.17、@design/D35-API-事件契约.md、bff.md（NestJS 11 代理透传 + traceId 传播 + 非 2xx 透传）、testing.md（AAA 模式、Mock 规范、子动作端点路径对齐验证）、design-constraints.md（AI 安全红线、危险动作约束）。

- 2026-07-29：A-33 V0 文件上传链路代码路径预检完成（前端→BFF→Core Service 闭环验证通过 + V1 MinIO 预签名直传差距记录）。针对 A-31/A-32 标记的"V0 剩余端到端场景验证（文件上传→MinIO 链路）"作为前置任务，按"前端页面 → 前端组件 → BFF 上传控制器 → BFF 存储服务 → Core Service 文档控制器 → MinIO/S3 集成"6 层链路逐层预检代码路径完整性与契约对齐。①**前端页面层**（`apps/web/src/app/(dashboard)/projects/[id]/documents/page.tsx`）：第 29 行 `import { DocumentUpload } from "@/components/cde/document-upload"` + 第 252 行 `<DocumentUpload projectId={projectId} onUploadComplete={() => void refetch()} />` 渲染上传组件，projectId 通过路由参数注入，上传完成后触发 `refetch()` 刷新文档列表。②**前端组件层**（`apps/web/src/components/cde/document-upload.tsx`）：第 220 行 `xhr.open("POST", CdeApiPaths.upload(projectId))` + 第 221 行 `xhr.withCredentials = true` + 第 222 行 `xhr.send(formData)` 通过 XMLHttpRequest 发送 multipart/form-data 请求（含 file 与 comment），支持进度回调与错误处理；状态机覆盖 queued/hashing/uploading/verifying/completed/failed 6 个状态对齐 D37.8 P04 CDE 资产库与上传中心；文件类型白名单（.rvt/.rfa/.3dm/.skp/.dwg/.dxf/.pdf/.docx/.xlsx/.pptx/.zip）+ 50MB 大小限制对齐 security.md §4 文件上传白名单。③**契约层**（`packages/shared/src/contracts/cde.contract.ts` 第 193 行）：`upload: (projectId: string) => `/api/v1/projects/${projectId}/documents/upload`` 路径定义，与后端端点完全对齐。④**BFF 上传控制器层**（`apps/bff/src/proxy/cde/upload.controller.ts`）：`@Controller("v1/projects/:projectId/documents")` + `@Post("upload")` + `@UseInterceptors(FileInterceptor("file"))` 接收 multipart/form-data；流程为 1) 校验 file 存在（缺失返回 400）→ 2) 从 request.params 解析 projectId + 从 request.body 解析 comment + 从 x-tenant-id 头解析 tenantId → 3) 调用 `StorageService.store()` 存储文件 → 4) 构造 createDocBody（含 name/path/mimeType/sizeBytes/storageKey/checksum/comment）→ 5) 调用 `ProxyService.forward()` 转发 JSON 到 Core Service（覆盖 content-type: application/json 避免核心端 HttpMediaTypeNotSupportedException）→ 6) 严格验证响应 schema（documentDtoSchema）；控制器在 `proxy.module.ts` 第 23 行 import + 第 85 行 controllers 数组注册，路由优先级在 ProxyController 之前确保正确匹配。⑤**BFF 存储服务层**（`apps/bff/src/storage/storage.service.ts`）：`StorageService.store()` 实现 V0 本地磁盘存储——路径 `{STORAGE_ROOT}/{tenantId}/{uuid}/{originalName}` + storageKey 格式 `{uuid}/{originalName}`（不含根路径便于日后迁移至 S3 Key）+ sha256 checksum 计算 + mkdir recursive 确保目录存在 + writeFile 写入 Buffer；`STORAGE_ROOT = process.env.FILE_STORAGE_ROOT ?? "/home/AiDesign/data/files"` 与 compose.production.yml 第 222 行 `volumes: - /home/AiDesign/data/files:/data/files` 持久化卷对齐；注释明确标注"V0 本地磁盘存储，V1 迁移至 MinIO/S3"。⑥**Core Service 文档控制器层**（`services/core/src/main/java/com/platform/core/cde/controller/DocumentController.java` 第 57 行）：`@PostMapping("/projects/{projectId}/documents")` + `@Valid @RequestBody CreateDocumentRequest request` + `tenantResolver.resolveTenantId(httpRequest)` 接收 JSON 请求，调用 `documentService.createDocument(tenantId, projectId, request)` 持久化文档与初始版本 v1 到 PostgreSQL，返回 `ApiResponse<DocumentDto>`（HTTP 201 CREATED）；`DocumentVersion.java` 第 64 行注释明确"对象存储 Key（S3/MinIO 引用，不可变）"字段已就位，V0 接受 BFF 提供的本地 storageKey，V1 切换为 MinIO object key 无需改 schema。**MinIO/S3 集成现状**：①`compose.production.yml` 已部署 minio:latest + minio-init（创建 bucket + 启用 versioning）+ 端口 127.0.0.1:18020:9000 + 127.0.0.1:18021:9001 + 数据卷 /home/AiDesign/data/minio:/data；②`services/core/src/main/resources/application.yml` 第 98-103 行配置 object-storage（endpoint/access-key/secret-key/bucket/region 全部从环境变量注入）；③`services/core/src/main/java/com/platform/core/common/config/AppProperties.java` 第 99 行 `public static class ObjectStorage` 配置类就位；④Core Service 当前未实现 S3Client/MinioClient bean（grep "S3Client|MinioClient|AmazonS3|software.amazon.awssdk" 无匹配），BackupService 注释明确"V1 简化：创建备份仅记录元数据，实际备份由外部脚本（pg_dump / aws s3 sync）异步执行"；⑤`apps/bff/src/proxy/cde/version-proxy.controller.ts` 第 33-34 行注释"二进制文件上传由前端通过 MinIO 预签名 URL 直传，BFF 仅代理元数据请求"为 V1 设计目标，V0 未实现 presigned URL 端点。验证结果：V0 文件上传链路代码路径完整闭环——前端 DocumentUpload 组件 → POST /api/v1/projects/{projectId}/documents/upload → BFF CdeUploadController 接收 multipart → StorageService 本地存储 → ProxyService 转发 JSON → Core Service DocumentController.create() 持久化 → DocumentDto 响应；契约路径（CdeApiPaths.upload ↔ @Controller + @Post ↔ @PostMapping）三层完全对齐；存储卷（FILE_STORAGE_ROOT ↔ compose.production.yml volume）持久化就位；安全白名单与大小限制对齐 security.md §4。V0/V1 差距记录：①MinIO 预签名 URL 直传端点（POST /api/v1/projects/{projectId}/documents/presign 或类似）待 V1 实现，当前 V0 文件经 BFF 中转存储到本地卷；②Core Service S3Client/MinioClient Bean 与 DocumentService 的 S3 putObject/getObject 集成待 V1 实现；③前端 DocumentUpload 组件 V1 演进为 presigned URL 直传 + 断点续传 + 暂停/恢复 + 恶意扫描状态（paused/scanning/rejected/verified/binding）待 V1（依赖后端 D35 UploadSession API）；④DocumentVersion 在 V0 接受 BFF 本地 storageKey（如 `{uuid}/{originalName}`），V1 切换为 MinIO object key 时需保持 storageKey 字段不变，仅修改 BFF 端写入逻辑；⑤版本上传（POST /v1/documents/:documentId/versions）当前由 CdeVersionProxyController 透传元数据，V1 需补充 presigned URL 直传 + 完成回调端点。下一步：远程环境部署验证（在远程服务器执行 `bash docker/deploy-remote.sh` 启动 6 阶段部署，部署完成后实际验证文件上传→本地卷持久化→文档列表刷新链路）；BFF OperationsProxyController 与 Core Service 端到端联调（远程部署后 Core 镜像重建启动）；Analysis 域 Core Service 端到端联调（BFF 代理已就位 + 前端已切换真实 API + 远程部署后端服务启动后验证）。权威源：@design/D07-CDE领域-版本.md、@design/D37-关键界面-交互状态.md §D37.8（P04 CDE 资产库与上传中心）、@design/D35-API-事件契约.md（CDE 域 API）、frontend.md（Next.js 15/React 19/Ant Design 5）、bff.md（NestJS 11 + FileInterceptor + SchemaValidator + 透传代理）、backend-java.md（Spring Boot 3.4 + @PostMapping + @Valid @RequestBody + 租户解析）、security.md（§4 文件上传白名单 + §2.3 路径穿越防护 + PII L4 专业设计成果分级）、database.md（DocumentVersion 审计字段 + storageKey 不可变约束）、design-constraints.md（AI 安全红线 + V0/V1 渐进式演进策略）。

- 2026-07-29：A-32 远程环境部署验证预检完成（构建链路 + 迁移脚本 + Nginx 反代 + 安全配置 4 个维度全部就位）。针对 A-31 标记的"远程环境部署验证（Change 域 V18 + Operations 域 V19 + Analysis 域 V20 迁移 + Core 重建部署）"作为前置任务，按"构建链路完整性 + 迁移脚本完整性 + Nginx 反代完整性 + 安全配置完整性"4 个维度预检远程部署所需全部资产。①**构建链路完整性**（4 个 Dockerfile 全部就位）：`services/core/Dockerfile` 使用 `maven:3.9-eclipse-temurin-21-aline` 镜像多阶段构建（builder 阶段配置阿里云 Maven 镜像加速 + 依赖预下载 + mvn clean package -DskipTests；runtime 阶段使用 `eclipse-temurin:21-jre-alpine` + 非 root 用户 appuser + EXPOSE 8080），解决本机无 mvnw 问题，构建通过 Docker 内 Maven 完成；`apps/bff/Dockerfile` 使用 `node:20-alpine` + pnpm@9.15.0 + npmmirror.com 加速 + shamefully-hoist=true + 先构建 @design-platform/shared 再构建 @design-platform/bff；`apps/web/Dockerfile` 使用 `node:20-alpine` + pnpm@9.15.0 + npmmirror.com 加速 + 先构建 shared 再构建 web；`services/ai/Dockerfile` 使用 `python:3.12-slim` + 阿里云 PyPI 镜像 + pip install --no-cache-dir + 非 root 用户 appuser + uvicorn 启动。4 个 Dockerfile 均配置国内镜像加速，构建速度有保障。②**迁移脚本完整性**（V1-V20 全部就位，重点验证 V18/V19/V20）：`services/core/src/main/resources/db/migration/` 目录下 V1-V20 共 20 个迁移文件全部就位，V18__init_change.sql（变更域 5 表：change_request/affected_item/task_plan_item/closure_evidence/change_operation，对齐 D37.16 P12 变更影响与闭环工作台 + 安全红线：高风险变更 stepUpToken + 职责分离 + UNKNOWN 阻断关闭 + blocksClosure 校验 + 高风险证据双人复核 + PII L2 分级）；V19__init_operations.sql（运营域 5 表：slo_target/queue_task/worker_status/connector_status/operations_action，对齐 D37.17 运营中心 + 危险动作约束：ISOLATE/FAILOVER/CANCEL 强制 stepUpToken + IRREVERSIBLE 双人审批 + retry storm 检测 V0 占位 + unknown job 显式标识 + 跨 Region 数据驻留）；V20__init_analysis.sql（工程分析域 9 表：analysis_problem/solver_profile/analysis_scenario/simulation_run/run_timeline_event/convergence_metric/analysis_result/result_quality_assessment/mesh_quality，对齐 D37.14 P10 工程分析运行与结果质量 + 安全红线：高风险动作 stepUpToken + 质量评估注册师签章 + 完成运行≠接受结果 + AI 辅助推荐须人工确认 + retry storm V0 占位 + unknown job 显式标识 + supersede 取代关系）。③**Nginx 反代完整性**（`docker/nginx/yun.gxjugu.com.conf` 就位）：4 个 upstream（aidesign_web 127.0.0.1:18070 / aidesign_bff 127.0.0.1:18060 / aidesign_core 127.0.0.1:18040 / aidesign_ai 127.0.0.1:18050）+ keepalive 32；HTTP→HTTPS 301 重定向 + Let's Encrypt webroot 验证路径；HTTPS 主 server（listen 443 ssl http2）配置 TLS 1.2/1.3 + 强密码套件 + ssl_session_cache + ssl_session_tickets off；安全头（HSTS max-age=31536000; includeSubDomains / X-Frame-Options DENY / X-Content-Type-Options nosniff / X-XSS-Protection 1; mode=block / Referrer-Policy strict-origin-when-cross-origin）对齐 security.md §2.1 + §7；client_max_body_size 500M（设计文件上传）；独立访问日志 /var/log/nginx/aidesign-access.log 与错误日志；proxy_set_header 透传 Host/X-Real-IP/X-Forwarded-For/X-Forwarded-Proto/X-Trace-Id。④**安全配置完整性**（`docker/compose.production.yml` + `docker/deploy-remote.sh` 就位）：project name=aidesign 独立命名空间；端口段 18000-18099 仅绑定 127.0.0.1（不对外暴露，由 Nginx 反代）；数据卷 /home/AiDesign/data/{postgres,minio,chroma,files} 持久化；network=aidesign-net 独立 bridge；7 个服务（postgres:16-alpine + minio:latest + minio-init + chromadb:latest + core-service + ai-service + bff + web）全部配置 healthcheck + mem_limit + restart: unless-stopped；deploy-remote.sh 6 阶段（创建 AiDesign 用户 + 加入 docker 组 / 生成 .env.production 含 openssl 强密钥 + chmod 600 / docker compose up -d --build + 等待就绪 18 次重试 / Nginx 配置 + nginx -t 校验 / certbot certonly --webroot 申请 Let's Encrypt 证书 + cron 自动续期 / 6 项连通性验证）；.env.production 含 DB_PASSWORD/JWT_SECRET/S3_SECRET_KEY/CHROMADB_AUTH_CREDENTIALS 4 个 openssl 强密钥 + LLM_API_KEY 留空（V0 不接入 LLM 付费 API）+ PUBLIC_DOMAIN=yun.gxjugu.com。验证结果：4 个 Dockerfile + 20 个迁移脚本 + 1 个 Nginx 配置 + 1 个 compose.production.yml + 1 个 deploy-remote.sh 全部就位，远程部署所需资产完整无缺；本机因 16GB 内存限制不执行 docker compose up 实际构建（避免内存爆炸），实际部署在远程服务器执行；Core Service 无 mvnw 文件不影响构建（Dockerfile 使用 maven 预装镜像）。下一步：在远程服务器执行 `bash docker/deploy-remote.sh` 启动 6 阶段部署；部署完成后执行 V0 端到端场景验证（文件上传→MinIO 链路）；BFF OperationsProxyController 与 Core Service 端到端联调（远程部署后 Core 镜像重建启动）；Analysis 域 Core Service 端到端联调（BFF 代理已就位 + 前端已切换真实 API + 远程部署后端服务启动后验证）。权威源：@design/D37-关键界面-交互状态.md §D37.14/§D37.16/§D37.17、@design/D44-部署拓扑-Hybrid-Site.md、@design/D40-信息-物理安全.md（Step-up 认证 + TLS 配置）、@design/D42-SLO-容量.md（资源限制）、deployment.md（Docker Compose V0 + 灰度发布 + Hybrid-Site 约束）、security.md（密钥管理 + JWT/Cookie + CORS + 安全 Header + PII 分级）、database.md（Flyway 迁移 + 审计字段 + 命名约定）、design-constraints.md（AI 安全红线 + R1 决策冻结 OD-06 Hybrid-Site 部署）。

- 2026-07-29：A-28 前端 P08/P10 页面对齐 D37.12/D37.14 设计基线验证完成（无需修改 + typecheck/lint 通过）。对照 @design/D37-关键界面-交互状态.md §D37.12（P08 规则检查与规范证据审阅器）和 §D37.14（P10 工程分析运行与结果质量）设计基线，审查 apps/web 真实前端代码现状。①P08（`apps/web/src/app/(dashboard)/compliance-results/[id]/page.tsx`）：路由 `/compliance-results/{id}` ✅ 对齐设计基线；三栏布局（左侧 ResultTree 结果树 + 中部 ResultDetailRail 结果详情 + 右侧 VerifyExceptionForm Exception 表单）✅ 对齐设计规格"左侧规则/结果树；中部 Viewer/文档条文；右侧结果、引用、证据、验证/例外"；核心组件齐全：RuleSetBadge/CoveragePanel/ResultTree/ResultDetailRail/VerifyExceptionForm ✅，CoveragePanel 覆盖率统计含 Unknown/NotApplicable 分母对齐 §Unknown/NotApplicable 红线；数据访问层 `use-compliance.ts` 已完全使用真实 API（useQuery/useMutation + apiClient，无 Mock 数据）；主动作"启动执行"对齐 §主动作"验证结果/创建 Issue/发起 Exception"（Issue 创建与 Exception 审批工作流标注 V1 依赖 Issue/Exception API）；V0 限制清晰标注：ClauseViewer/CitationCard/EvidenceGraph 待 V1（依赖 BIM Viewer 与规范条文库）、创建 Issue 待 V1（依赖 Issue API）、Exception 审批工作流待 V1（依赖 Exception API）、多 Execution 聚合待 V1（V0 仅展示首个 execution 的 results）。②P10（`apps/web/src/app/(dashboard)/analysis/problems/[problemId]/page.tsx`）：路由 `/analysis/problems/{problemId}` ✅ 对齐设计基线；6 Tabs 布局（Problem/Input/Scenario/Run/Result/Quality）✅ 对齐设计规格"Problem/Input/Scenario tabs + Run monitor + Result Viewer/Chart/Table + Quality/Evidence rail"；核心组件齐全：InputPanel（Assumption/BC/Load forms + MeshQuality）✅、ScenarioPanel（SolverProfile + 场景列表）✅、RunMonitorPanel（RunTimeline + Residual/Balance charts）✅、ResultViewerPanel（结果概要 + 关键指标表 + Benchmark 对比 + 大结果分层加载提示）✅、QualityRailPanel（QualityChecklist + 评估概要 + 决策类型说明）✅；主动作 3 个按钮（运行 Scenario / 提交结果质量评估 / 创建变更提案）✅ 对齐 §主动作，V0 禁用态 + Tooltip 提示；AI 安全红线落实：requiresHumanReview/isAiAssisted 标识、"完成进程不等于接受结果"Alert、"复核界面不显示具有权威错觉的'AI 已批准'"Alert；可访问性落实：Timeline 事件列表 + 收敛历史条形图 Tooltip + 关键指标表格摘要 + 参数表单 label/unit ✅ 对齐 §可访问性"图表有数据表/摘要"；V0 限制清晰标注：AuthoringMap 待 V1（依赖 BIM Viewer）、3D 结果可视化待 V1、主动作实际执行待 V1（依赖后端 AnalysisProblem/Scenario/Run/Result/Quality API）；use-analysis.ts 仍使用 Mock 数据（MOCK_PROBLEMS/MOCK_SCENARIOS 等 8 个 Mock 常量），但 BFF 当前无 analysis 代理，Mock 数据清理属于"Analysis 域 BFF 代理 + Core Service 实现"独立任务。③验证结果：`pnpm --filter @design-platform/web typecheck` 通过（exit code 0，tsc --noEmit 无错误）；`pnpm --filter @design-platform/web lint` 通过（exit code 0，仅 dashboard/governance 等无关页面 7 个 react-hooks/exhaustive-deps Warning，P08/P10 相关文件无错误无警告）。V0 差距记录：①Analysis 域 BFF 代理（AnalysisProxyController）+ Core Service（AnalysisProblem/Scenario/Run/Result/Quality Controller + Service + Repository）待 V1 实现后，移除 use-analysis.ts Mock 数据切换真实 API；②ClauseViewer/CitationCard/EvidenceGraph 待 V1（依赖 BIM Viewer 与规范条文库）；③AuthoringMap/3D 结果可视化待 V1（依赖 BIM Viewer）；④/analysis/problems/{id} 之外的设计基线路由 `/simulation-runs/{runId}`、`/result-packages/{id}` 当前作为 Tab 嵌入 ProblemDetailPage，独立路由待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.12/§D37.14、@design/D36-界面信息架构.md、@design/D33-工程分析-求解.md、@design/D35-API-事件契约.md、frontend.md（Next.js 15/React 19/Ant Design 5/WCAG 2.2 AA）、security.md（AI 安全红线、requiresHumanReview/isAiAssisted 标识）、design-constraints.md（AI 结果人工复核红线）。

- 2026-07-29：A-27 Operations 域集成测试第四阶段完成（Controller API IT 5 个测试类共 58 个用例 + 编译通过）。对照 A-19 Change 域 ChangeRequestControllerApiIT 模式，按"先 Schema 验证 IT → Repository CRUD IT → Service 状态机 IT → Controller API IT"分阶段推进 Operations 域集成测试。第三阶段完成 OperationsActionServiceIT 后，本阶段进入 Controller API 层端到端验证，覆盖 Operations 域 5 个 Controller（Action/QueueTask/Slo/Worker/Connector），所有端点通过 TestRestTemplate 启动 PostgreSQL 16 + Flyway V19 已建表环境，使用真实 access token + x-tenant-id + x-user-id 头部，验证从 HTTP 请求到数据库持久化的完整链路。①`OperationsActionControllerApiIT`（13 个用例，POST /api/v1/operations/action 端点）：覆盖 LOW 风险（RESUME Worker）/MEDIUM 风险（RETRY QueueTask）/HIGH 风险（ISOLATE Worker）/IRREVERSIBLE（CANCEL QueueTask）成功路径 + 审计字段持久化（operationId/auditTraceId/initiatedBy 从 x-user-id 头解析/stepUpTokenHash 以 v0hash: 前缀）/分发 PAUSE 到 QueueTask + FAILOVER 到 Worker；覆盖 MEDIUM 风险未确认影响预览（400 + 业务码非 0 + 错误信息含 impactPreviewAcknowledged）/HIGH 风险缺失 stepUpToken（400 + 错误信息含 stepUpToken）/retry storm 检测（409 CONFLICT + 错误信息含 retry storm）/无效 targetId（400 + 错误信息含 targetId）/跨租户访问（404 不暴露资源存在性）/缺少 x-user-id 头（401）/缺少 access token（401）。②`QueueTaskControllerApiIT`（13 个用例，GET/POST /api/v1/operations/queue 端点）：覆盖 GET 空列表/有数据列表/status=QUEUED + type=AI_GENERATION 过滤；GET 详情查询（存在/不存在 404/跨租户 404）；POST 创建任务（默认 retryCount=0/maxRetries=3）；POST pause（RUNNING → PAUSED）/resume（PAUSED → RUNNING）/retry（FAILED → QUEUED + retryCount +1）/cancel（RUNNING → CANCELLED + completedAt）；POST 缺少 x-user-id 头（401）。③`SloTargetControllerApiIT`（10 个用例，GET/POST/PUT /api/v1/operations/slos 端点）：覆盖 GET 空列表/有数据列表/status=CRITICAL 过滤；GET 详情查询（存在/不存在 404/跨租户 404）+ BigDecimal 精度保留验证（availabilityTarget=0.9990 NUMERIC(5,4)/errorBudgetRemaining=100.0000 NUMERIC(8,4)）；POST 创建 SLO 目标（含 BigDecimal 精度保留 + windowDays=28）；PUT 更新 SLO 状态（HEALTHY → CRITICAL：errorBudgetRemaining=-15.5000 突破错误预算 + availabilityCurrent=0.9850 低于 99.9% 目标 + errorCount24h=150 高错误率）；POST 缺少 x-user-id 头（401）。④`WorkerStatusControllerApiIT`（12 个用例，GET/POST /api/v1/operations/workers 端点）：覆盖 GET 空列表/有数据列表/type=AI + status=RUNNING 过滤；GET region=cn-beijing-1 过滤（Hybrid-Site region + isCustomerSiteWorker=true 客户站点 Worker，对齐 D44 部署拓扑红线）；GET 详情查询（存在/不存在 404/跨租户 404）+ BigDecimal 精度保留验证（cpuPercent=45.50）；POST pause（RUNNING → STOPPED）/resume（STOPPED → IDLE）；POST 缺少 x-user-id 头（401）。⑤`ConnectorStatusControllerApiIT`（10 个用例，GET /api/v1/operations/connectors 端点）：覆盖 GET 空列表/有数据列表/type=LLM + status=DEGRADED 过滤；GET 详情查询（存在/不存在 404/跨租户 404）；GET **OD-05 ManualHandoff 红线验证**（建筑 AI Provider 强制 isManualHandoff=true + licenseRemaining="ManualHandoff: 未获正式 API/许可" + endpointUrl=null + status=UNKNOWN）；GET 多类型连接器混合查询（LLM + MINIO + REVIT 同时返回 3 条）。所有测试通过 `withAccessToken(tenantId, accessToken)` + `x-user-id` 头部构造真实认证链路，使用 `extractData`/`extractCode` 工具方法解析 ApiResponse 包装的响应。验证结果：`mvn test-compile` BUILD SUCCESS，5 个测试类全部编译通过（class 文件总计 101943 字节：OperationsActionControllerApiIT 31074 + QueueTaskControllerApiIT 19867 + SloTargetControllerApiIT 17041 + WorkerStatusControllerApiIT 17317 + ConnectorStatusControllerApiIT 16644）；运行需本地 PostgreSQL 16 + Docker 环境，运行验证留待 V1 部署阶段或 BFF 端到端联调时执行。V0 差距记录：①OperationsOverviewController 的 GET /api/v1/operations/overview 端点未单独编写 IT（聚合视图，依赖 5 个子域 Controller 数据，待 V1 实现）；②与 BFF OperationsProxyController 端到端联调待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.17、@design/D42-SLO-容量.md、@design/D44-部署拓扑-Hybrid-Site.md、@design/D40-信息-物理安全.md（Step-up 认证）、@design/D35-API-事件契约.md（危险动作审计）、database.md（审计字段、命名约定）、security.md（PII 分级、字段加密、JWT 认证）、design-constraints.md（AI 安全红线、危险动作约束、OD-05 ManualHandoff）。

- 2026-07-29：A-26 Operations 域集成测试第三阶段完成（OperationsActionServiceIT 16 个用例 + 编译通过）。对照 A-19 Change 域 ChangeRequestServiceIT 模式，按"先 Schema 验证 IT → Repository CRUD IT → Service 状态机 IT → Controller API IT"分阶段推进 Operations 域集成测试。第二阶段完成 5 个 Repository IT 后，本阶段进入 Service 层危险动作约束验证。新增 `services/core/src/test/java/com/platform/core/operations/integration/OperationsActionServiceIT.java`：通过 `AbstractIntegrationTest` 启动 PostgreSQL 16 + Flyway V19 已建表环境，注入 `OperationsActionService` + 4 个 Repository（OperationsAction/WorkerStatus/ConnectorStatus/QueueTask），覆盖 16 个测试用例：①`shouldExecuteLowRiskActionWithoutStepUpToken`（**LOW 风险（RESUME Worker）**：stepUpToken=null + impactPreviewAcknowledged=false 应通过，Worker STOPPED→IDLE，对齐 D37.17 §危险动作 LOW 风险无额外校验红线）；②`shouldExecuteMediumRiskActionWithImpactPreviewOnly`（**MEDIUM 风险（RETRY QueueTask）**：stepUpToken=null + impactPreviewAcknowledged=true 应通过，FAILED→QUEUED + retryCount+1，对齐 MEDIUM 仅需影响预览确认约束）；③`shouldRejectMediumRiskActionWithoutImpactPreview`（MEDIUM 风险 impactPreviewAcknowledged=false 抛 BusinessException(4220/BAD_REQUEST)，错误信息含 MEDIUM + impactPreviewAcknowledged）；④`shouldExecuteHighRiskActionWithStepUpToken`（**HIGH 风险（ISOLATE Worker）**：stepUpToken + impactPreviewAcknowledged=true 通过，Worker isIsolated=true + isolatedReason + isolatedAt + status=STOPPED + currentTaskId=null）；⑤`shouldRejectHighRiskActionWithoutStepUpToken`（HIGH 风险 stepUpToken=null 抛 BusinessException(4220/BAD_REQUEST)，错误信息含 HIGH + stepUpToken，Worker 未被隔离）；⑥`shouldRejectHighRiskActionWithoutImpactPreview`（HIGH 风险 impactPreviewAcknowledged=false 抛 BusinessException）；⑦`shouldExecuteIrreversibleActionWithDualReviewV0Placeholder`（**IRREVERSIBLE（CANCEL QueueTask）**：stepUpToken + impactPreviewAcknowledged=true 通过，V0 占位双人审批，审计记录含 riskLevel=IRREVERSIBLE + stepUpTokenHash + impactPreviewAcknowledged + initiatedBy 从 x-user-id 头解析）；⑧`shouldRejectIrreversibleActionWithoutStepUpToken`（IRREVERSIBLE stepUpToken=null 抛 BusinessException(4220/BAD_REQUEST)，错误信息含 IRREVERSIBLE）；⑨`shouldDetectRetryStormAndRejectRetryAction`（**retry storm 检测**：预置 5 条同 target_id 的 FAILED OperationsAction，RETRY 动作抛 BusinessException(4220/CONFLICT)，错误信息含 retry storm + targetId + 当前 FAILED 数 5，QueueTask 状态保持 FAILED 未变）；⑩`shouldAllowRetryActionAtRetryStormBoundary`（**retry storm 阈值边界**：预置 4 条 FAILED（4 < 5 阈值），RETRY 动作应 COMPLETED）；⑪`shouldDispatchFailoverActionToWorker`（**分发 FAILOVER 到 Worker**：HIGH 风险 + stepUpToken + 影响预览确认，Worker status=ERROR + isolatedReason 含 FAILOVER + isolatedAt）；⑫`shouldRejectUnknownActionForWorker`（**分发不支持的动作类型**：CANCEL 对 Worker 目标抛 BusinessException(4220)，审计记录状态=FAILED + error_message 持久化）；⑬`shouldRejectInvalidTargetId`（**targetId 非 UUID 格式**：抛 BusinessException(PARAM_INVALID/BAD_REQUEST)，错误信息含 targetId）；⑭`shouldPersistAuditFields`（**审计字段持久化**：operationId 以 OPS-ACT- 前缀、auditTraceId 已生成、initiatedBy 从 x-user-id 头解析、initiatedAt/completedAt 已记录、affectedCount=1、stepUpTokenHash 以 v0hash: 前缀（不存储明文））；⑮`shouldUseSystemWhenHttpRequestIsNull`（httpRequest 为 null 时 initiatedBy="system"，对齐系统自动触发场景）；⑯`shouldEnforceTenantIsolation`（**跨租户隔离**：租户 B 试图 ISOLATE 租户 A 的 Worker 抛 BusinessException(NOT_FOUND)，不暴露资源存在性，Worker 未被隔离）。工具方法：buildSampleWorker（含 cpuPercent/memoryPercent BigDecimal、lastHeartbeat、region、isCustomerSiteWorker=false、isolated=false 默认值）、buildSampleQueueTask（含 payload/queuedAt 必填字段）、buildFailedActionForRetryStorm（预置 FAILED OperationsAction 用于 retry storm 检测）、buildHttpRequest（MockHttpServletRequest 含 x-user-id 头）。验证结果：`mvn test-compile` BUILD SUCCESS，OperationsActionServiceIT.class 已生成（36838 字节）；运行需本地 PostgreSQL 16 + Docker 环境，运行验证留待 V1 部署阶段或第四阶段 Controller API IT 启动时执行。V0 差距记录：①OperationsActionControllerApiIT 等 7 个 Controller API 集成测试待第四阶段实现；②与 BFF OperationsProxyController 端到端联调待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.17（§危险动作）、@design/D40-信息-物理安全.md（Step-up 认证）、@design/D35-API-事件契约.md（危险动作审计）、@design/D42-SLO-容量.md、@design/D44-部署拓扑-Hybrid-Site.md、database.md（审计字段、命名约定）、security.md（PII 分级：step_up_token_hash L1 不存储明文 / reason L2 / initiatedBy L2）、design-constraints.md（AI 安全红线、危险动作约束）。

- 2026-07-29：A-25 Operations 域集成测试第二阶段完成（Repository IT 4 个测试类共 50 个用例 + 编译通过）。在 A-24 QueueTaskRepositoryIT 基础上补齐 SloTargetRepositoryIT/WorkerStatusRepositoryIT/ConnectorStatusRepositoryIT/OperationsActionRepositoryIT，第二阶段 5 个 Repository IT 全部完成，对照 A-19 Change 域 Repository IT 模式。①`SloTargetRepositoryIT`（11 个用例）：覆盖 save/findByIdAndTenantId/租户隔离/分页查询/status 过滤/BigDecimal 精度保留（availability_target NUMERIC(5,4)/error_budget_remaining NUMERIC(8,4)）/window_days 配置/CRITICAL 场景持久化（error_budget_remaining=-15.5000 突破错误预算 + availability_current=0.9850 低于 99.9% 目标 + error_count_24h=150 高错误率）/非空约束/外键约束。②`WorkerStatusRepositoryIT`（13 个用例）：覆盖 save/findByIdAndTenantId/租户隔离/分页查询/type/status/region 多维过滤/Hybrid-Site 字段持久化（region=cn-beijing-1 + is_customer_site_worker=true 客户站点 Worker）/隔离 Worker 持久化（is_isolated=true + isolated_reason + isolated_at）/心跳 last_heartbeat 字段/non-null 约束/外键约束。③`ConnectorStatusRepositoryIT`（12 个用例）：覆盖 save/findByIdAndTenantId/租户隔离/分页查询/type/status 过滤/**OD-05 ManualHandoff 持久化**（建筑 AI Provider 强制 is_manual_handoff=true + license_remaining="ManualHandoff: 未获正式 API/许可" + endpoint_url=null + status=UNKNOWN）/Hybrid-Site region 字段/调用指标 call_count_1h/error_count_1h/avg_latency_ms/非空约束/外键约束。④`OperationsActionRepositoryIT`（14 个用例）：覆盖 save/findByIdAndTenantId/租户隔离/分页查询/status/actionType 过滤/operationId 业务编号查询/**retry storm 检测占位**（countByTenantIdAndTargetIdAndStatus 统计同 target_id 的 FAILED 动作数=4 触发阈值）/状态机流转（QUEUED→RUNNING→COMPLETED）/FAILED 状态 + error_message 持久化/**IRREVERSIBLE 动作（CANCEL）双人审批**（stepUpTokenHash + impactPreviewAcknowledged=true + reviewer1/reviewer2 双人审批 + affected_count=15 + reason="取消高风险 AI 生成任务：检测到 prompt 注入风险，需要双人复核"）/HIGH 风险动作（ISOLATE）stepUpToken + 影响预览确认/MEDIUM 风险动作（RETRY）仅需影响预览确认/non-null 约束/外键约束/**operation_id 唯一约束**（uq_operations_action_operation_id 重复抛 DataIntegrityViolationException）。验证结果：`mvn test-compile` BUILD SUCCESS，OperationsActionRepositoryIT.class 已生成（30242 字节），5 个 Repository IT 全部编译通过；运行需本地 PostgreSQL 16 + Docker 环境，运行验证留待 V1 部署阶段或第三阶段 Service 状态机 IT 启动时执行。V0 差距记录：①OperationsActionServiceIT（危险动作状态机 + stepUpToken 校验 + retry storm 检测 + IRREVERSIBLE 双人审批）待第三阶段实现；②OperationsActionControllerApiIT 等 7 个 Controller API 集成测试待第四阶段实现；③与 BFF OperationsProxyController 端到端联调待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.17、@design/D42-SLO-容量.md、@design/D44-部署拓扑-Hybrid-Site.md、@design/D40-信息-物理安全.md（Step-up 认证）、@design/D35-API-事件契约.md（危险动作审计）、database.md（审计字段、命名约定）、security.md（PII 分级、字段加密）、design-constraints.md（AI 安全红线、危险动作约束）。

- 2026-07-29：A-24 Operations 域集成测试第二阶段（Repository IT）启动，QueueTaskRepositoryIT 完成（14 个测试用例 + 编译通过）。对照 A-19 Change 域集成测试模式，按"先 Schema 验证 IT → Repository CRUD IT → Service 状态机 IT → Controller API IT"分阶段推进 Operations 域集成测试。第一阶段（A-23）完成 OperationsSchemaIT 11 个用例验证 V19 迁移脚本 5 张表 DDL 落库正确后，本阶段进入 Repository CRUD 层验证。新增 `services/core/src/test/java/com/platform/core/operations/integration/QueueTaskRepositoryIT.java`：通过 `AbstractIntegrationTest` 启动 PostgreSQL 16 + Flyway V19 已建表环境，注入 `QueueTaskRepository` 直接操作数据库，不经过 Controller，覆盖 14 个测试用例：①`shouldSaveQueueTaskAndReturnGeneratedId`（save 持久化 + UUID 生成 + 默认值校验 retry_count=0/max_retries=3 默认值、row_version/created_at/updated_at 不为 null、queued_at 必填）；②`shouldFindByIdAndTenantId`（按 ID + tenantId 查询）；③`shouldEnforceTenantIsolation`（跨租户查询返回 empty，验证租户隔离红线）；④`shouldFindByTenantIdWithPagination`（按 tenantId 分页查询 5 条）；⑤`shouldFindByTenantIdAndStatusWithPagination`（按 tenantId + status 分页查询，3 QUEUED + 2 RUNNING + 1 FAILED 隔离）；⑥`shouldFindByTenantIdAndTypeWithPagination`（按 tenantId + type 分页查询，AI_GENERATION 2 条 vs COMPLIANCE_CHECK 1 条）；⑦`shouldFindByTenantIdAndPriorityWithPagination`（按 tenantId + priority 分页查询，CRITICAL 1 条 vs HIGH 1 条）；⑧`shouldFindByTenantIdAndWorkerIdWithPagination`（按 tenantId + workerId 分页查询，Worker 已绑定 1 条 vs 未绑定 1 条）；⑨`shouldCountByTenantIdAndStatus`（按 status 统计：QUEUED 2 / RUNNING 1 / FAILED 0）；⑩`shouldCountByTenantIdAndStatusIn`（按 statusIn 统计活跃任务 QUEUED + RUNNING = 2）；⑪`shouldPersistRetryStormScenario`（**retry storm 场景持久化**：retry_count=10 > max_retries*2=6 阈值，last_error 记录 "调用 OpenAI API 失败：429 Too Many Requests"，状态 FAILED，对齐 D37.17 §retry storm 检测红线）；⑫`shouldPersistHybridSiteDataRegionField`（**Hybrid-Site 字段持久化**：data_region=cn-beijing-1 跨境数据传输约束、project_id/stage_id 关联，对齐 OD-06 Hybrid-Site 部署 + D44 部署拓扑红线）；⑬`shouldPersistTaskStatusTransition`（状态机流转 QUEUED → RUNNING → COMPLETED 持久化，含 started_at/completed_at/duration_sec 字段更新）；⑭`shouldRejectMissingRequiredField`（payload 为 null 抛 DataIntegrityViolationException，验证 NOT NULL 约束）。修复 buildSampleQueueTask 辅助方法：必填字段 payload/queuedAt 显式赋值，避免 NOT NULL 约束违反。验证结果：`mvn test-compile` BUILD SUCCESS（0.400s），QueueTaskRepositoryIT.class 已生成到 target/test-classes/com/platform/core/operations/integration/（23924 字节，时间戳 11:35），OperationsSchemaIT.class 也已生成（34671 字节）；运行需本地 PostgreSQL 16 + Docker 环境，运行验证留待 V1 部署阶段或下一阶段 Service 状态机 IT 启动时执行。V0 差距记录：①SloTargetRepositoryIT/WorkerStatusRepositoryIT/ConnectorStatusRepositoryIT/OperationsActionRepositoryIT（4 个 Repository IT）待第二阶段剩余部分实现；②OperationsActionServiceIT（危险动作状态机 + stepUpToken 校验 + retry storm 检测 + IRREVERSIBLE 双人审批）待第三阶段实现；③OperationsActionControllerApiIT 等 7 个 Controller API 集成测试待第四阶段实现；④与 BFF OperationsProxyController 端到端联调待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.17、@design/D42-SLO-容量.md、@design/D44-部署拓扑-Hybrid-Site.md、database.md（审计字段、命名约定）、security.md（PII 分级、字段加密）、design-constraints.md（AI 安全红线、危险动作约束）。

- 2026-07-29：A-23 Operations 域集成测试第一阶段完成（OperationsSchemaIT 11 个用例 + AbstractIntegrationTest 基础设施更新）。对照 A-19 Change 域集成测试模式，按"先 Schema 验证 IT → Repository CRUD IT → Service 状态机 IT → Controller API IT"分阶段推进 Operations 域集成测试。①更新 `services/core/src/test/java/com/platform/core/testsupport/AbstractIntegrationTest.java`：`resetDatabase()` 方法的 DROP SCHEMA 列表新增 `operations` schema（V19 迁移前置清理）；Flyway `.schemas(...)` 列表新增 `operations`（V19 迁移 clean 范围扩展）；`truncateBusinessTables()` 新增 5 张运营域表（operations_action/connector_status/worker_status/queue_task/slo_target）的 TRUNCATE 语句，确保测试间数据隔离。②新增 `services/core/src/test/java/com/platform/core/operations/integration/OperationsSchemaIT.java`：验证 V19 迁移脚本运营域 5 张表 DDL 落库正确，覆盖 11 个测试用例——`shouldCreateAllOperationsTables`（验证 operations schema 下 5 张表存在）、`shouldHaveSloTargetColumns`（20 个字段断言，含 SLO 指标字段 availability_target/availability_current/error_budget_remaining、延迟指标 p95/p99_latency_ms、健康状态 status、窗口 window_days）、`shouldHaveQueueTaskColumns`（21 个字段断言，含 retry storm 检测 retry_count/max_retries、数据驻留 data_region、负载 payload PII: L3）、`shouldHaveWorkerStatusColumns`（22 个字段断言，含 Hybrid-Site region/is_customer_site_worker、隔离状态 is_isolated/isolated_reason/isolated_at、心跳 last_heartbeat、PII: L3 current_task_payload）、`shouldHaveConnectorStatusColumns`（19 个字段断言，含 OD-05 ManualHandoff is_manual_handoff、许可证 license_remaining、健康检查 last_health_check_at）、`shouldHaveOperationsActionColumns`（24 个字段断言，含危险动作 action_type/target_type/target_id、风险等级 risk_level、step_up_token_hash PII: L1、双人审批 reviewer1/reviewer2、审计追踪 audit_trace_id）、`shouldHaveUniqueConstraintOnWorkerCode`（通过尝试插入重复 worker_code 验证唯一约束 uq_worker_status_code 触发异常）、`shouldHaveUniqueConstraintOnConnectorCode`（通过尝试插入重复 connector_code 验证唯一约束 uq_connector_status_code 触发异常）、`shouldHaveUniqueConstraintOnOperationId`（通过尝试插入重复 operation_id 验证唯一约束 uq_operations_action_operation_id 触发异常）、`shouldHaveQueueTaskIndexes`（4 个索引断言，含 tenant_id+status/type/priority 组合索引）、`shouldHaveWorkerStatusIndexes`（3 个索引断言，含 last_heartbeat 存活检测索引）、`shouldHaveOperationsActionIndexes`（4 个索引断言，含 initiated_by 审计追踪索引）、`shouldInsertQueueTaskWithRetryStormFields`（验证 retry_count=10、max_retries=3 模拟 retry storm 场景可持久化）、`shouldInsertWorkerStatusWithHybridSiteFields`（验证 region=cn-beijing-1、is_customer_site_worker=true 的 Hybrid-Site Worker 可持久化）。③字段名严格对齐 V19 SQL 实际定义：queue_task 使用 `worker_id`/`data_region`/`last_error`、worker_status 使用 `worker_code`/`is_isolated`/`isolated_reason`、connector_status 使用 `connector_code`/`is_manual_handoff`/`license_remaining`、operations_action 使用 `operation_id`/`audit_trace_id`/`impact_preview_acknowledged`。④编译验证：`mvn test-compile` BUILD SUCCESS，OperationsSchemaIT 全部 11 个测试用例编译成功（运行需本地 PostgreSQL 16 + Docker 环境，运行验证留待 V1 部署阶段或第二阶段 Repository IT 启动时执行）。V0 差距记录：①QueueTaskRepositoryIT（Repository CRUD + 租户隔离 + retry storm 检测）待第二阶段实现；②OperationsActionServiceIT（危险动作状态机 + stepUpToken 校验 + retry storm 检测 + IRREVERSIBLE 双人审批）待第三阶段实现；③OperationsActionControllerApiIT（7 个 Controller API 集成测试）待第四阶段实现；④与 BFF OperationsProxyController 端到端联调待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.17、@design/D42-SLO-容量.md、@design/D44-部署拓扑-Hybrid-Site.md、database.md（审计字段、命名约定）、security.md（PII 分级、字段加密）、design-constraints.md（AI 安全红线、危险动作约束）。

- 2026-07-29：A-22 Operations 域 Java 后端实现完成（D37.17 运营中心 V0 基线，5 子域 + Overview 聚合端点 + 危险动作统一入口）。新增 12 个枚举（SloStatus/QueueTaskType/QueueTaskStatus/QueueTaskPriority/WorkerType/WorkerRuntimeStatus/ConnectorType/ConnectorHealthStatus/OperationsActionType/OperationsActionStatus/OperationsActionTargetType/OperationsRiskLevel）+ V19 Flyway 迁移脚本（operations schema 5 张表：slo_target/queue_task/worker_status/connector_status/operations_action，含 PII 分级注释与 retry storm 检测字段）。5 个子域完整 DDD 五层结构：①SLO（SloTarget/SloTargetRepository/SloService/SloController/SloTargetDto）；②QueueTask（QueueTask/QueueTaskRepository/QueueTaskService/QueueTaskController/QueueTaskDto/ListQueueTasksRequest，含 retry storm 检测 retry_count > max_retries * 2 阈值 + unknown job 显式标识 + 跨 Region data_region 数据驻留约束）；③Worker（WorkerStatus/WorkerStatusRepository/WorkerService/WorkerController/WorkerStatusDto/ListWorkersRequest，含 isolate/failover 危险动作 + Hybrid-Site region/isCustomerSiteWorker 字段 + 心跳存活检测）；④Connector（ConnectorStatus/ConnectorStatusRepository/ConnectorService/ConnectorController/ConnectorStatusDto/ListConnectorsRequest，含 isolate/reconcile/failover 动作 + OD-05 ManualHandoff 约束 + 许可证剩余描述）；⑤OperationsAction（OperationsAction/OperationsActionRepository/OperationsActionService/OperationsActionController/OperationsActionRequest/OperationsActionResponseDto，统一危险动作入口 POST /api/v1/operations/action，按 actionType+targetType+targetId 三元组分发到 Worker/Connector/QueueTask 服务，实现风险等级校验 LOW/MEDIUM/HIGH/IRREVERSIBLE + stepUpToken 校验 + impactPreviewAcknowledged 校验 + IRREVERSIBLE 双人审批 V0 占位 + retry storm 检测 + 审计追踪 auditTraceId + 操作人 initiatedBy 解析）。新增 Overview 聚合端点（OperationsOverviewController/OperationsOverviewService/OperationsOverviewDto）：聚合 4 子域统计返回 17 字段概览（running/queued/failed/paused tasks + running/error/stopped workers + connected/degraded/disconnected connectors + critical/warning SLOs + hasRetryStorm/hasUnknownJobs 特殊状态 + dataResidencyRegions），整体状态判定（errorWorkers>0 或 failedTasks>0 或 criticalSlos>0 或 disconnectedConnectors>0 → degraded）。同步修正前端契约 `packages/shared/src/contracts/monitoring.contract.ts` 中 `MonitoringApiPaths.action` 由 `/api/v1/operations:action`（冒号语法）改为 `/api/v1/operations/action`（斜杠分隔，与后端 `@PostMapping` 路径一致）；更新 BFF `apps/bff/src/proxy/operations/operations-proxy.controller.ts` 注释列出 9 个后端端点路径；更新 BFF 单元测试 `apps/bff/tests/unit/proxy/operations-proxy.controller.test.ts` 中 POST action 端点路径对齐。验证结果：①`mvn clean compile` BUILD SUCCESS（380 文件，含全部 12 枚举 + 5 子域 + Overview + OperationsAction 共 27 个 Java 文件）；②`pnpm --filter @design-platform/shared build` 成功；③`pnpm -r typecheck` 0 错误（shared/bff/web 全通过）；④`pnpm --filter @design-platform/bff lint` 0 错误（14 个历史遗留 warnings 与本次变更无关）；⑤`pnpm exec vitest run operations-proxy.controller.test.ts` 8/8 通过。安全红线落实（D37.17 §Operations 危险动作）：ISOLATE/FAILOVER 标记 HIGH 风险（需 stepUpToken + impactPreviewAcknowledged）、CANCEL 标记 IRREVERSIBLE（需双人审批，V0 占位通过）、RETRY/RECONCILE/PAUSE 标记 MEDIUM（需 impactPreviewAcknowledged）、RESUME 标记 LOW（无额外校验）。PII 分级落实：queue_task.payload L3、worker_status.current_task_payload L3、operations_action.reason L2、operations_action.step_up_token_hash L1（不存储明文，V0 用 hashCode 占位，V1 接入 KMS SHA-256+盐）。Hybrid-Site 部署字段落实：worker_status.region/is_customer_site_worker、queue_task.data_region、connector_status.region。权威源：@design/D37-关键界面-交互状态.md §D37.17、@design/D42-SLO-容量.md、@design/D29-可观测性-合规性-指标.md、@design/D44-部署拓扑-Hybrid-Site.md、@design/D40-信息-物理安全.md（Step-up 认证）、@design/D35-API-事件契约.md（危险动作审计）、security.md（PII 分级）、design-constraints.md（AI 安全红线）。

- 2026-07-29：A-21 BFF Change 域代理层端到端联调完成（ChangeApiPaths 路径对齐 + 15 个单元测试全部通过）。发现 BFF `ChangeProxyController` 与 Core `ChangeRequestController` 路径不一致问题：前端 `ChangeApiPaths` 中状态流转端点使用冒号语法（如 `:approve`/`:submit-impact-assessment`），但后端 `@PostMapping` 使用斜杠分隔（如 `/approve`/`/submit-impact`），导致前端调用 404。本次修正：①更新 `packages/shared/src/contracts/change.contract.ts` 中 `ChangeApiPaths`：6 处路径对齐后端实际路由（submitImpactAssessment 由 `${id}:submit-impact-assessment` 改为 `${id}/submit-impact`；approve/reject/recall/verifyClosure 由冒号语法改为斜杠；generateTaskPlan 由 `${id}/task-plans:generate` 改为 `${id}/task-plans/:generate`）；补充 15 个子实体 CRUD 端点（affectedItemDetail/createAffectedItem/updateAffectedItem/deleteAffectedItem/recheckAffectedItem；taskPlanDetail/createTaskPlan/updateTaskPlan/deleteTaskPlan/startTaskPlan；closureEvidenceDetail/createClosureEvidence/deleteClosureEvidence/verifyClosureEvidence），覆盖 5 个后端 Controller（ChangeRequestController/AffectedItemController/TaskPlanItemController/ClosureEvidenceController/ChangeOperationController）全部 API。②更新 BFF `apps/bff/src/proxy/change/change-proxy.controller.ts` 类注释：列出 5 个后端 Controller 全部端点路径，明确状态流转端点用斜杠分隔、子实体动作端点（recheck/verify/start/generate）保留冒号语法（与后端 Spring `@PostMapping("/:generate")` 等一致）。③补充 BFF 单元测试 `apps/bff/tests/unit/proxy/change-proxy.controller.test.ts` 至 15 个用例（原 8 个 + 新增 7 个）：新增 `submit-impact`/`reject`/`recall`/`verify-closure` 4 个状态流转端点路径对齐验证，新增 `affected-items/:itemId:recheck`/`closure-evidences/:evidenceId:verify`/`task-plans/:itemId:start` 3 个子实体动作端点验证；修正原 `approve` 和 `task-plans:generate` 测试用例路径与后端一致。验证结果：`pnpm --filter @design-platform/shared build` 成功；`pnpm -r typecheck` 0 错误（shared/bff/web 全通过）；`pnpm --filter @design-platform/bff lint` 0 错误（14 个历史遗留 warnings 与本次变更无关）；`pnpm vitest run change-proxy.controller.test.ts` 15/15 通过（5ms）。前端 `use-changes.ts` 通过 `ChangeApiPaths.*` 间接引用路径，无需修改组件代码即可使用修正后路径。数据通路：前端（use-changes.ts）→ BFF（ChangeProxyController @All("*") 纯透传）→ Core Service（ChangeRequestController + 4 个子实体 Controller）完整闭环，路径对齐 D37.16 §D37.16 P12 变更影响与闭环工作台设计基线。权威源：@design/D37-关键界面-交互状态.md §D37.16、@design/D35-API-事件契约.md、api-conventions.md（统一响应格式、traceId 传播）。

- 2026-07-29：A-19 Change 域集成测试第二阶段完成（ChangeRequestRepositoryIT 17 个用例全部通过）。对照 A-15 治理域 AccessGrantRepositoryIT 模式，按 "Repository CRUD + 租户隔离 + 状态过滤" 推进。新增 `services/core/src/test/java/com/platform/core/change/integration/ChangeRequestRepositoryIT.java`：通过 `AbstractIntegrationTest` 启动 PostgreSQL 16 + Flyway V18 已建表环境，注入 `ChangeRequestRepository` 直接操作数据库，不经过 Controller，覆盖 17 个测试用例：①`shouldSaveChangeRequestAndReturnGeneratedId`（save 持久化 + UUID 生成 + 默认值校验 impact_assessment/ai_assisted_analysis 默认 `{}`、confirmed_no_impact/is_ai_assisted 默认 false、row_version/created_at/updated_at 不为 null）；②`shouldFindByIdAndTenantId`（按 ID + tenantId 查询）；③`shouldEnforceTenantIsolation`（跨租户查询返回 empty，验证租户隔离红线）；④`shouldFindByCodeAndTenantId`（按业务编号 code 查询，验证唯一约束）；⑤`shouldFindByTenantIdWithPagination`（按 tenantId 分页查询 5 条）；⑥`shouldFindByTenantIdAndStatusWithPagination`（按 tenantId + status 分页查询，3 DRAFT + 2 SUBMITTED 隔离）；⑦`shouldFindByTenantIdAndProjectId`（按 tenantId + projectId 分页查询，PROJ-A 2 条 vs PROJ-B 1 条）；⑧`shouldCountByTenantIdAndStatus`（按 status 统计：DRAFT 2 / SUBMITTED 1 / CLOSED 0）；⑨`shouldCountByTenantIdAndType`（按 type 统计：DESIGN_CHANGE 1 / REQUIREMENT_CHANGE 1）；⑩`shouldCountByTenantIdAndPriority`（按 priority 统计：NORMAL 1 / CRITICAL 1）；⑪`shouldUpdateStatus`（状态机流转 DRAFT→SUBMITTED 持久化）；⑫`shouldUpdateApprovalFields`（批准阶段 approved_by/approved_at/status 字段更新）；⑬`shouldPersistCriticalPriorityChange`（高风险变更持久化：CRITICAL 优先级 + isAiAssisted=true + ai_assisted_analysis JSON 内容 + risk_assessment 字段，对齐 D37.16 §AI 安全红线）；⑭`shouldRejectMissingRequiredField`（title 为 null 抛 DataIntegrityViolationException，验证 NOT NULL 约束）；⑮`shouldRejectNonExistentTenant`（引用不存在 tenant_id 抛外键约束异常，验证 tenant_id 外键）；⑯`shouldRejectDuplicateCode`（相同 code 抛唯一约束异常，验证 uq_change_request_code）；⑰`shouldCascadeDeleteRelatedEntities`（物理删除 + ON DELETE CASCADE 配置正确）。修复 buildSampleChangeRequest 辅助方法重载链 bug：原 `buildSampleChangeRequest(tenantId, code, status, type, priority)` 调用 `buildSampleChangeRequest(tenantId, code, status, "PROJ-DEFAULT")` 丢失 type/priority 参数 → 重构为全参数版本 `buildSampleChangeRequest(tenantId, code, status, projectId, type, priority)`，其他重载委托至此版本，确保 type/priority 正确传递。验证结果：`mvn -Dtest=ChangeRequestRepositoryIT test` BUILD SUCCESS，17 个测试用例全部通过（0 failures, 0 errors, 0 skipped），耗时 8.352s；日志中的 ERROR 行（null value / duplicate key / foreign key violation）为负向用例预期异常，由 `assertThrows(DataIntegrityViolationException.class, ...)` 捕获并通过断言。V0 差距记录：①ChangeRequestServiceIT（状态机流转 + stepUpToken 校验 + 职责分离 + UNKNOWN 阻断关闭 + 高风险双人复核）待第三阶段实现；②ChangeRequestControllerApiIT（7 个 Controller API 集成测试，参考 A-16 治理域模式）待第四阶段实现；③与 BFF ChangeProxyController 端到端联调待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.16、@design/D34-数据-数据库.md §D34.5、database.md（审计字段、命名约定）、security.md（PII 分级、字段加密）。

- 2026-07-29：A-19 Change 域集成测试第一阶段完成（ChangeSchemaIT + AbstractIntegrationTest 基础设施更新）。对照 A-15 治理域集成测试模式，按"先 Schema 验证 IT → Repository CRUD IT → Service 状态机 IT → Controller API IT"分阶段推进。①更新 `services/core/src/test/java/com/platform/core/testsupport/AbstractIntegrationTest.java`：`resetDatabase()` 方法的 DROP SCHEMA 列表新增 `change` schema（V18 迁移前置清理）；Flyway `.schemas(...)` 列表新增 `change`（V18 迁移 clean 范围扩展）；`truncateBusinessTables()` 新增 5 张变更域表（change_operation/closure_evidence/task_plan_item/affected_item/change_request）的 TRUNCATE 语句，确保测试间数据隔离。②新增 `services/core/src/test/java/com/platform/core/change/integration/ChangeSchemaIT.java`：验证 V18 迁移脚本变更域 5 张表 DDL 落库正确，覆盖 10 个测试用例——`shouldCreateAllChangeTables`（验证 change schema 下 5 张表存在）、`shouldHaveChangeRequestColumns`（30 个核心字段断言，含状态机字段 type/priority/status、风险字段 risk_assessment/is_ai_assisted、审计字段 created_at/updated_at/row_version/deleted_at）、`shouldHaveAffectedItemColumns`（22 个字段断言，含影响项标记 impact/recheck_required、复核状态 recheck_status、追溯字段 source_baseline_id/watermark/object_ref_id）、`shouldHaveTaskPlanItemColumns`（21 个字段断言，含阻断关闭标记 blocks_closure、跳过审批 skip_reason/skip_approved_by、优先级 priority/sequence_order）、`shouldHaveClosureEvidenceColumns`（22 个字段断言，含双人复核 reviewer1/reviewer2、验证追溯 verified_by/verified_at/verification_note、证据来源 source_id/source_description/evidence_url）、`shouldHaveChangeOperationColumns`（14 个字段断言，含阶段流转 from_status/to_status/sequence/operator_id/comment）、`shouldHaveUniqueConstraintOnCode`（通过尝试插入重复 code 验证唯一约束 uq_change_request_code 触发异常）、`shouldHaveAffectedItemIndexes/shouldHaveTaskPlanItemIndexes/shouldHaveClosureEvidenceIndexes`（3 个索引存在性断言，要求 tenant_id + change_id 组合索引）。③字段名严格对齐 V18 SQL 实际定义（修复早期版本字段名不一致问题：task_plan_item 使用 `skip_reason`/`skip_approved_by` 而非 `skipped_reason`/`skipped_by`；closure_evidence 使用 `verification_note`/`evidence_url` 而非 `review_notes`/`review_completed_at`；change_operation 使用 `operator_id`/`comment`/`from_status`/`to_status`/`sequence` 而非 `operator`/`notes`/`duration_ms`/`trace_id`/`is_ai_assisted`）。④编译验证：`mvn test-compile` 通过，ChangeSchemaIT 全部 10 个测试用例编译成功（运行需本地 PostgreSQL 16 + Docker 环境，当前 Docker 未启动故运行验证留待 V1 部署阶段）。V0 差距记录：①ChangeRequestRepositoryIT（Repository CRUD + 租户隔离 + 状态过滤）待第二阶段实现；②ChangeRequestServiceIT（状态机流转 + stepUpToken 校验 + 职责分离 + UNKNOWN 阻断关闭 + 高风险双人复核）待第三阶段实现；③ChangeRequestControllerApiIT（7 个 Controller API 集成测试，参考 A-16 治理域模式）待第四阶段实现；④与 BFF ChangeProxyController 端到端联调待 V1；⑤运行时验证待远程环境或本地 Docker 启动后执行。权威源：@design/D37-关键界面-交互状态.md §D37.16、@design/D34-数据-数据库.md §D34.5、database.md（审计字段、命名约定）、security.md（PII 分级、字段加密）。

- 2026-07-29：A-18 Change 域后端 Java 实现完成（V0 基线 + 全状态机校验）。对照 `@design/D37-关键界面-交互状态.md §D37.16 P12` 与 `@design/D11-专业协同与多专业校审.md`、`@design/D46-追踪完整性-一致性总审.md`，按 DDD 五层架构推进 Change 域 Java 后端实现。①新增 14 个枚举（`change/domain/enums/`）：ChangeStatus（DRAFT/SUBMITTED/IMPACT_ASSESSMENT/PENDING_APPROVAL/APPROVED/IN_PROGRESS/VERIFIED/CLOSED/REJECTED/RECALLED）、ChangeType（DESIGN_CHANGE/REQUIREMENT_CHANGE/CLASH_RESOLUTION/REGULATORY_CHANGE/AI_SUGGESTED/EMERGENCY）、ChangePriority（LOW/MEDIUM/HIGH/CRITICAL）、AffectedObjectType（REQUIREMENT/DESIGN_MODEL/DRAWING/RULE/FINDING/PUBLICATION/OTHER）、AffectedAction（ADDED/MODIFIED/REMOVED/REPLACED）、ImpactLevel（NONE/LOW/MEDIUM/HIGH/UNKNOWN）、RecheckStatus（NOT_REQUIRED/PENDING/IN_PROGRESS/COMPLETED/FAILED）、TaskPlanStatus（PENDING/IN_PROGRESS/COMPLETED/SKIPPED/BLOCKED）、ClosureEvidenceType（DESIGN_REVIEW/RULE_RUN/AI_REVIEW/SIGNATURE/TEST_REPORT/VERIFICATION/OTHER）、ClosureEvidenceStatus（PENDING/SUBMITTED/VERIFYING/VERIFIED/REJECTED/INVALID）、ChangeOperationPhase（CREATE_DRAFT/SUBMIT/IMPACT_ASSESSMENT/SUBMIT_IMPACT/APPROVE/REJECT/RECALL/START_IMPLEMENTATION/GENERATE_TASK_PLAN/SUBMIT_VERIFICATION/VERIFY_CLOSURE/CLOSED）、ChangeOperationPhaseStatus（COMPLETED/IN_PROGRESS/FAILED/SKIPPED）。②5 个子域完整五层结构（domain/repository/service/dto/controller）：ChangeRequest（变更请求核心实体，状态机流转 SUBMITTED→IMPACT_ASSESSMENT→PENDING_APPROVAL→APPROVED→IN_PROGRESS→VERIFIED→CLOSED）、AffectedItem（受影响项，UNKNOWN 影响项阻断关闭）、TaskPlanItem（处置任务，blocksClosure=true 阻断关闭）、ClosureEvidence（关闭证据，高风险类型双人复核机制）、ChangeOperation（操作阶段时间线，记录变更全生命周期事件）。③`V18__init_change.sql` 数据库迁移脚本：创建 `change` schema，5 张核心表（change_request/affected_item/task_plan_item/closure_evidence/change_operation），含索引、约束、注释、PII 分级标注；change_request 表强制 audit 字段（created_at/updated_at/created_by/updated_by/row_version/deleted_at/deleted_by）支持乐观锁与软删除；外键约束 ON DELETE CASCADE 保证引用完整性。④ChangeRequestService 实现 11 个业务方法：listChangeRequests（分页查询+多条件过滤）、getChangeRequestDetail（详情+子实体聚合）、createChangeRequest（草稿创建）、updateChangeRequest（草稿编辑）、deleteChangeRequest（草稿删除）、submitImpactAssessment（影响评估提交，CRITICAL 优先级强制 stepUpToken）、approveChangeRequest（批准，需 stepUpToken + 责任确认 + 职责分离：批准人≠发起人）、rejectChangeRequest（拒绝，需 stepUpToken）、recallChangeRequest（撤回，需 stepUpToken）、startImplementation（启动实施，需 APPROVED 状态 + 指定实施人）、verifyClosure（验证关闭，需 stepUpToken + 职责分离：关闭人≠批准人/实施人 + 所有任务完成 + 所有证据已验证 + 无 UNKNOWN 影响项）。⑤同步修复编译错误：ErrorCode 为 final class（int 常量），不存在 `VALIDATION_FAILED` 枚举值，将 4 个 Service 类共 29 处错误引用替换为 `ErrorCode.BUSINESS_RULE_VIOLATION`（4220，业务规则违反），对齐 api-conventions.md §业务错误码段。⑥编译验证：`mvn clean compile` BUILD SUCCESS，335 个源文件全部通过编译（含 35 个新增 Change 域文件）。V0 差距记录：①集成测试与单元测试待补充（参考 A-15 治理域集成测试模式）；②stepUpToken 二次认证实际校验逻辑待 V1 实现（V0 占位通过）；③AI 辅助影响分析（ai_assisted_analysis 字段）待 V1 LLM Provider 接入；④影响图可视化数据结构（ImpactGraph）待 V1 设计；⑤关闭证据自动收集（DESIGN_REVIEW/RULE_RUN/AI_REVIEW）待 V1 联动发布/合规/AI 复核服务；⑥与 BFF ChangeProxyController 端到端联调待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.16、@design/D11-专业协同与多专业校审.md、@design/D46-追踪完整性-一致性总审.md、@design/D35-API-事件契约.md。

- 2026-07-28：BFF 代理层补齐 Change + Operations 域控制器（V0 透传 + V1 后端对接预留）。对照 `@design/D37-关键界面-交互状态.md §D37.16 P12` 与 `§D37.17 Monitoring`，按"前端骨架 + V0 透传 + V1 后端 API 对接预留"策略推进。①新增 `apps/bff/src/proxy/change/change-proxy.controller.ts`：`@Controller("v1/changes")` + `@All("*")` 通配匹配，覆盖 ChangeApiPaths 全部 16 个端点（list/create/detail/update/delete/submitImpactAssessment/approve/reject/recall/verifyClosure/affectedItems/taskPlans/generateTaskPlan/closureEvidences/operations）；extractBody 对 GET/HEAD/DELETE 返回 undefined（对齐 RESTful 语义）；extractForwardHeaders 转发 Authorization/x-tenant-id/x-user-id/x-trace-id/Idempotency-Key/content-type/Accept-Language，traceId fallback 使用 request.traceId；normalizeQuery 过滤 undefined 保留 string 与 string[]。②新增 `apps/bff/src/proxy/operations/operations-proxy.controller.ts`：`@Controller("v1/operations")` + `@All("*")` 通配匹配，覆盖 MonitoringApiPaths 全部 9 个端点（overview/slos/queue/queueTask/workers/worker/connectors/connector/action），包括 `POST /v1/operations:action` 危险动作（isolate/retry/reconcile/failover/pause/resume/cancel）透传 stepUpToken + impactPreviewAcknowledged + reason 字段。③`apps/bff/src/proxy/proxy.module.ts` 注册两个新控制器到 controllers 数组，置于 GovernanceProxyController 之后、ProxyController 之前（NestJS 按声明顺序匹配路由，更具体的 path 前缀优先）；更新模块注释说明路由优先级链。④新增 `apps/bff/tests/unit/proxy/change-proxy.controller.test.ts` 8 个测试用例（GET 列表透传授权头与 query / POST 创建转发 body / POST :approve 高风险动作转发 stepUpToken / DELETE body 为 undefined / GET 子资源路径 affected-items / traceId fallback / 非 2xx 响应原样透传如 404 / POST task-plans:generate 动作透传）+ `apps/bff/tests/unit/proxy/operations-proxy.controller.test.ts` 8 个测试用例（GET overview 透传授权头 / GET slos / GET queue query 参数 / GET workers/:id 详情路径 / POST operations:action 危险动作透传 stepUpToken + impactPreviewAcknowledged / traceId fallback / 非 2xx 响应原样透传如 501 / GET connectors type 过滤）。V0 策略说明：纯透传模式，不做 schema 验证；后端 Change/Operations API（/api/v1/changes/**、/api/v1/operations/**）尚未实现，返回 404/501 时由前端显示空状态（对齐 D37 §空状态红线），不伪造数据；安全红线（stepUpToken 二次认证、职责分离、危险动作影响预览）由 Core Service 校验，BFF 仅做透传不解析。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm --filter @design-platform/bff lint` 通过（exit code 0，14 warning 全部来自历史文件 ai-capability/solutions-proxy.controller.test.ts 与本次变更无关，0 errors），`pnpm --filter @design-platform/bff test -- --run change-proxy.controller.test.ts operations-proxy.controller.test.ts` 16 测试全部通过。V0 差距记录：后端 Change 域 API（ChangeRequest/ImpactGraph/TaskPlan/ClosureEvidence）待 V1 实现；后端 Operations 域 API（SLO/Queue/Worker/Connector）待 V1 实现；危险动作执行引擎（isolate/retry/reconcile/failover）待 V1（依赖 Saga + Outbox + stepUpToken 二次认证）；schema 验证待 V1 后端实现后补充（参考 governance-proxy 软验证模式）。权威源：@design/D37-关键界面-交互状态.md §D37.16/§D37.17、@design/D35-API-事件契约.md、@design/D42-SLO-容量.md、@design/D44-部署拓扑-Hybrid-Site.md。

- 2026-07-28：运营中心（Monitoring / Operations）对照 D37.17 设计基线完成（V0 前端骨架 + V1 API 对接预留）。对照 `@design/D37-关键界面-交互状态.md §D37.17 Monitoring` 与 `@design/D42-SLO-容量.md`、`@design/D44-部署拓扑-Hybrid-Site.md`，按"前端骨架 + V0 空状态 + V1 API 对接预留"策略推进。①新增 `packages/shared/src/contracts/monitoring.contract.ts`：定义 SloStatus/QueueTaskType/QueueTaskStatus/QueueTaskPriority/WorkerType/WorkerRuntimeStatus/ConnectorType/ConnectorHealthStatus/OperationsActionType 9 个枚举（UPPERCASE 对齐 Java 后端枚举序列化），SloTargetDto/QueueTaskDto/WorkerStatusDto/ConnectorStatusDto/OperationsOverviewDto 5 个核心 DTO，ListQueueTasksRequest/ListWorkersRequest/ListConnectorsRequest 3 个列表请求 DTO，OperationsActionRequest/OperationsActionResponseDto 2 个动作 DTO，MonitoringApiPaths 端点定义（overview/slos/queue/queueTask/workers/worker/connectors/connector/action）；附 9 个枚举映射常量（SLO_STATUS_LABEL/COLOR、QUEUE_TYPE_LABEL、QUEUE_STATUS_LABEL/COLOR、QUEUE_PRIORITY_LABEL/COLOR、WORKER_TYPE_LABEL、WORKER_STATUS_LABEL/COLOR、CONNECTOR_TYPE_LABEL、CONNECTOR_STATUS_LABEL/COLOR、OPERATIONS_ACTION_LABEL）。②`packages/shared/src/index.ts` 新增 `export * from "./contracts/monitoring.contract"` 暴露契约类型。③新增 `apps/web/src/hooks/use-monitoring-operations.ts`：封装 useOperationsOverview（30s 自动刷新）、useSlos、useQueueTasks（15s 自动刷新，支持 status/type/priority/workerId/keyword 过滤）、useQueueTask（详情）、useWorkers（支持 status/type/region 过滤）、useWorker（详情）、useConnectors（支持 status/type 过滤）、useConnector（详情）、useOperationsAction（主动作 mutation，invalidate overview/queue/workers/connectors 缓存）9 个 TanStack Query hooks；retry 策略对 404/501（后端未实现）跳过重试避免浪费请求；附 computeQueueStats/computeWorkerStats/computeConnectorStats 3 个派生统计工具函数与 canExecuteOperationsAction 约束判定函数（必须 reason + 影响预览确认 + 高风险动作 isolate/failover 需 stepUpToken + 不可逆动作 cancel 需 stepUpToken）。④改造 `apps/web/src/app/(dashboard)/monitoring/page.tsx`：移除 mock 数据，使用 useOperationsOverview/useSlos/useQueueTasks/useWorkers/useConnectors 真实 API hooks；五 Tab 布局（概览 / SLO 影响 / 任务队列 / Worker / 连接器）对齐 D37.17 §Operations；顶部 Alert 提示 V1 接入路径 + 刷新按钮；retry storm 检测时显示红色 Alert 并暂停自动重试；unknown job 显式标识不并入 queued/running；Hybrid-Site Region 标记（worker.region + isCustomerSiteWorker 标识客户站点）；空状态用 Empty 占位不伪造数据（对齐 D37.17 §空状态红线）；概览 Tab 显示整体状态（ALL UP/DEGRADED）+ 4 项关键统计（运行/排队/失败/异常 Worker）+ 服务状态卡 + retry storm/unknown job 告警；SLO Tab 显示 SLO 卡片网格（含可用率目标/当前/错误预算剩余/24h 请求数/错误数/p95-p99 延迟）；Queue Tab 显示任务表格（含任务 ID/类型/优先级/状态/Worker/重试次数/排队时长/操作）；Worker Tab 显示 Worker 卡片网格（含当前任务/已处理/失败/平均耗时/心跳/CPU/内存/Region/主动作按钮 V0 占位 disabled）；Connector Tab 显示连接器列表（含类型/状态/调用数/错误数/延迟/许可证）。⑤V0 主动作约束（D37.17 §Operations 危险动作）：isolate/retry/reconcile/failover 为危险动作，V0 占位 disabled，对齐"必须打开影响预览，显示租户/项目/资源数量、不可逆性、替代方案、审批/Step-up 和审计引用"；不能在图表卡片上放无上下文"修复全部"。⑥特殊状态处理（D37.17 §Operations）：unknown job 显示明确文字/图标，不并入 queued/running；retry storm 检测时显示告警并暂停自动重试；数据驻留限制提示跨 Region 操作显示数据驻留约束。同步修复：将派生数据 `slos/queueTasks/workers/connectors` 通过 useMemo 缓存，依赖项设为对应 query.data，消除 react-hooks/exhaustive-deps 4 个 warning。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm --filter @design-platform/web lint` 通过（exit code 0，monitoring/page.tsx 0 warning，剩余 7 个 warning 为已有 dashboard/governance 页面历史警告与本次变更无关，0 errors）。V0 差距记录：后端 Operations API（/api/v1/operations/**）待 V1 实现；主动作执行引擎（isolate/retry/reconcile/failover）待 V1（依赖 Saga + Outbox + stepUpToken 二次认证）；retry storm 自动恢复功能待 V1（需 stepUpToken）；Worker 主动作（暂停/恢复）待 V1（需 stepUpToken + 影响预览）；连接器健康主动检查待 V1（依赖 Connector Schedule）；SLO 错误预算自动告警待 V1（依赖 Prometheus Alertmanager）。权威源：@design/D37-关键界面-交互状态.md §D37.17 Monitoring、@design/D42-SLO-容量.md、@design/D44-部署拓扑-Hybrid-Site.md、@design/D35-API-事件契约.md（Operations 域契约待 V1 定义）。

- 2026-07-28：P12 变更影响与闭环工作台对照 D37.16 设计基线完成（V0 前端骨架 + V1 API 对接预留）。对照 `@design/D37-关键界面-交互状态.md §D37.16 P12` 与 `@design/D11-专业协同与多专业校审.md`、`@design/D46-追踪完整性-一致性总审.md`，按"前端骨架 + V0 空状态 + V1 API 对接预留"策略推进。①新增 `packages/shared/src/contracts/change.contract.ts`：定义 ChangeStatus/ChangeType/ChangePriority/AffectedObjectType/ImpactLevel/AffectedAction/RecheckStatus/TaskPlanStatus/ClosureEvidenceType/ClosureEvidenceStatus/ChangeOperationPhase 11 个枚举（UPPERCASE 对齐 Java 后端枚举序列化），AffectedItemDto/TaskPlanItemDto/ClosureEvidenceItemDto/ChangeRequestDto/ChangeRequestDetailDto/ChangeOperationPhaseDto 6 个核心 DTO，ListChangeRequestsRequest/CreateChangeRequestRequest/SubmitImpactAssessmentRequest/ApproveChangeRequestRequest/RejectChangeRequestRequest/GenerateTaskPlanRequest/VerifyClosureRequest/RecallChangeRequestRequest 8 个请求 DTO，ChangeApiPaths 端点定义（list/create/detail/update/delete/submitImpactAssessment/approve/reject/generateTaskPlan/taskPlans/affectedItems/closureEvidences/verifyClosure/recall/operations）；附 15 个枚举映射常量（CHANGE_STATUS_LABEL/COLOR、CHANGE_TYPE_LABEL、CHANGE_PRIORITY_LABEL/COLOR、AFFECTED_OBJECT_TYPE_LABEL、IMPACT_LEVEL_LABEL/COLOR、AFFECTED_ACTION_LABEL/COLOR、RECHECK_STATUS_LABEL/COLOR、TASK_PLAN_STATUS_LABEL/COLOR、CLOSURE_EVIDENCE_TYPE_LABEL、CLOSURE_EVIDENCE_STATUS_LABEL/COLOR、CHANGE_OPERATION_PHASE_LABEL、CHANGE_OPERATION_PHASE_STATUS_LABEL/COLOR）。②`packages/shared/src/index.ts` 新增 `export * from "./contracts/change.contract"` 暴露契约类型。③新增 `apps/web/src/hooks/use-changes.ts`：封装 useChangeRequests（列表查询，支持 keyword/status/type/priority 过滤）、useChangeRequestDetail（详情含子实体）、useTaskPlans/useClosureEvidences/useChangeOperations（3 类子实体列表查询）、useAffectedItems（受影响对象列表）、useCreateChangeRequest（创建草稿）、useUpdateChangeRequest（更新草稿）、useDeleteChangeRequest（删除草稿）、useSubmitImpactAssessment（提交影响评估，需 stepUpToken）、useApproveChangeRequest（批准变更，需 stepUpToken + 责任确认）、useRejectChangeRequest（拒绝变更，需 stepUpToken）、useGenerateTaskPlan（生成处置任务）、useVerifyClosure（验证关闭，需 stepUpToken + 责任确认）、useRecallChangeRequest（撤回变更，需 stepUpToken）15 个 TanStack Query hooks；retry 策略对 404/501（后端未实现）跳过重试避免浪费请求；附 computeAffectedItemsStats/computeTaskPlanStats/computeClosureEvidenceStats 3 个派生统计工具函数与 canSubmitImpactAssessment/canApproveChange/canVerifyClosure 3 个约束判定函数（关闭前所有任务完成 + 所有证据已验证 + 无 Unknown 影响项 + 职责分离：关闭人不能是批准人/实施人）。④改造 `apps/web/src/app/(dashboard)/changes/page.tsx` 列表页：移除 mock 数据，使用 useChangeRequests hook 拉取真实数据；顶部 Alert 提示 V1 接入路径；错误态用 DataErrorAlert + 重试；表格列含编号/标题（含发起人+批准人）/类型/优先级/状态/影响项数量/处置进度（任务+证据双 Tag）/更新时间/操作；过滤栏含关键字搜索 + 状态/类型/优先级 Select + 刷新按钮；空态引导"新建变更请求"（V0 占位 disabled）。⑤改造 `apps/web/src/app/(dashboard)/changes/[changeId]/page.tsx` 详情页：移除全部 mock 数据，使用 useChangeRequestDetail/useTaskPlans/useClosureEvidences/useChangeOperations 真实 API；6 Tab 布局（来源&理由 / 影响图&矩阵 / 受影响对象 / 处置计划 / 关闭证据 / 操作阶段）对齐 D37.16 §布局；顶部 ChangeHeader 展示编号/标题/状态/类型/优先级 Tag + 8 字段 Descriptions（含 AI 辅助影响分析 Alert 标记"AI 不替代注册建筑师/工程师"）；中部 Tabs 每页含空状态（区分"尚未分析"与"确认无影响"严格分离，对齐 D37.16 §空/未知）；底部主动作栏含"提交影响评估 / 批准变更 / 生成处置任务 / 验证关闭 / 拒绝变更"5 个按钮（按状态条件显示 disabled）+ 批注 TextArea + 职责分离原则 Alert。⑥子组件实现：SourceRationalePanel（来源/理由/影响评估结论 + 5 阶段 Timeline 时间线）、ImpactGraphPanel（影响矩阵表格按对象类型分组统计 ADDED/MODIFIED/REMOVED + V1 占位 Trace 追踪）、AffectedItemsPanel（受影响对象 9 列表格含类型/编号/名称/专业/动作/影响/需复查/复核状态/负责人）、TaskPlanPanel（5 指标卡 + 任务表格）、ClosureEvidencePanel（证据列表 + 关闭批注 TextArea）、OperationsPanel（操作阶段时间线 Timeline）。⑦V0 空状态：后端 ChangeRequest API 未实现时（404/501），所有列表显示 Empty 占位，不伪造数据（对齐 D37.16 §空状态红线"区分尚未分析 / 确认无影响 / 已收集关闭证据"）。⑧主动作约束（D37.16 §主动作：批准与实施/关闭职责分离）：提交影响评估只在 IMPACT_ASSESSMENT 状态启用；批准变更只在 PENDING_APPROVAL 状态启用且需 stepUpToken + 责任确认；生成处置任务只在 APPROVED 状态启用；验证关闭只在 IN_PROGRESS 状态启用且需 stepUpToken + 责任确认 + 所有任务完成 + 所有关闭证据已验证 + 无 Unknown 影响项 + 职责分离（关闭人不能是批准人/实施人）；高风险变更（CRITICAL 优先级）强制 stepUpToken 二次认证（V0 占位）。⑨AI 安全红线（design-constraints.md §AI 安全红线）：所有 AI 辅助影响分析结果标记 isAiAssisted=true，须人工确认；AI 不替代注册建筑师/工程师的专业审签和监管审批；高风险变更强制进入人工复核流程；isAiAssisted=true 时显示"AI 辅助影响分析"Alert 提示。同步修复：shared 包未重新构建导致 web 找不到 change.contract 导出 → `pnpm --filter @design-platform/shared build` 重新编译；详情页移除未使用的 ChangePriority/ChangeStatus/ChangeType 导入；详情页两处双引号 `"尚未分析"`/`"确认无影响"` 转 `&quot;` HTML 实体符合 react/no-unescaped-entities 规则。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm --filter @design-platform/web lint` 通过（exit code 0，仅 7 个 `react-hooks/exhaustive-deps` warning 与已有 dashboard/governance 页面保持一致模式，0 errors）。V0 差距记录：后端 ChangeRequest/ImpactGraph/TaskPlan/ClosureEvidence API（D37.16/D11）待 V1 实现；影响图可视化（节点辐射图）待 V1（依赖 D3.js 或类似库）；Trace 追溯图（变更→需求→设计→验证）待 V1（依赖 TraceLink API）；AI 辅助影响分析管道待 V1（依赖 R3 GoldenDataset + LLM Provider）；处置任务自动生成（AUTO 策略）待 V1（依赖规则引擎）；stepUpToken 二次认证流程待 V1（依赖 IAM step-up 服务）；关闭证据自动收集（DESIGN_REVIEW/RULE_RUN/AI_REVIEW/SIGNATURE）待 V1（依赖发布/合规/AI 复核服务联动）。权威源：@design/D37-关键界面-交互状态.md §D37.16、@design/D11-专业协同与多专业校审.md、@design/D46-追踪完整性-一致性总审.md、@design/D35-API-事件契约.md（Change 域契约待 V1 定义）。

- 2026-07-28：P11 专业提交、校审与发布向导对照 D37.15 设计基线完成（V0 前端骨架 + V1 API 对接预留）。对照 `@design/D37-关键界面-交互状态.md §D37.15 P11` 与 `@design/D05-全流程阶段-阶段门.md`、`@design/D11-专业协同与多专业校审.md`，按"前端骨架 + V0 空状态 + V1 API 对接预留"策略推进。①新增 `packages/shared/src/contracts/publication.contract.ts`：定义 PublicationStatus/ReadinessCheckStatus/EvidenceType/EvidenceOutcome/ReviewerDecisionValue/SignatureRole/SignatureStatus/RecipientType/PublicationOperationPhase/OperationPhaseStatus/ReviewStepKey 11 个枚举，ReadinessCheckDto/EvidenceItemDto/ReviewerDecisionDto/ReviewChecklistItem/SignatureDto/RecipientDto/PublicationOperationPhaseDto/PublicationManifestDto/BaselineDto/PublicationDto/PublicationDetailDto 11 个核心 DTO，ListPublicationsRequest/CreatePublicationRequest/UpdateReviewerDecisionRequest/AcknowledgeWarningRequest/SubmitPublicationRequest/RecallPublicationRequest 6 个请求 DTO，PublicationApiPaths 端点定义（list/create/detail/update/delete/submit/recall/checks/acknowledgeWarnings/evidence/reviewers/updateReviewerDecision/signatures/recipients/operations/baselines/baselineDetail）；附 12 个枚举映射常量（PUBLICATION_STATUS_LABEL/COLOR、READINESS_CHECK_STATUS_LABEL/COLOR、EVIDENCE_TYPE_LABEL、EVIDENCE_OUTCOME_LABEL/COLOR、REVIEWER_DECISION_LABEL/COLOR、SIGNATURE_ROLE_LABEL、SIGNATURE_STATUS_LABEL/COLOR、RECIPIENT_TYPE_LABEL、OPERATION_PHASE_LABEL、OPERATION_PHASE_STATUS_LABEL/COLOR）+ DEFAULT_REVIEW_CHECKLIST/HIGH_RISK_REVIEW_CHECKLIST 2 个检查清单常量。②`packages/shared/src/index.ts` 新增 `export * from "./contracts/publication.contract"` 暴露契约类型。③新增 `apps/web/src/hooks/use-publications.ts`：封装 usePublications（列表查询，支持 keyword/status/projectId 过滤）、usePublicationDetail（详情含子实体）、usePublicationChecks/usePublicationEvidence/usePublicationReviewers/usePublicationSignatures/usePublicationRecipients/usePublicationOperations（6 类子实体列表查询）、useBaselines（Baseline 列表，向导 Step 1 选择）、useCreatePublication（创建草稿）、useAcknowledgeWarnings（确认警告项）、useUpdateReviewerDecision（更新复核决策）、useSubmitPublication（提交发布，强制 stepUpToken + responsibilityAcknowledged）、useRecallPublication（撤回发布，强制 stepUpToken）、useDeletePublication（删除草稿）15 个 TanStack Query hooks；retry 策略对 404/501（后端未实现）跳过重试避免浪费请求；附 computeCheckStats/computeReviewerStats/computeSignatureStats/computeOperationStats 4 个派生统计工具函数与 canSubmitPublication 约束判定函数（阻断项=0 + 警告项已确认 + 复核决策无 PENDING/REJECT 才可提交）。④改造 `apps/web/src/app/(dashboard)/publications/page.tsx` 列表页：移除 mock 数据，使用 usePublications hook 拉取真实数据；顶部 Alert 提示 V1 接入路径；错误态用 DataErrorAlert + 重试；表格列含编号/标题/Baseline+hash/状态 Tag/签名进度 Tooltip/收件人/Retention/发布时间/操作；过滤栏含关键字搜索 + 状态 Select + 刷新按钮；空态引导"新建发布"跳转 `/publications/new`。⑤改造 `apps/web/src/app/(dashboard)/publications/new/page.tsx` 向导页：移除 mock 数据，使用 useBaselines/usePublicationChecks/usePublicationEvidence/usePublicationReviewers/useCreatePublication/useAcknowledgeWarnings/useSubmitPublication 真实 API；7 步 Stepper（baseline/completeness/evidence/reviews/manifest/submit/receipt）实现状态流转；顶部阻断/警告/待评审/拒绝汇总条；每步主区按 D37.15 设计渲染（BaselineStep/CompletenessStep/EvidenceStep/ReviewsStep/ManifestStep/SubmitStep/ReceiptStep 子组件）；canGoNext 守卫对齐 D37.15 §正常状态（Step 1 需选 Baseline + 填标题；Step 2 需 blockingCount=0；Step 3 需所有 warning 已确认；Step 4 需所有评审有决策且无 REJECT；Step 5 需标题/收件人/Retention/签名要求；Step 6 需 stepUpReason≥10 字符 + stepUpToken + 责任确认）；Step 1→2 首次创建 Publication 草稿；Step 3→4 提交警告项确认；Step 6 调用 useSubmitPublication mutation。⑥新增 `apps/web/src/app/(dashboard)/publications/[id]/page.tsx` 详情页：7 Tab 布局（概览/完整性检查/证据/复核决策/签名/收件人/操作阶段），顶部发布头展示编号/标题/状态/Baseline/发布人/时间，中部统计卡（阻断/警告/待评审/签名进度），主动作撤回（PUBLISHED/READY_TO_PUBLISH/PUBLISHING 可撤回，需 stepUpToken）+ 删除草稿（仅 DRAFT）；撤回 Modal 含不可逆警告 Alert + 撤回原因 TextArea（≥10 字符）+ stepUpToken Password 输入；操作阶段 Timeline 展示 SEALING/SIGNING/OBJECT_LOCK/NOTIFICATION 4 阶段进度。⑦V0 空状态：后端 Publication API 未实现时（404/501），所有列表显示 Empty 占位，不伪造数据（对齐 D37.15 §空状态红线"区分无发布 / 草稿中 / 已发布 / 失败 / 已撤回"）。⑧主动作约束（D37.15 §主动作）：最终提交只在所有阻断项关闭 + 精确 Baseline 冻结 + SoD 满足时启用；Review 决策 Accept/Return/Reject/Conditional 必须 reason + checklist；Conditional 须有责任人/期限/影响范围；Step-up 确认后提交 Operation 不可逆；撤回操作需 stepUpToken 二次认证。⑨安全红线（design-constraints.md §AI 安全红线 + §PII 分级 L5）：所有发布必须由注册建筑师/工程师签章；签名后对象锁定不可篡改；Retention 与 ISO 19650 §archival requirements 对齐；isAiAssisted 强制 false（发布不依赖 AI 输出）；requiresHumanReview 强制 true（发布必须人工签章）。同步修复：shared 包未重新构建导致 web 找不到 publication.contract 导出 → `pnpm --filter @design-platform/shared build` 重新编译；`publications/[id]/page.tsx` 删除文件底部重复的 `import { Input } from "antd"`；删除 Modal 的 `okButtonDanger` 属性（antd 版本不支持，统一通过 `okButtonProps.danger` 表达）；RecipientType 索引显式声明参数类型 `render: (type: RecipientType) => ...`；`publications/new/page.tsx` 移除未使用的 DEFAULT_REVIEW_CHECKLIST/HIGH_RISK_REVIEW_CHECKLIST/PublicationStatus/ReviewChecklistItem 导入与 ReviewsStep 的 onUpdateDecision 参数（V0 仅显示评审状态，不直接修改决策）；useUpdateReviewerDecision hook 调用同步移除。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm --filter @design-platform/web lint` 通过（exit code 0，仅 7 个 `react-hooks/exhaustive-deps` warning 与已有 dashboard/governance 页面保持一致模式，0 errors）。V0 差距记录：后端 Publication/Submission/Signature/Recipient API 待 V1 实现；Baseline 冻结 + Hash 计算链路待 V1（依赖 CDE Version 服务）；Operation 执行引擎（Sealing/Signing/Object Lock/Notification）待 V1（依赖 Saga + Outbox）；归档包生成与对象锁定待 V1（依赖 MinIO Object Lock API）；电子签章集成待 V1（依赖外部 CA 服务）；收件人通知通道（邮件/站内信）待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.15、@design/D05-全流程阶段-阶段门.md、@design/D11-专业协同与多专业校审.md、@design/D35-API-事件契约.md（Publication 域契约待 V1 定义）。

- 2026-07-28：P09 AI/Agent 复核中心对照 D37.13 设计基线完成（V0 前端骨架 + V1 API 对接预留）。对照 `@design/D37-关键界面-交互状态.md §D37.13 P09` 与 `@design/D27-Agent-工具调用治理.md`、`@design/D28-AI-ML生命周期-评测.md`、`@design/D24-AI能力目录-网关.md`，按"前端骨架 + V0 空状态 + V1 API 对接预留"策略推进。①新增 `packages/shared/src/contracts/ai-review.contract.ts`：定义 AiRunMode/AiRunStatus/AiStepStatus/ToolCallStatus/GuardrailType/GuardrailStatus/AiRiskLevel/ReviewDecision/ReviewStatus 9 个枚举（UPPERCASE 对齐 Java 后端枚举序列化），AiTokenUsageDto/AiInputManifestDto/AiCitationDto/AiConfidenceDto/ToolCallDto/GuardrailDto/AiStepDto/OutputDiffDto/AiInvocationRunDto/ReviewEvaluationDto 10 个核心 DTO，ListAiRunsRequest/ApproveToolCallRequest/SubmitReviewDecisionRequest/ControlAiRunRequest 4 个请求 DTO，AiReviewApiPaths 端点定义（runs/runDetail/steps/stepDetail/toolCalls/toolCallDetail/approveToolCall/rejectToolCall/guardrails/controlRun/reviews/reviewDetail/submitDecision/evaluation）。②`packages/shared/src/index.ts` 新增 `export * from "./contracts/ai-review.contract"` 暴露契约类型。③新增 `apps/web/src/hooks/use-ai-review.ts`：封装 useAiRuns（列表查询，支持 status/mode/riskLevel/pendingReviewOnly 过滤）、useAiRun（详情）、useAiSteps（步骤列表）、useToolCalls（工具调用列表）、useApproveToolCall（APPROVE/REJECT mutation，高风险需 stepUpToken）、useGuardrails（护栏结果列表）、useReviewEvaluation（评估详情）、useSubmitReviewDecision（提交复核决策 mutation，invalidate run/runs/evaluation 缓存）、useControlAiRun（PAUSE/RESUME/CANCEL mutation，触发多缓存失效）9 个 TanStack Query hooks；retry 策略对 404/501（后端未实现）跳过重试避免浪费请求；附 computeGuardrailStats/computeTokenUsage 派生工具函数与 AI_RUN_STATUS_LABEL/AI_RUN_MODE_LABEL/AI_RISK_LEVEL_LABEL/TOOL_CALL_STATUS_LABEL/GUARDRAIL_STATUS_LABEL 等 5 个枚举映射常量。④新增 `apps/web/src/app/(dashboard)/projects/[id]/ai/runs/[runId]/page.tsx` 主页面：三栏布局（左 260px Step 时间线 + 中 1fr 输入/输出对照 + 右 360px Evidence rail），顶部 RunHeader 展示状态/模式/风险 Tag 与暂停/恢复/取消控制按钮，等待审批 ToolCall 与 Guardrail 阻断/警告 Alert 横幅提示，中栏 Segmented 切换「输入/输出 Diff/工具调用/护栏」4 个 Tab，右栏 EvidenceRail 展示 Citation 列表（含相关性评分）+ Confidence 区段（含依据/校准来源）+ 责任确认 Alert，底部 ReviewDecisionBar 决策栏（仅 requiresHumanReview && status===COMPLETED 时显示），最末 AI 辅助输出声明 Alert（标记"AI 不替代注册建筑师/工程师的专业审签"）。⑤新增 5 个子组件（位于 `apps/web/src/app/(dashboard)/projects/[id]/_components/ai-review/`）：`step-timeline.tsx`（左栏 Step 时间线，每步展示状态/类型/耗时/TC 数/GR 数/错误信息，选中高亮联动中部 ToolCall 过滤）、`tool-call-card.tsx`（工具调用卡片，展示输入/输出 JSON 折叠视图，AWAITING_APPROVAL 状态显示 Approve/Reject 按钮，高风险工具调用需 stepUpToken 二次认证，拒绝需明确原因 Modal）、`output-diff.tsx`（输出 Diff 展示，支持 text/json/structured 三种类型，行级 unified diff 用 +/- 前缀与红绿背景高亮，字段级 diff 用表格展示 added/removed/modified，附 AI 输出仅形成 Draft/Proposal 提示）、`guardrail-banner.tsx`（护栏横幅，PASSED/WARNING/BLOCKED 三态对应 success/warning/error Alert，展示触发规则列表/原因/详细信息/处置建议，BLOCKED 时显示"不允许下游使用"红色提示）、`review-decision-bar.tsx`（决策栏含 ACCEPT_AS_DRAFT/EDIT/REJECT/ESCALATE 4 个 Radio 选项 + 决策原因 TextArea + 检查清单 Checkbox 5 项基础+2 项高风险额外项 + stepUpToken（高风险必填）+ 盲审 Checkbox + 责任确认 Checkbox 强制勾选 + 目标 ETag 提示，ACCEPT_AS_DRAFT 时显示 draftType Select）。⑥V0 空状态：后端 AI Review API（D27/D28）未实现时，所有列表显示 Empty 占位，不伪造数据（对齐 D37.13 §空状态红线"区分无 Run / Run 进行中 / Run 已完成 / 等待 ToolCall 审批"）。⑦主动作约束（D37.13 §主动作）：Accept 仅生成 Draft/Proposal（不直接进入业务状态），需字段级 diff + 目标 ETag + 责任确认；高风险输出只允许形成 Proposal/草稿；拒绝/上报必须填写 reason；高风险决策需 stepUpToken。⑧AI 安全红线（design-constraints.md §AI 安全红线）：所有 AI 输出标记 isAiAssisted=true；AI 不替代注册建筑师/工程师的专业审签和监管审批；高风险输出强制进入人工复核流程；ToolCall 审批与 Run 决策高风险需 stepUpToken（V0 占位）。同步修复：useAiRuns 的 params 类型改为 `Omit<ListAiRunsRequest, "projectId">` 避免默认值缺 projectId 报错；useControlAiRun 的 onSuccess 删除未使用的 data 参数；computeTokenUsage 的 steps 参数标记为 `_steps: AiStepDto[] | undefined` 兼容旧调用方；review-decision-bar.tsx 移除未使用的 useState/Title 导入；page.tsx 移除未使用的 CheckCircleOutlined/CloseCircleOutlined/EditOutlined/ReviewDecision 导入与 isRunning/isPaused/isCompleted 局部变量；修复模块路径 `../../_components/ai-review/` → `../../../_components/ai-review/`（page.tsx 在 `[runId]/page.tsx` 层级，需回退 3 层找到 `_components/`）；DataErrorAlert 的 `action` prop 改为 `onRetry + retryLabel`；ToolCallList/GuardrailList 中 `import("@design-platform/shared").ToolCallDto[]` 内联类型改为顶部已导入的 `ToolCallDto[]`。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm --filter @design-platform/web lint` 通过（exit code 0，仅 7 个 `react-hooks/exhaustive-deps` warning 与已有 dashboard/governance 页面保持一致模式，0 errors）。V0 差距记录：后端 AIInvocationRun/AgentRun/Step/ToolCall/Guardrail API（D27/D28）待 V1 实现；Agent 实时运行控制（暂停/恢复/取消）后端控制接口待 V1；字段级 diff 自动生成待 V1（依赖 OutputDiff 算法与 BIM 字段映射）；Citation 自动提取待 V1（依赖 RAG 引用追溯链路）；Confidence 校准管道待 V1（依赖 R3 GoldenDataset 评估）。权威源：@design/D37-关键界面-交互状态.md §D37.13、@design/D27-Agent-工具调用治理.md、@design/D28-AI-ML生命周期-评测.md、@design/D24-AI能力目录-网关.md、@design/D35-API-事件契约.md（AI Review 域契约待 V1 定义）。

- 2026-07-28：P07 协调、碰撞与 Issue 工作台对照 D37.11 设计基线完成（V0 前端骨架 + V1 API 对接预留）。对照 `@design/D37-关键界面-交互状态.md §D37.11 P07` 与 `@design/D11-协调-碰撞-问题.md`，按"前端骨架 + V0 空状态 + V1 API 对接预留"策略推进。①扩展 `packages/shared/src/contracts/coordination.contract.ts`：新增 CoordinationCheckType/ClashRunStatus/CoordinationFindingSeverity/CoordinationFindingStatus/ClusterStatus/WaiverStatus 6 个枚举（大写值对齐 Java 后端枚举序列化），ClashRuleDto/ClashRunDto/FindingDto/ClusterDto/ViewpointDto/IssueCommentDto/WaiverDto 7 个核心 DTO，ListClashRunsRequest/ListFindingsRequest/ListClustersRequest/CreateClashRunRequest/CreateIssueFromFindingRequest/CreateWaiverRequest/ReviewWaiverRequest/MergeClusterRequest 8 个请求 DTO，CoordinationApiPaths 端点定义（runs/findings/clusters/issueCreateFromFinding/waivers/waiverReview/viewpoints/comments）。**关键修复**：将原 FindingSeverity/FindingStatus 重命名为 CoordinationFindingSeverity/CoordinationFindingStatus，避免与 `review.schema.ts` 的同名类型冲突（review 域使用 lowercase，coordination 域使用 UPPERCASE 对齐 Java 序列化）。②新增 `apps/web/src/hooks/use-coordination.ts`：封装 useClashRuns/useFindings/useClusters/useCreateIssueFromFinding/useCreateWaiver/useReviewWaiver/useViewpoints/useComments/useWaivers 等 20+ TanStack Query hooks；retry 策略对 404/501（后端未实现）跳过重试避免浪费请求；onSuccess 自动 invalidate 关联 query 触发刷新。③改造 `apps/web/src/app/(dashboard)/projects/[id]/coordination/page.tsx` 为三栏布局：左栏 RunSelector（ClashRun 列表 + 状态监控）+ RuleMatrix（规则矩阵高亮当前 Run 应用的规则）；中栏 Segmented 切换 Findings/Clusters/Issues 三视图 + DataGrid（Finding 列含严重性/状态/规则/源构件/目标构件/操作，Cluster 列含标题/主规则/主严重性/Finding 数/AI 置信度/审核状态/操作，BCF Issue 列含标题/优先级/状态/操作）；右栏 IssueContextRail（Viewpoint/Comment/Waiver 三段式上下文）。顶部摘要卡（Open Findings/Critical Findings/Pending Clusters/Open Issues）+ 工具栏（关键字搜索 + 创建 Issue/Waiver 按钮）。④新增 4 个子组件：`run-selector.tsx`（左侧 Run 选择面板，展示 ClashRun 状态机 PENDING/RUNNING/COMPLETED/FAILED/CANCELLED，支持切换 Run 自动清空选中态）、`rule-matrix.tsx`（左侧规则矩阵面板，展示项目下所有 ClashRule，高亮当前 Run 应用的规则）、`issue-context-rail.tsx`（右侧 Issue 上下文面板，三段式 ContextSection 实现 Viewpoint/Comment/Waiver 列表，封装 useViewpoints/useComments/useWaivers hooks，处理 API 错误和空状态）、`waiver-panel.tsx`（WaiverCreateModal + WaiverReviewModal 双模态框，对齐 D37.11 §关闭/豁免"显示范围/期限/批准人，过期自动回待审"，强制 approvalRole 和 expiresAt 字段，审核流程支持 Approve/Reject + 原因）。⑤V0 空状态：后端 Coordination API（ClashRun/Finding/Cluster/Waiver）未实现时（404/501），所有列表显示 Empty 占位，不伪造数据（对齐 D37.11 §空状态红线"区分当前无 Run / Run 进行中 / Run 已完成但无 Finding / Cluster 已就绪"）。⑥主动作约束（D37.11 §主动作）：Run 结果不能直接成为已确认 Issue，必须人工确认 → "创建 Issue" 按钮仅在 Finding.status === "OPEN" 或 Cluster.status === "PROPOSED" 时启用；关闭 Issue 需验证新模型版本和证据 → WaiverReviewModal 显式提示"批准后将在到期日自动回待审"。⑦AI 安全红线（design-constraints.md §AI 安全红线）：高风险动作（Waiver 审核/撤销）预留 stepUpToken 字段，V0 占位不强制；所有 Waiver 操作进入审计日志（后端 AuditLogInterceptor 已就位）。同步修复：noUncheckedIndexedAccess 导致 `record.issueId` 可能 undefined → 显式判空 `record.issueId!`；`Form.Item.requiredMark` 类型不匹配 → 使用 `requiredMark` 布尔简写；`useMemo` 依赖项 lint 警告 → 将派生数据 `findings/clusters/bcfIssues` 通过 useMemo 缓存，依赖项设为对应 query.data。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm --filter @design-platform/web lint` 通过（exit code 0，仅 7 个 `react-hooks/exhaustive-deps` warning 与已有 dashboard/governance 页面保持一致模式，0 errors）。V0 差距记录：协调检查执行引擎（ClashRun 执行）待 V1 实现（依赖 D11 协调域 API）；BCF Viewpoint 渲染（相机/剖切/选中构件）待 V1 集成 BIM Viewer；Cluster 拆分/合并 UI 待 V1（MergeClusterRequest 已定义）；AI 置信度训练管道待 V1（依赖 R3 GoldenDataset）。权威源：@design/D37-关键界面-交互状态.md §D37.11、@design/D11-协调-碰撞-问题.md、@design/D35-API-事件契约.md（Coordination 域契约待 V1 定义）。

- 2026-07-28：A-15 治理域集成测试完成。新增 3 个集成测试类（共 25 个测试用例），全部通过 `mvn verify` 验证：①`GovernanceSchemaIT`（11 个测试用例）验证 V17 迁移脚本治理域 8 张表（access_grants/releases/data_assets/audit_logs/evidence_packages/backups/restore_drills/restore_drill_items）DDL 落库正确性，包括表存在性、字段完整性、索引、PII 等级标注、COMMENT 注释及触发器；②`AccessGrantRepositoryIT`（9 个测试用例）验证 AccessGrant 实体 CRUD、租户隔离、分页查询、状态统计及约束验证，覆盖 JPA `@Version` 乐观锁初始值为 0（Hibernate 行为，DB DEFAULT 1 被 INSERT 显式值覆盖）场景；③`AuditLogWriteIT`（5 个测试用例）验证审计日志拦截器→异步写入器→Repository 完整链路，覆盖 POST 触发审计、GET 不触发审计、details 结构化、匿名 actor 解析及 PATCH 风险等级评估。同步修复多项问题：①治理域与 IAM 域 `AccessGrantRepository` 类名冲突 → 重命名为 `GovernanceAccessGrantRepository`；②治理域与 IAM 域 `AccessGrant` 实体名冲突 → 添加 `@Entity(name = "GovernanceAccessGrant")`；③Flyway clean 不清理非 defaultSchema → 在 `AbstractIntegrationTest.resetDatabase` 中手动 DROP 所有业务 schema 后再执行 Flyway clean + migrate；④`@WebMvcTest` 切片测试中 `AuditWebMvcConfig` 依赖缺失 → 添加 `@ConditionalOnBean(AsyncAuditWriter.class)` 注解；⑤`AuthControllerTest` 方法调用不匹配 → `resolveTenantId` 改为 `resolveTenantIdOptional`；⑥`ComplianceRuleApiIT` basis 字段类型不匹配 → JSON 对象改为字符串。集成测试基础设施增强：pom.xml 添加 awaitility 依赖用于异步操作稳定等待；`AbstractIntegrationTest.truncateBusinessTables()` 添加治理域 8 张表清理逻辑。验证结果：855 个单元测试 + 131 个集成测试全部通过。A-12（审计日志中间件）、A-13（Flyway 迁移脚本）、A-14（治理域单元测试）、A-15（治理域集成测试）任务全部完成，治理域后端 Java 实现已具备完整测试覆盖（V0 基线）。

- 2026-07-28：A-16 治理域 Controller API 集成测试完成。新增 7 个 Controller API 集成测试类（共 99 个测试用例），全部通过 `mvn verify` 验证：①`AccessGrantControllerApiIT`（12 用例）覆盖访问授权列表查询、详情查询、状态过滤、跨租户隔离、APPROVE/REVOKE/SHORTEN 操作、x-user-id 头部 401 校验、legal hold 4220 业务异常；②`ReleaseControllerApiIT`（18 用例）覆盖发布管理 APPROVE/CANTRY/PROMOTE/ROLLBACK/DEPRECATE 全状态机流转、stepUpToken 高风险操作保护、跨租户隔离；③`DataAssetControllerApiIT`（14 用例）覆盖数据资产 HOLD/RELEASE_HOLD/ARCHIVE/DELETE/REPAIR 操作及业务规则校验；④`AuditLogControllerApiIT`（9 用例）覆盖多条件组合过滤（category/result/riskLevel/traceId/时间范围）、Specification 查询逻辑、租户隔离；⑤`EvidencePackageControllerApiIT`（18 用例）覆盖证据包 SEAL/VERIFY/EXPORT/CHALLENGE 操作及 DRAFT→SEALED→VERIFIED 状态机流转；⑥`BackupControllerApiIT`（16 用例）覆盖备份创建、列表查询、状态过滤、恢复操作（PRODUCTION 需 stepUpToken）；⑦`RestoreDrillControllerApiIT`（12 用例）覆盖灾备演练创建（RUNNING/SCHEDULED）、列表查询、状态过滤、跨租户隔离。同步修复共性问题：①多个 Service 类（EvidencePackageService/BackupService/RestoreDrillService）抛出 BusinessException 时未显式指定 HttpStatus → 为 NOT_FOUND/UNAUTHORIZED/FORBIDDEN 异常分别指定 `HttpStatus.NOT_FOUND/UNAUTHORIZED/FORBIDDEN`，使 GlobalExceptionHandler 正确返回 404/401/403 而非默认 422；②RestoreDrillControllerApiIT 外键约束错误（`restore_drill.backup_id` 引用不存在的 `backup_point` 记录）→ 注入 `BackupPointRepository` 并新增 `createBackup` 辅助方法，先创建 BackupPoint 再用其 ID 作为 backupId。验证结果：`mvn verify` BUILD SUCCESS，230 个测试用例全部通过（0 failures, 0 errors, 0 skipped），其中治理域 124 个测试用例（99 个 Controller API + 25 个 Repository/Schema/Write 集成测试）。A-16 任务完成，治理域后端 Java 实现已具备完整 Controller API 集成测试覆盖（V0 基线），可进入下一阶段任务（BFF 代理层 / Core Service 业务逻辑增强 / 前端 V1 API 对接）。

- 2026-07-28：P01 我的工作页 API 集成完成（V0 前端骨架 + V1 API 对接预留）。在原有 P01 三栏布局基础上，从 mock 占位升级为真实 API 契约 + hook 封装，对齐 D37.5 §P01 设计规格。①新增 `packages/shared/src/contracts/work-item.contract.ts`：定义 WorkItem 聚合契约，包含 6 类来源类型（TASK/ISSUE/REVIEW/APPROVAL/EXCEPTION/AI_REVIEW）、5 类时间分组（NOW/OVERDUE/UPCOMING/WAITING/COMPLETED）、4 类风险等级（LOW/MEDIUM/HIGH/CRITICAL）、5 类状态（PENDING/IN_PROGRESS/BLOCKED/COMPLETED/CANCELLED）、3 类快捷动作（CLAIM/ACKNOWLEDGE/COMPLETE）；定义 WorkItemDto/NextActionDto/SavedViewDto/QuickActionRequest/QuickActionResponse/ListWorkItemsRequest 等 DTO；定义 WorkItemApiPaths（list/detail/quickAction/savedViews/savedView）端点。②`packages/shared/src/index.ts` 新增 `export * from "./contracts/work-item.contract"` 暴露契约类型。③新增 `apps/web/src/hooks/use-work-items.ts`：封装 useWorkItems（列表聚合查询，支持 group/type/risk/keyword/onlyMine 参数）、useWorkItem（详情）、useSavedViews（保存视图列表）、useQuickAction（执行 CLAIM/ACKNOWLEDGE/COMPLETE 快捷动作，成功后 invalidate list + detail query）、useSavedView CRUD（创建/更新/删除保存视图）6 个 TanStack Query hooks；retry 策略对 404/501（后端未实现）跳过重试避免浪费请求。④重写 `apps/web/src/app/(dashboard)/dashboard/page.tsx`：移除 mock 数据，使用 useWorkItems hook 拉取工作项；实现左侧时间分组（5 类，每分组显示数量 Tag，hover 高亮主色）、中部工作项表格（类型/标题/项目/阶段/SLA/风险/状态/操作 8 列，每列对齐 D37.5 §核心组件规格）、右侧快速预览（选中行展示详情 + Claim/Acknowledge/Complete 三个快捷动作按钮）；顶部 Alert 提示 V1 接入路径（聚合查询 API workflow.work.list 待 V1 实现）；SavedView 切换器通过 Segmented 实现，支持「我的工作 / 全部 / 已逾期 / 即将到期」4 类预设视图；表格行点击选中后右侧 Quick Preview 显示详情与 nextAction；快捷动作 mutation 成功后 message.success 提示并自动刷新列表。⑤V0 空状态：后端聚合查询 API 未就位时（404/501），5 个分组显示 0 暂无任务空状态（不伪造数据，对齐 D37.5 §空状态红线「区分当前无任务/筛选无结果/数据同步中」）。⑥同步修复：noUncheckedIndexedAccess 导致 `counts[group.key]` 可能 undefined → 初始化为 `Record<WorkGroupKey, number>` 完整枚举 + 访问时 `?? 0` 空值合并；未使用的 Radio 导入移除。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm -r lint` 通过（0 errors，14 warnings 均为已有历史警告）。权威源：@design/D37-关键界面-交互状态.md §D37.5 P01 我的工作、@design/D36-界面信息架构.md §D36.24、@design/D35-API-事件契约.md。

- 2026-07-28：A-17 治理域前端页面从 mock 切换到真实 API 完成。前端 5 个治理中心页面全部接入 BFF GovernanceProxyController → Core Service 真实数据通路：①`apps/web/src/hooks/use-governance.ts` 扩展：新增 `useCreateBackup` / `useRestoreBackup` 2 个 mutation hook，封装 `POST /api/v1/backups`（创建备份）与 `POST /api/v1/backups/{id}/restore`（恢复操作）端点；恢复操作强制 `stepUpToken` 字段以对齐 V0 高风险操作保护策略；保留 `useBackups` / `useBackup` / `useRestoreDrills` / `useRestoreDrill` / `useCreateRestoreDrill` 5 个已有 hook，并通过 `normalizeEnumFields` 递归归一化 Java 后端返回的大写枚举值（如 `RUNNING` → `running`），前端组件无需感知。②`apps/web/src/app/(dashboard)/governance/backup-restore/page.tsx` 改造：移除全部 mock 数据（`MOCK_BACKUPS` / `MOCK_DRILLS`）和本地 `BackupPointDto` / `DrillRecordDto` 类型定义，改用 `@design-platform/shared` 的 `GovernanceBackupPoint` / `GovernanceRestoreDrill` 类型；备份点列表通过 `useBackups({page:1,pageSize:100})` 拉取，灾备演练列表通过 `useRestoreDrills` 拉取，客户端按 `type` 过滤；恢复操作通过 `useRestoreBackup` mutation 触发，Modal 新增"恢复原因（必填）"输入框（写入审计日志便于追溯）；演练启动通过 `useCreateRestoreDrill` mutation，自动选取最近已验证全量备份作为演练源；演练状态标签对齐 `GovernanceRestoreDrillStatus`（scheduled/running/completed/failed/cancelled），通过情况使用 `passed` 字段展示；保留 `handleVerify` 为前端占位（后端暂无 verify 端点）。③与 `access-review` / `audit` 页面保持一致模式：服务端按 `type`/`riskLevel`/`category` 过滤，客户端按关键字搜索；所有 mutation 操作 invalidate 对应 list query 触发刷新；loading 态用 `<Spin>` 包裹表格，empty 态用 `<Empty>` 占位。验证结果：`pnpm -r typecheck` 全部通过（shared/web/bff 3 包 0 errors），`pnpm -r lint` 通过（exit code 0，仅 3 个 `react-hooks/exhaustive-deps` 警告与已有 access-review/audit 页面保持一致模式，0 errors）。A-17 任务完成，治理中心 5 个页面（Access Review / AI & Rule Release / Data Governance / Audit & Evidence / Backup & Restore）全部完成 V1 API 对接，治理域前端→BFF→Core Service 数据通路完整闭环（V0 基线）。下一步可推进：Core Service 业务逻辑增强（如 AI 风险评估、自动封存等）、剩余前端页面与设计基线对齐、远程环境部署验证。

- 2026-07-28：P03 需求追踪矩阵页对照 D37.7 设计基线完成。对照 `design-ui-system/pages/requirements.html` 与 `@design/D37-关键界面-交互状态.md §D37.7 P03`，按"前端骨架 + V0 空状态 + V1 API 对接预留"策略推进。①新增 `packages/shared/src/contracts/requirement.contract.ts`：定义 RequirementSource/Requirement/TraceLink/CoverageSummary/RequirementHistory 5 个核心 DTO，RequirementSourceType/RequirementCategory/RequirementPriority/RequirementStatus/TraceLinkType 5 个枚举，ListRequirementsRequest/ListRequirementSourcesRequest 请求 DTO，RequirementApiPaths 端点定义（sources/requirements/requirementDetail/traceLinks/coverage/history）。②新增 `apps/web/src/hooks/use-requirements.ts`：封装 useRequirements/useRequirementSources/useRequirement/useRequirementHistory/useTraceLinks/useCoverageSummary 6 个 hook，retry 策略对 404/501（后端未实现）跳过重试避免浪费请求。③新增 `apps/web/src/app/(dashboard)/projects/[id]/requirements/page.tsx`：三栏布局（左侧 SourceTreePanel 260px / 中部需求列表 1fr / 右侧 RequirementDetailRail 360px），顶部 CoverageSummaryBar 覆盖度汇总 + 过滤栏（keyword/category/status）+ 项目面包屑。④新增 `apps/web/src/components/requirement/source-tree-panel.tsx`：按来源类型分组树展示（Brief/Code/Contract/Addendum/Other），每类型 Tag 计数。⑤新增 `apps/web/src/components/requirement/requirement-detail-rail.tsx`：详情、Source Locator、Category/Priority/Status 矩阵、Linked Design Elements（TraceLink 列表）、Change History Timeline。⑥新增 `apps/web/src/components/requirement/coverage-summary-bar.tsx`：Total/Approved/Implemented/Coverage Rate 4 个指标卡 + Progress 条。V0 空状态：后端 Requirement API 未实现时显示"导入来源"引导 Alert，不伪造数据（对齐 D37.7 §空状态红线"无来源时先导入/登记来源；无 Trace 显示必需关系和建议，不自动创建虚假覆盖"）。同步修复：document-upload.tsx 的 HashOutlined → NumberOutlined（@ant-design/icons 无 HashOutlined 导出）；readiness-dashboard.tsx 第 194 行未转义引号 → `&ldquo;就绪&rdquo;`。typecheck + lint 全部通过。V0 差距记录：导入来源 / 新建需求 / 修订提交 主动作按钮 V0 禁用态，待 V1 API 就位后启用；Trace Matrix 视图切换（树/矩阵）待 V1；AI Candidate Diff（Accept/Edit/Reject）待 V1 LLM Provider 接入；字段级冲突合并（Draft 并发修改）待 V1。权威源：@design/D37-关键界面-交互状态.md §D37.7、@design/D14-需求与信息要求.md（待定义）、@design/D46-追踪完整性-一致性总审.md。

- 2026-07-28：P02 项目首页与 Gate 驾驶舱对照 D37.7 设计基线完成。新增 `apps/web/src/components/project/readiness-dashboard.tsx` 实现 4 项 Readiness 指标卡（Stage Progress / Gate Readiness / Baseline Freeze / Publication Readiness），每指标含图标、Tag 占位、Progress 进度条、"数据源待 V1 接入"提示。配置向导 Alert 在 stages 为空时展示。集成到 `apps/web/src/app/(dashboard)/projects/[id]/page.tsx` ProjectHeader 下方。V0 限制：4 项指标均为占位 0%，待 V1 接入 D05 阶段进度 + D05 门禁通过率 + D05 基线冻结 + D11/D18 发布就绪度 API。权威源：@design/D37-关键界面-交互状态.md §D37.7 P02。

- 2026-07-28：P01 我的工作页对照 D37.5 设计基线完成。重写 `apps/web/src/app/(dashboard)/dashboard/page.tsx` 为三栏布局：①左侧 Now/Overdue/Upcoming/Waiting/Completed 5 个时间分组（对齐 D37.5 布局规格），每分组显示数量 Tag，hover 状态高亮分组主色；②中部 WorkItem DataGrid 占位（Empty + "浏览项目"快捷入口）+ 最近访问项目卡片（复用 useProjects hook 真实数据，展示前 5 项）；③右侧 Quick Preview 占位 + Claim/Acknowledge/Complete 三个快捷动作按钮（V0 禁用态，对齐 D37.5 主动作规格"快捷动作仅允许 Claim/Acknowledge/低风险 Complete"）。同步更新 `app-layout.tsx` 侧边栏第一项 label 从 "Dashboard" 改为 "My Work"（路由保留 /dashboard 兼容现有跳转与登录重定向）。V0 限制：Task/Issue/Review/Approval/Exception/AIReview 聚合查询 API 未就位，5 个分组均显示 0 暂无任务空状态（不伪造数据），顶部 Alert 提示 V1 接入路径。权威源：@design/D37-关键界面-交互状态.md §D37.5、@design/D36-界面信息架构.md §D36.24。

- 2026-07-28：P04 CDE 资产库与上传中心对照 D37.8 设计基线完成。对照 `design-ui-system/pages/cde-library.html` 与 `@design/D37-关键界面-交互状态.md §D37.8`，识别 V0 实现与设计规格的差距，按"对齐核心交互 + 标注 V1 差距"策略推进。①`apps/web/src/components/cde/document-upload.tsx` 重构为可恢复上传队列：新增 D37.8 上传状态机常量（queued/hashing/uploading/verifying/completed/failed，对齐设计规格 Queued/Hashing/Uploading/Verifying/Completed/Rejected 6 个核心态），每项独立展示文件名、Tag 状态、Progress 进度、SHA256 哈希摘要（前端 SubtleCrypto API 计算，作为 D37.8 Manifest 占位）、失败重试按钮、清除已完成操作。②前端 sha256 通过 `crypto.subtle.digest("SHA-256", buffer)` 计算，V1 由后端 VerifiedManifest 接管。③队列串行处理（processingUidRef + pendingQueueRef 防 race），onChange 多文件入队后自动触发 processNext 递归消费。④V0 简化映射文档化：V1 完整状态机含 Paused/Scanning/Rejected/Verified/Binding 待后端 D35 UploadSession 接口就位后启用。V0 差距记录：路由命名 `/documents` vs 设计 `/assets` 待 V1 重命名（需同步迁移 /api/v1/documents → /api/v1/assets）；FolderTree 左侧分类树、AssetGrid 缩略图视图、Metadata/Version 右侧固定 rail 待 V1 布局升级（V0 用 Drawer 折叠展示版本历史已满足核心交互）。权威源：@design/D37-关键界面-交互状态.md §D37.8、@design/D36-界面信息架构.md §D36.24、@design/D07-CDE领域-版本.md。

- 2026-07-28：BFF 认证透传修复完成。问题：前端 `readCookie("access_token")` 因 httpOnly 无法读取，Authorization 头永远为空；BFF `extractForwardHeaders` 仅读 Authorization 头，未从 Cookie 中提取 access_token，导致文件上传至 Core 服务时返回 500「未登录或 token 已失效」。修复方案：①新增 `apps/bff/src/middleware/auth-token.middleware.ts` 全局中间件，从 httpOnly Cookie 中提取 access_token 注入到 `request.headers.authorization`，提取 tenant_id 注入到 `request.headers['x-tenant-id']`，跳过 `/api/v1/auth/login`、`/api/v1/auth/refresh`、`/api/v1/auth/logout`、`/api/v1/health`、`/api/v1/metrics` 路径；②在 `app.module.ts` 中注册中间件链 `TraceIdMiddleware → AuthTokenMiddleware → LoggingMiddleware`；③前端 `api-client.ts` 显式设置 `credentials: "include"` 让浏览器携带 httpOnly Cookie；④前端 `document-upload.tsx` XHR 上传显式设置 `xhr.withCredentials = true`。新增 15 个单元测试覆盖 Cookie 提取、跳过路径、URL 编码、空值兜底等场景。BFF 490 测试全部通过、typecheck 通过。权威源：@design/D39-身份多租户-授权.md §D39.7、security.md §2.2。

- 2026-07-27：前端合规页面与设计基线对齐（D36/D37 界面规格对照）完成。①新增 P08 规则检查与规范证据审阅器页面 `/compliance-results/[id]`（对齐 @design/D37-关键界面-交互状态.md §D37.12 路由规格 `/compliance-results/{id}`）。②实现 4 个核心组件：CoveragePanel 覆盖率面板（按 D37.12 规格，分母包含 Unknown/NotApplicable，显式标注）、ResultTree 结果树（左侧规则/结果树，按 objectType 分组，支持选中态）、ResultDetailRail 结果详情栏（含 evidence、explanation，PASS 状态禁用 Exception 入口，Unknown/NotApplicable/Error 显式原因提示）、VerifyExceptionForm Exception 草稿表单（影响范围/依据/期限/补偿控制/签审角色 5 字段，V0 仅本地态，V1 接入后端审批工作流）。③实现 RuleSetBadge 规则集徽章组件（D37.12 核心组件，Tooltip 显示完整 ID）。④更新 /compliance-checks 页面：列表行操作增加「P08 审阅」按钮跳转、详情 Modal footer 增加「打开 P08 审阅器」按钮、顶部增加 Alert 引导提示。⑤三栏布局：左侧 ResultTree + 中部 ResultDetailRail + 右侧 VerifyExceptionForm，对齐 D37.12 设计规格。⑥typecheck 通过（@design-platform/web）。V0 限制说明：ClauseViewer/CitationCard/EvidenceGraph 待 V1（依赖 BIM Viewer 与规范条文库）、创建 Issue 待 V1（依赖 Issue API）、Exception 审批工作流待 V1（依赖 Exception API）、多 Execution 聚合待 V1（V0 仅展示首个 execution 的 results）。

- 2026-07-27：CDE 文档 BFF 代理补齐 + 端到端验证完成。①新增 2 个 BFF 代理控制器：CdeDocumentProxyController（/v1/projects/:projectId/documents + /v1/documents/:id，含文档 CRUD/检出/检入 7 个端点）、CdeVersionProxyController（/v1/documents/:documentId/versions，含版本列表/上传/详情 3 个端点）。契约验证策略：POST 创建/检出/上传使用严格验证（documentDtoSchema/documentVersionDtoSchema/checkoutDtoSchema），GET 详情使用软验证，列表/更新/删除/检入保持透传。②修复 3 个阻断问题：documentDtoSchema.createdBy 从 .nullable() 改为 .nullable().optional()（Jackson 省略 null 字段导致 key 缺失）；cde.schema.ts createdBy 同步修复；Dockerfile 移除 corepack 改用 npm install -g pnpm（registry.npmjs.org 在国内 Docker 容器无法访问）。③全链路验证：文档创建 code=0→文档列表 3 个→详情 DRAFT/2MB→检出 CHECKED_OUT→检入 PUBLISHED 全部通过。CDE 数据通路：前端 → BFF CdeDocumentProxyController → Core DocumentController → PostgreSQL 完整闭环。同时修复 Core Service 的 compliance.basis 字段类型不匹配（CreateRuleRevisionRequest.basis Map→String）、IdsRuleConverter 适配、complianceRuleDtoSchema basis z.record()→z.union()。

- 2026-07-27：合规规则执行引擎（D21.6 DSL）实现完成。核心进展：①修复 CheckResult.execution_id 为空导致数据库约束 violation 的问题——executeRule 方法中 RuleExecution 未提前保存导致 ID 为 null，改为先 saveAndFlush 获取 ID 再执行规则引擎；②修复 basis 字段类型不匹配——complianceRuleDtoSchema 中 basis 从 z.record() 改为 z.union([z.string(), z.record()]) 兼容 TEXT 列返回值、CreateRuleRevisionRequest.basis 从 Map<String,Object> 改为 String 对齐数据库 TEXT 列、IdsRuleConverter 适配序列化；③新增 EXISTENCE_CHECK 规则类型（检查必需空间/元素是否存在，支持 requiredItems 列表配置）；④多规则综合测试通过——单次检查运行执行 3 种规则类型（PROPERTY_CHECK/COUNT_CHECK/EXISTENCE_CHECK），生成 20 个检查结果（15 PASS/5 FAIL），耗时 10ms，状态 COMPLETED。远程环境全链路验证：Core 服务重建部署 + BFF 重启，合规规则创建→修订→规则集组装→检查运行→DSL 引擎执行→结果查询全闭环通过。DSL 引擎当前支持 4 种规则类型：PROPERTY_CHECK（属性阈值比较）、COUNT_CHECK（数量统计）、RANGE_CHECK（范围检查）、EXISTENCE_CHECK（存在性检查）。

- 2026-07-27：V0 全栈验证完成。执行 14 项阻塞项排查（B-01~~B-14），修复 4 项新增阻塞：① DeepSeek API Key 已配置在 AI 容器；② LogMessageBroker 移除 @ConditionalOnMissingBean + Core 重建部署；③ Flyway 14/14 迁移全部成功、50 张表 8 schema 就位；④ BFF HealthController 注册到 AppModule + Nginx proxy_pass 添加 /api 前缀。全栈连通性验证：前端 19 页全部 200、BFF→Core→AI 端到端 API 10/10 通过、MinIO 上传正常、ChromaDB 心跳 200、RAG 查询正常、Auth 登录成功（code 0 + JWT）。种子数据就位（admin@aidesign.com / Test@123456 / V0-TEST-001）。提交记录：a621776（BFF health）、2f205c3（Core enum+MessageBroker）。V0 基线可运行状态达成。在 116.8.109.229 云服务器创建 AiDesign 隔离用户（group=docker, home=/home/AiDesign），Docker Compose 全栈启动 7 个服务（PostgreSQL 16/MinIO/ChromaDB/Core Java 21/AI Python/BFF NestJS/Web Next.js），使用独立 Docker 网络 `aidesign-net`（bridge）和端口段 18000-18099（仅 127.0.0.1 绑定）。数据卷挂载至 `/home/AiDesign/data/`。发现三个关键问题并修复：① Next.js basePath "/aidesign" 未同步到容器 → 本地 pnpm build 后推送 `.next/` 到容器覆盖；② Nginx `location /aidesign/` 的 `proxy_pass http://127.0.0.1:18070/` 去掉了前缀导致 404 → 改为 `location /aidesign { proxy_pass http://127.0.0.1:18070/aidesign; }` 保留前缀；③ Docker build 容器内无法访问 npm registry → apps/web/Dockerfile 配置 npmmirror 国内镜像。验证结果：`https://yun.gxjugu.com/aidesign/` → 308→200 (Dashboard)、/aidesign/dashboard → 200、/aidesign/login → 200。确认 BFF /api/bff/health 返回 404（BFF 未实现 health 端点）。新增阻塞项 B-11~~B-14。提交记录：3c567cb。
- 2026-07-22：设计正文按 D01-D46 章节拆分完成。将唯一设计正文 `deep-research-report.md`（16090 行）按章节拆分为 47 个独立文件（D00-D46），命名规范为 `Dxx-小写短横线标题.md`。拆分脚本位于 `scripts/split-design-doc.py`，每个文件添加了统一文档头部（来源、创建/更新日期、原始行范围、引用约定）。同步更新 `INDEX.md`，新增 D01-D46 章节索引表、章节分组统计、命名规范和引用约定。新增决策 16 记录本次拆分决策。原文件归档至 `his_bak/v1.0-Design-deep-research-report-20260722.md`。拆分后仍维持唯一事实源原则，不产生第二套设计事实；引用约定更新为 `@design/Dxx-xxx.md` 格式（同文件内引用使用 §编号，跨文件引用使用 @design/文件名.md）。基于最新设计文档状态（R1 冻结完成、R2 主体完成、业界对标审计补充整合、Trae 规则库扩展）对 `施工图全流程AI平台综合建设方案.md`（原 686 行 → 848 行）和 `施工图全流程AI平台综合建设方案.html`（重写为 1395 行/84KB）进行同步更新。更新涉及 17 处内容增补或重写，核心变化包括：执行摘要新增 R1/R2 进展概述和 OD 冻结值表、试点范围基线表关联 OD 决策号、新增 V0 模块化单体部署架构和 V0 最小 AI 能力集、技术栈新增 V0 简化栈与升级路径、新增 R2 Support Matrix 冻结版本和 Contract Catalog 48 契约说明、新增 OWASP LLM Top 10 与 EU AI Act 对标、第 18 节部署方案全面重写为 Hybrid-Site 冻结画像、第 21 节新增 6 项 Gate 准入状态表和 IR-08 八任务包状态、主要风险表新增当前状态列、下一步推进按 R2/R3/R4 三阶段重组。HTML 阅读版同步重写，含左侧固定目录导航、Mermaid 图表渲染、状态三色标记（绿/橙/红）、返回顶部按钮等专业商务风格。本方案为非权威派生材料，冲突时回归唯一设计正文 `deep-research-report.md`。
- 2026-07-22：PrismScan L2-project 规则库深度分析与提取完成。对 `/Users/minjianq/Documents/06-Mi-Model-Rule/prismscan-rules/rules/L2-project/` 下 38 条通用工程规则（base=5/domain=30/framework=1/language=2，severity error=8/warning=22/info=8）进行深度分析，按本项目技术栈（Next.js 15/React 19/Ant Design 5 + NestJS 11 + Java 21/Spring Boot 3.4 + Python 3.12/FastAPI + PostgreSQL 16 + Docker Compose→K8s）和设计要求（OD-01 境外英文包/OD-05 外部 AI ManualHandoff/OD-06 Hybrid-Site/AI 安全红线）进行适配提取。提取策略分三类：①新建 8 个规则文件——`security.md`（alwaysApply，合并 security+privacy+auth 核心原则，含 PII 5 级分类、JWT access≤15min/refresh≤7d+rotation、AI 安全红线 4 级风险人工复核、CI 安全门禁 gitleaks/trufflehog/semgrep）、`testing.md`（alwaysApply，多语言测试统一标准，覆盖率总≥80%/建筑核心≥85%/diff=100%/AI 审签路径=100%，Mock 规范禁止真实调用付费 API，TDD 六步法）、`database.md`（globs `services/core/**, apps/bff/**`，PostgreSQL 16 审计字段强制、Flyway 迁移规则、高风险迁移三步法/四步法、Hybrid-Site RPO≤4h/RTO≤8h 备份策略）、`api-conventions.md`（globs `apps/bff/**, services/**`，统一响应格式三语言 ApiResponse/PageResponse、双层状态码 HTTP 200+code===0、业务错误码段表、traceId 全链路传播、对齐 r2-contract-catalog）、`observability.md`（globs `services/**, apps/bff/**`，结构化日志 JSON snake_case 三语言实现、RED/USE 指标、OpenTelemetry+W3C TraceContext、Sentry 全服务集成含脱敏 beforeSend、SLO 核心设计 API≥99.95%）、`deployment.md`（globs `docker/**, .github/**, services/**`，灰度发布 5%→20%→50%→100% 每阶段监控 10 分钟+自动熔断、回滚≤5 分钟、Hybrid-Site 特殊约束含 Windows Worker 离线/弱网、Docker 镜像多阶段构建+非 root 用户、12-factor 环境变量注入、事故响应 P0 15 分钟+48 小时 5Whys 复盘）、`code-review.md`（globs `**`，Reviewer 必查项含 AI 人工复核流程/安全/性能/测试/文档、Conventional Comments 8 标签、PR 大小≤500/≤1500、项目特化 Approval 规则：普通 1 人/契约变更 2 人参考 r2-contract-catalog/AI 安全红线 2 人/Hybrid-Site 2 人参考 r2-deployment-profile）、`devex.md`（globs `.vscode/**, *.config.*, package.json, pyproject.toml, pom.xml`，VSCode 必装扩展、settings.json formatOnSave、本地服务编排 PostgreSQL 16:5432+MinIO:9000/9001、DevEx 指标表首次 build<60s/增量<5s/单测<30s、Husky+lint-staged pre-commit eslint--fix+prettier--write）；②强化 6 个现有规则文件——`coding-standards.md` 补充核心工程原则 10 大 DO+禁止项与代码风格 TS 7 项严格模式、`frontend.md` 补充状态管理 Zustand+TanStack Query+Zod+RHF 与 WCAG 2.2 AA Checklist+工具链+Ant Design 5 适配与 i18n OD-01 适配 UTC+公制 SI+定点数与性能 SLO Web Vitals+Bundle 上限+Ant Design 按需导入、`bff.md` 补充 JWT 规范+RBAC 授权模型+MFA+密码策略与限流算法选型+分层限流+429 响应+熔断器 CLOSED→OPEN→HALF_OPEN+降级策略 L1-L4、`backend-java.md` 补充构造器注入+Controller 薄层+异常处理+JVM 调优-Xms==-Xmx+G1GC+HeapDump+测试门禁 jacoco≥80%+ArchUnit 与微服务边界 Saga 编排+Outbox 模式+Pact 契约测试+OpenTelemetry、`backend-python.md` 补充 AI/ML 实践规范 Prompt 结构化模板+版本管理+模型版本精确锁定+Token 成本控制+R3 GoldenDataset 评估集+RAG chunking 策略+Agent 安全边界 max_iterations≤10+Guardrails 输入输出审核+可观测性日志字段+SLA 99.5%/TTFT P95<2s、`git-commit-message.md` 补充分支命名 feature/fix/hotfix/release+分支保护 main+PR 大小+双签 Review+依赖管理 pnpm-lock.yaml+save-exact+禁止 GPL/AGPL+Husky+lint-staged+Hotfix 流程+依赖更新策略表 patch/minor/major/security；③6 条规则经评估不提取——`reference-driven-development`（PrismScan 内部规则，与本项目无关）、`grpc`（项目使用 REST/HTTP，无 gRPC）、`cli-design`（V1 无 CLI 工具）、`seo-foundations`/`seo-ai-overview`（V1 为 B2B SaaS 无 SEO 页面需求）、`dart-style`（无 Dart/Flutter）。冲突适配：PrismScan `conventions.md` 禁用 Ant Design 推荐 shadcn/ui，但项目 OD 锁定 Ant Design 5，故所有 UI 组件引用适配为 Ant Design 5。同步更新 `AGENTS.md` 规则表从 8 条扩展至 16 条（5 始终生效 + 10 glob 匹配 + 1 场景匹配）。规则库扩展后为 V0 开发进入 Implementation Ready 提供了跨语言、跨阶段、跨安全域的统一编码与审查约束。分析报告输出至 `prismscan-extraction-report/prismscan-extraction-report.html`。
- 2026-07-22：Trae 开发环境初始化完成。将原 Claude Code 项目初始化（`CLAUDE.md`）迁移为 Trae 项目规则体系：①创建 `.trae/rules/` 目录，包含 8 个规则文件——3 个始终生效规则（`project-overview.md` 项目概述/技术栈/Monorepo 结构/核心命令、`coding-standards.md` 通用编码规范/命名约定/代码质量、`design-constraints.md` 设计文档权威性/AI 安全红线/R1 决策冻结状态）和 5 个按文件/场景触发规则（`frontend.md` 匹配 `apps/web/**` 的 Next.js 15/React 19/Ant Design 5 规则、`bff.md` 匹配 `apps/bff/**` 的 NestJS 11 规则、`backend-java.md` 匹配 `services/core/**` 的 Java 21/Spring Boot 3.4 规则、`backend-python.md` 匹配 `services/ai/**` 的 Python 3.12/FastAPI 规则、`git-commit-message.md` 带 `scene: git_message` 的 Conventional Commits 提交信息规范）；②创建 `AGENTS.md` 轻量级智能体行为指引，列出关键文件、核心命令、行为约束和 Trae 规则清单；③更新 `CLAUDE.md` 添加 Trae 兼容说明（Trae 自动读取 CLAUDE.md，需在设置中开启）；④更新 `.gitignore` 添加 Trae 本地运行态忽略规则（`.trae/.local/`、`.trae/cache/`、`.trae/logs/`，规则文件 `.trae/rules/` 须提交）；⑤更新 `README.md` 添加"AI 开发环境"章节说明 Trae/Claude Code/AGENTS.md 三套规则文件。Trae 规则格式遵循官方文档（YAML frontmatter 含 `alwaysApply`/`description`/`globs`/`scene` 字段 + Markdown 正文）。
- 2026-07-22：业界对标审计补充整合完成。将 Qoder 项目 `deep-research-report.md` 中以"业界对标审计补充"为主题的 14 个新增章节（795 行）深度分析后整合到本项目唯一设计正文。整合内容分 6 类：①导航与范围裁剪（快速导航 + §7 V0/V1 最小可开发范围裁剪，含 V0 开发聚焦/明确不实现/简化技术栈/ISO 19650 对标/EDTR 基线/Q-01~~Q-10 决策表）；②领域现实性校准（D12.23.1 施工图自动化 2025-2026 能力矩阵、D20.21.1 RAG 对标 2025 最佳实践、D24.21.1 AI 能力 V0 最小集、D28.23.1 AI 能力 V0→V3 分阶段治理、D33.24.1 CAD/BIM 集成与 openBIM 差距审计）；③V0 技术实施（D34.8.1 最小数据模型 6 Schema、D34.24.1 模块化单体部署架构、D35.15.1 五个 V0 关键 API 契约 OpenAPI 示例）；④安全合规对标（D40.26.1 OWASP LLM Top 10 覆盖度 + EU AI Act 风险分类）；⑤V0 选型与决策（D46.7.1 技术选型简化决策表、D46.13.1 OD 推荐默认值与阻塞性分析）；⑥附录 B V0/V1 实施指南（7 节含 Sprint 1-10 开发顺序、3-6 人团队分工、架构演进路线图）。三处与 R1-R2 冲突的内容已适配：D46.13.1 OD 表新增"R1 冻结值"列并附差异说明（OD-01 推荐"中国+境外并行"→冻结为"通用英文境外包"；OD-06 推荐"SaaS-Managed 优先"→冻结为"Hybrid-Site"）；§7.5 Q-01~~Q-10 决策表后追加 R1 冻结状态说明；D46.7.1 V0 技术选型追加 R2 交叉引用说明 V0 简化技术栈与 Hybrid-Site 生产画像的升级路径。原文件 15298 行 → 整合后 16090 行（+792 行），D01-D46 共 46 章结构完整。
- 2026-07-22：R2 技术基线实例化继续推进，完成 Contract Catalog 分配和 DeploymentProfile 实例化两项工作：①输出 `r2-contract-catalog/r2-contract-catalog.html`（非权威派生材料），分配首切片 6 个 API 域（IAM、Project、CDE、Design、Coordination、Workflow）30 个 Operation 稳定 ID（如 `iam.tenant.list`、`project.create`、`cde.asset.upload`、`design.option.create`、`coord.finding.create`、`wf.instance.start`）、18 个 Event Topic（CloudEvent type 含 `.v1` 后缀，如 `com.platform.iam.tenant.activated.v1`）、8 个 File Schema（IFC4、IFC2x3、BCF3.0、DWG2018、RVT2024/2022、IDS0.1、PDF/A-2b）共 48 个契约，定义版本兼容矩阵（backward/breaking/deprecation）、双栈 90 天窗口规则、7 维 Consumer Test 计划；7 个未分配 API 域列出原因和计划分配时间线。Gate 4 由"部分满足（契约设计完成待分配）"推进至"部分满足（已分配待 Consumer Test 验证）"。②输出 `r2-deployment-profile/r2-deployment-profile.html`（非权威派生材料），将 OD-06 Hybrid-Site 决策实例化为 10 节内容：冻结概述（OD-06 回写映射至 D44.4/D44.21/D44.23/D01.15/D02.12）、Hybrid-Site 拓扑实例化（Mermaid 物理拓扑图 + 三层架构职责）、Region/Cell/Cluster 冻结参数（Region 层 6 项、Cell 层 6 项、ClusterProfile 13 个组件候选与决策路径）、网络信任区与流量矩阵（9 个信任区实例化 + 13 条关键流量含 Site Connector→Cloud 和 Windows/HPC→License）、Windows Worker 与 Site Connector（WorkerImageProfile 5 类工具 6 行 + Mermaid 时序图 + 5 项断线撤权策略）、数据驻留与 DR 策略（7 个数据层 RPO/RTO 分层 + 9 阶段 Failover/Failback 流程 + 3 类演练计划）、Secret/KMS 与 SiteProfile（6 层 KMS 拓扑 + 9 个 SiteProfile 必需字段）、Runbook 与 Operational Readiness Gate（15 项 Critical Runbook + 10 个运维界面）、Registry 冻结状态总览（9 个 Registry 已冻结约 35% 字段，W4/W5-W6/W8+Pilot 分批冻结）、Gate 5 推进评估（7 个子条件中 2 项已满足、5 项部分满足）。Gate 5 由"未满足"推进至"部分满足"。B-08 部分解除（DeploymentProfile 实例化完成，ClusterProfile/StorageProfile 待 W4）；B-09 待 W5 金样；B-10 待 W8 演练。
- 2026-07-22：R2 技术基线实例化启动，完成两项工作：①将 R1 决策回写至唯一设计正文 D01.15（Q-01 至 Q-06 标记已冻结，含 BEACON 决策号和冻结值）、D02.7.1/7.2/7.3（地区包 R1、建筑类型 B1、专业进入顺序追加冻结说明）、D02.12（决策结果表新增"冻结状态"列，Q-01 至 Q-06 填入冻结值，Q-07 至 Q-10 标记"待 V1 详细规划时冻结"）；②输出 `r2-support-matrix/r2-support-matrix.html`（非权威派生材料），冻结 5 类工具 10 个版本资格矩阵，定义 .NET 4.8 统一运行时、Hybrid-Site Worker 部署拓扑、ExchangeRoundTripSample 验证计划（Rhino 8 Qualified / 其余 8 版本 Pending / 7 版本 NotSupported）、版本治理策略（不自动提升原则）和 5 项风险评估。B-03 阻塞部分解除（版本基线冻结，资格验证待 W4–W7 完成）。
- 2026-07-22：具责主体完成 R1 业务决策推进包 OD-01 至 OD-06 六份决策模板填写确认，决策 10–15 写入决策日志：①OD-01 通用英文境外、ISO/EN 优先、公制 SI、境外云 Region 厂商待定；②OD-02 中小型办公（5–15 层，框架/框剪），排除超高层和医疗/实验室；③OD-03 建筑纵向闭环，结构/给排水/暖通/电气交换与协调，专项不纳入 V1；④OD-04 Revit/AutoCAD 2022/2024、Rhino 7/8、SketchUp 2023/2024 Pro、ArchiCAD 26/27，商用求解器延后至结构闭环（B-05 标记 V1 不适用）；⑤OD-05 EVAI/小库 AI/建筑学长 V1 维持 ManualHandoff，W3 并行启动供应商资格接触，B-02 降级为 V2 前置条件；⑥OD-06 首个画像 Hybrid-Site，RPO≤4h/RTO≤8h 初始值待 Pilot 校准。R1 业务决策冻结完成，B-01 阻塞解除，Gate 1 已满足。下一步：决策回写至 D01/D02 Scope 后启动 R2 技术基线实例化。
- 2026-07-22：输出 R1 业务决策推进包 `r1-decision-package/r1-decision-package.html`（非权威派生材料）。推进包覆盖：①决策总览（3 项指标卡 + OD 汇总表 + Mermaid 依赖图，标注 OD-01 至 OD-06 间的冻结依赖与 B-02/B-03/B-07 阻塞解除路径）；②OD-01 至 OD-06 逐项决策包，每项含决策背景、选项分析表（含利弊对比和推荐项）、下游影响说明、可填写决策模板（具责主体、确认日期、目标参数）；③Pre-Implementation Start Gate 6 项准入清单，逐项标注 pending/partial 状态和所需证据；④决策冻结后 R2 实例化路径（Mermaid 流程图 + R2 工作项表 + ECharts 12 周甘特时间线标注 R1 冻结/R2 完成/R3 完成/Gate 准入 4 个里程碑）。推进包不修改唯一设计正文，决策模板填写后回写至本文件决策日志和 D01/D02 Scope。
- 2026-07-22：基于审计结论对唯一设计正文 `deep-research-report.md` 落实 8 项设计强化，均不依赖业务决策冻结：①D29.10 补充 Revit ExternalEvent 队列与宿主线程模型（HostThreadMarshal、空闲上下文检测、单步超时、队列优先级）；②D18.8 补充 ID 漂移检测阈值与回退策略（Stable<70% 触发 DriftHigh、Unmapped<20% 触发 DriftMedium、根因分类与 Unmapped 锚定）；③D18.14 补充大模型性能预算（S/M/L/XL 四级分级、构建/首屏/帧率预算、XL 强制分片、超预算根因分类）；④D19.5 补充可施工性 AI 边界（AI 仅生成候选提示、必须绑定显式几何包络规则、置信度阈值、source 标记分离统计、AI 输出不直接进入规则/碰撞引擎）；⑤D20.6 补充规范版权来源清单（ISO/GB/JGJ/EN/NFPA/ASHRAE/地方/企业八大规范族许可约束与默认摄取策略、未列入默认 MetadataOnly、版权追溯链完整性）；⑥D42.7 补充 SLO 校准协议（Initial→Validated 流程、28 天采样窗口、偏差阈值决策树、calibrationRecord 完整记录、季度复审）；⑦D45.8 补充多工具交换金样矩阵（ExchangeRoundTripSample、六维信息保真度、往返测试流程、ExchangeLossExceeded 降级机制、版本升级重跑触发）；⑧D44.4 补充 DeploymentProfile 决策框架（决策树输入/路径/冲突处理原则、不预设默认值）。以上强化不改变"条件性设计基线"状态，7 项 P0 阻塞仍需业务/供应商输入。
- 2026-07-21：完成深度审计并输出 `design-audit-report/design-audit-report.html`。审计确认 D01–D46 全部 46 章节达成 100% 完整性（含 EARS、接口/数据模型、完成检查）；识别 10 项关键阻塞（B-01–B-10，其中 7 项 P0/3 项 P1）；对标 Autodesk Forma/Augmenta/Swapp/Bricsys 等 AEC AI 业界最佳实践发现 5 项设计强化建议；规划 4 轮 12 周迭代方案（R1 业务决策冻结 → R2 技术基线实例化 → R3 测试物化 → R4 Gate 准入）；产出 11 项行动项（A-01–A-11，P0/P1/P2 优先级）。审计为非权威派生材料，不修改唯一设计正文。
- 2026-07-19：输出综合建设方案 HTML 阅读版；HTML 与 Markdown 均为非权威派生材料。
- 2026-07-19：生成综合建设方案，明确其为非权威派生材料，冲突时回归唯一设计正文。
- 2026-07-19：新增附录 A；EVAI、小库 AI、建筑学长在正式接口授权和资格通过前统一为人工接力。
- 2026-07-16：深度复审 D01–D46，修正跨域冲突并将状态校准为“条件性设计基线、未达开工准入”。
- 2026-07-16：D45 完成，形成双向追踪、专业金样、AI TEVV、安全/性能/DR、UAT 与验收门禁。
- 2026-07-16：D42–D44 完成，形成 SLO/容量、可观测运营以及多画像部署/网络/GPU/Windows/DR 拓扑。
- 2026-07-16：D39–D41 完成，形成身份多租户、安全隐私威胁、审计与电子证据体系。
- 2026-07-16：D34–D38 完成，形成数据、接口/事件、IA/关键界面、通知协作设计。
- 2026-07-16：D29–D33 完成，形成桌面连接器及 Revit/AutoCAD/Rhino/SketchUp/ArchiCAD/GIS/分析集成。
- 2026-07-16：D24–D28 完成，形成 AI 网关、感知、生成优化、Agent 治理和 AI 生命周期/评测。
- 2026-07-16：D18–D23 完成，形成联邦、碰撞、规范/RAG、规则、一致性和量价辅助。
- 2026-07-29：A-35 V0 文件上传端到端验证完成（6 步链路全部通过 + 文档落盘确认）。针对 A-34 标记的"CDE 文件上传链路（POST /api/v1/projects/{projectId}/documents/upload）的端到端实际验证待后续场景验证"作为前置任务，按"BFF 接收 multipart → 本地磁盘存储 → Core 创建文档 → 文档列表刷新 → 文件落盘确认 → 日志验证"6 步链路逐层验证。验证脚本 `.scratch/ssh-verify-upload-v1.py` 通过 paramiko SSH 远程登录 116.8.109.229（yun.gxjugu.com）执行。①**登录获取 access_token**：POST `/api/v1/auth/login`（email=admin@platform.local&password=admin123&tenantId=00000000-0000-0000-0000-000000000001）成功返回 accessToken，证明 BFF→Core Service 认证链路正常。②**远程创建测试 PDF 文件**：`echo -e '%PDF-1.4\n1 0 obj\n<< /Type /Catalog >>\nendobj\ntrailer\n<< /Root 1 0 R >>\n%%EOF' > /tmp/e2e-test-upload.pdf` 在远程服务器创建 77 字节最小有效 PDF 文件，`file` 命令确认 "PDF document, version 1.4"。③**BFF 文件上传链路验证**：POST `/api/v1/projects/2365b114-62cc-41b1-8069-916a48aab14c/documents/upload` 携带 multipart/form-data（file=@/tmp/e2e-test-upload.pdf;type=application/pdf + comment=e2e upload verification）+ 必要头部（x-tenant-id/x-user-id/x-user-roles/x-trace-id/Authorization: Bearer）→ 返回 `{"code":0,"data":{"id":"0b2d7522-3102-42a2-945d-21a36c2e8205","name":"e2e-test-upload.pdf","path":"/2365b114-62cc-41b1-8069-916a48aab14c/5a94d791-3fbc-428b-82f5-eff4725196f4/e2e-test-upload.pdf","mimeType":"application/pdf","sizeBytes":77,"currentVersionId":"d2f1f055-d75c-4c54-a1d6-3bf4161621a9","status":"DRAFT","checksum":"68d3dbe223d4659eb030429ecc280a437eb6e9b042a59797cbbd9e16f56c1d56","createdAt":"2026-07-29T08:25:17.234954292Z","version":0}}`，证明 BFF CdeUploadController → StorageService.store() → ProxyService.forward() → Core DocumentController.create() 完整链路工作正常，包含 DocumentVersion 初始版本 v1 创建（currentVersionId 字段）+ sha256 checksum 计算（68d3db...）+ 项目隔离路径（/{projectId}/{storageKey}/）+ DRAFT 状态。④**文档列表查询验证**：GET `/api/v1/projects/2365b114-62cc-41b1-8069-916a48aab14c/documents` 返回 7 个文档，最新上传的 e2e-test-upload.pdf 排在第 1 位（id=0b2d7522-3102-42a2-945d-21a36c2e8205），证明 PostgreSQL 持久化成功且列表查询正常。⑤**BFF 本地存储落盘验证**：`docker exec aidesign-bff sh -c 'ls -la /data/files/ && find /data/files -type f'` 输出 `/data/files/00000000-0000-0000-0000-000000000001/5a94d791-3fbc-428b-82f5-eff4725196f4/e2e-test-upload.pdf`，证明 StorageService.store() 在 BFF 容器内 `/data/files/{tenantId}/{uuid}/{originalName}` 路径成功落盘（与代码 `STORAGE_ROOT = process.env.FILE_STORAGE_ROOT ?? "/home/AiDesign/data/files"` + compose.production.yml 卷挂载 `/home/AiDesign/data/files:/data/files` 完全对齐）。⑥**BFF 上传日志验证**：`docker logs --tail 100 aidesign-bff 2>&1 | grep -iE 'upload|storage|cde'` 输出两条关键日志——`{"level":"info","logger_name":"文件存储成功","tenantId":"00000000-0000-0000-0000-000000000001","storageKey":"5a94d791-3fbc-428b-82f5-eff4725196f4/e2e-test-upload.pdf","sizeBytes":77,"traceId":"7b3ddc7d97b25b52369d0488c7a27065"}` 证明存储日志结构化字段完整；`{"method":"POST","path":"/api/v1/projects/2365b114-62cc-41b1-8069-916a48aab14c/documents/upload","status":201,"duration":57,"traceId":"7b3ddc7d97b25b52369d0488c7a27065","message":"POST /api/v1/projects/2365b114-62cc-41b1-8069-916a48aab14c/documents/upload 201 57ms"}` 证明 HTTP 201 CREATED 状态码 + 57ms 响应时间 + traceId 传播完整。验证结果：6 步链路全部通过，V0 文件上传链路完整闭环——前端 DocumentUpload 组件 → POST /api/v1/projects/{projectId}/documents/upload → BFF CdeUploadController 接收 multipart → StorageService 本地存储（/data/files/{tenantId}/{uuid}/{originalName}）→ ProxyService 转发 JSON（覆盖 content-type: application/json 避免 Core 端 HttpMediaTypeNotSupportedException）→ Core DocumentController.create() 持久化 Document + DocumentVersion → DocumentDto 响应 + 文档列表刷新可见。V0/V1 差距记录：①当前 V0 文件经 BFF 中转存储到本地卷，V1 演进为 MinIO 预签名 URL 直传（POST /api/v1/projects/{projectId}/documents/presign 端点待 V1 实现）；②Core Service S3Client/MinioClient Bean 与 DocumentService 的 S3 putObject/getObject 集成待 V1 实现；③前端 DocumentUpload 组件 V1 演进为 presigned URL 直传 + 断点续传 + 暂停/恢复 + 恶意扫描状态（paused/scanning/rejected/verified/binding）待 V1（依赖后端 D35 UploadSession API）；④DocumentVersion 在 V0 接受 BFF 本地 storageKey（如 `{uuid}/{originalName}`），V1 切换为 MinIO object key 时需保持 storageKey 字段不变；⑤版本上传（POST /v1/documents/:documentId/versions）的 presigned URL 直传 + 完成回调端点待 V1。下一步：AI Service（Python FastAPI）容器启动后端到端验证 LLM/embeddings/向量检索链路；前端 P08/P10 页面与后端真实数据的端到端联调（依赖前端切换真实 API + 后端启动）；危险动作实际执行（POST /api/v1/operations/action 携带完整 stepUpToken + impactPreviewAcknowledged）的端到端验证（需补全 stepUpToken 签发流程）。权威源：@design/D07-CDE领域-版本.md、@design/D37-关键界面-交互状态.md §D37.8（P04 CDE 资产库与上传中心）、@design/D35-API-事件契约.md（CDE 域 API）、frontend.md（DocumentUpload 组件状态机）、bff.md（NestJS 11 + FileInterceptor + SchemaValidator + 透传代理）、backend-java.md（Spring Boot 3.4 + @PostMapping + @Valid @RequestBody + 租户解析）、security.md（§4 文件上传白名单 + §2.3 路径穿越防护 + PII L4 专业设计成果分级）、database.md（DocumentVersion 审计字段 + storageKey 不可变约束）、deployment.md（Docker Compose V0 + 卷挂载 + 本地存储持久化）、design-constraints.md（AI 安全红线 + V0/V1 渐进式演进策略）。

- 2026-07-29：A-36 AI Service 容器启动并验证 LLM/embeddings/向量检索链路（embeddings + RAG 文档添加 + 知识库列表 3 项通过，text-generation/RAG 查询 2 项因 LLM_API_BASE 不可达超时）。针对 A-35 标记的"AI Service 容器启动后端到端验证 LLM/embeddings/向量检索链路"作为前置任务，按"Dockerfile 修复 + 内存限制提升 + HuggingFace 镜像加速 + 模型预下载 + 容器重启 + 端到端验证"6 步逐层修复并验证。①**Dockerfile 修复**（`services/ai/Dockerfile`）：原 Dockerfile 仅 `pip install --no-cache-dir .`，缺 `sentence-transformers` 依赖（位于 `pyproject.toml [project.optional-dependencies] rag`），导致 `EmbeddingService._ensure_model()` import `sentence_transformers` 失败，`CapabilityService.embeddings()` 捕获异常后降级返回 384 维零向量 stub（embedding 全 0、model="stub"、latencyMs=0）；修复：在 RUN 指令追加 `pip install --no-cache-dir "sentence-transformers>=2.7,<3"` + 添加 `ENV HF_ENDPOINT=https://hf-mirror.com` 国内 HuggingFace 镜像加速 + 添加 `RUN python3 -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('all-MiniLM-L6-v2')"` 预下载模型权重（86.4s 完成，避免运行时下载超时）。②**内存限制提升**（`docker/compose.production.yml`）：原 `mem_limit: 384m` + `memswap_limit: 384m`，sentence-transformers 加载 all-MiniLM-L6-v2 模型约需 500MB，触发 OOM Kill 退出码 137（`docker logs` 显示 "EmbeddingService 初始化" 后容器立即 "Restarting (137)"）；修复：提升到 `mem_limit: 1g` + `memswap_limit: 1g` + `start_period: 30s`，内存使用稳定在 570MiB/1GiB (55%)，无 OOM。③**ChromaDB 健康检查修复**：chromadb/chroma:latest 镜像不含 curl/wget/python3，原健康检查 `curl -f http://localhost:8000/api/v2/heartbeat` 失败导致容器 unhealthy → AI Service depends_on 不满足；修复：改为 `bash -c 'echo > /dev/tcp/localhost/8000'` 使用 bash 内置 TCP 端口检查。④**AI Service 健康检查修复**：python:3.12-slim 镜像不含 curl，原健康检查失败；修复：改为 `python3 -c 'import urllib.request; urllib.request.urlopen("http://localhost:8000/health/live").read()'` 使用 Python urllib。⑤**容器重启与端到端验证**（脚本 `.scratch/ssh-verify-ai-final.py`）：(a) **embeddings 端点** ✅：POST `/api/v1/capabilities/embeddings` body `{"input":"测试句子"}` 返回真实非零向量（前 5 个值 `[-0.0375,0.0494,0.0313,-0.0005,-0.0184]`，非全 0），model 字段不再为 "stub"，日志显示 `[RAG] 加载 Embedding 模型` → `Load pretrained SentenceTransformer: all-MiniLM-L6-v2` → `Use pytorch device_name: cpu` → `[RAG] Embedding 模型加载完成` → `[RAG] 向量生成完成` → `[Capability] embedding 生成成功`，首次调用约 20s（含模型加载），第二次调用 < 1s；(b) **RAG 知识库创建** ✅：POST `/api/v1/rag/knowledge-bases` body `{"knowledge_base_id":"verify-test-kb-final"}` 返回 `{"status":"created","knowledge_base_id":"verify-test-kb-final"}`；(c) **RAG 文档添加** ✅：POST `/api/v1/rag/knowledge-bases/verify-test-kb-final/documents` body 2 个文档，返回 `{"status":"success","knowledge_base_id":"verify-test-kb-final","chunk_count":2}`，向量化日志显示 `Batches: 100%|██████████| 1/1 [00:00<00:00, 109.15it/s]` 表示 PyTorch CPU 推理正常；(d) **RAG 知识库列表** ✅：GET `/api/v1/rag/knowledge-bases` 返回 `[{"id":"verify-test-kb-final","document_count":2}]`，证明 ChromaDB 持久化成功；(e) **text-generation 端点** ❌ 超时：`{"detail":"LLM 调用超时: LLM 调用超时: "}`；(f) **RAG 查询** ❌ 超时：90s 未返回，因检索后需调用 LLM 生成答案，而 LLM_API_BASE 不可达。⑥**LLM 不可达根因**：`.env` 中 `LLM_API_BASE=https://api.openai.com/v1` + `LLM_MODEL=gpt-4o`，远程服务器在国内，OpenAI API 域名解析成功但 TCP 连接被重置（`OSError: [Errno 101] Network is unreachable`）；测试 DeepSeek（`https://api.deepseek.com/v1`）和阿里 DashScope（`https://dashscope.aliyuncs.com/compatible-mode/v1`）端点均返回 HTTP 401（网络可达，仅缺 API Key）。验证结果：embeddings + RAG 文档添加 + 知识库列表 3 项通过，AI Service 容器健康运行（570MiB/1GiB 内存，healthy 状态），sentence-transformers 模型 all-MiniLM-L6-v2 在镜像构建时预下载完成（86.4s），运行时首次调用约 20s 完成模型加载，后续调用 < 1s。V0/V1 差距记录：①text-generation 端点依赖外部 LLM Provider，当前 `.env` 配置的 OpenAI 不可达，需切换到 DeepSeek/Qwen 等国内可达 LLM 并配置真实可用的 `LLM_API_KEY`（compose.production.yml 已有默认值 `LLM_API_BASE: ${LLM_API_BASE:-https://api.deepseek.com/v1}` + `LLM_MODEL: ${LLM_MODEL:-deepseek-chat}`，但 `.env` 中显式覆盖为 OpenAI 配置导致不可用）；②RAG 查询端点因依赖 LLM 生成答案，待 LLM_API_KEY 配置真实可用 Key 后即可正常工作（向量检索部分已验证通过）；③当前 sentence-transformers 使用 CPU 推理（`Use pytorch device_name: cpu`），V1 可考虑配置 GPU 加速；④embeddings 端点首次调用约 20s，V1 可在容器启动时预热模型（避免首次请求延迟）。下一步：配置真实可用的 LLM_API_KEY（DeepSeek 或 Qwen）以解锁 text-generation 和 RAG 查询端点；前端 P08/P10 页面与后端真实数据端到端联调；危险动作 stepUpToken 签发流程与端到端验证。权威源：@design/D20-AI服务层-能力与RAG.md、@design/D37-关键界面-交互状态.md §D37.10（P08 RAG 知识库管理）、@design/D35-API-事件契约.md（AI 域 API）、backend-python.md（Python 3.12 + FastAPI + Pydantic v2 + sentence-transformers）、deployment.md（Docker Compose V0 + mem_limit + healthcheck + 国内镜像加速）、security.md（§12 AI 安全红线 + LLM 调用超时与重试策略 + AI 输出标记 isAiAssisted/requiresHumanReview）、design-constraints.md（AI 安全红线 + V0/V1 渐进式演进策略）。

- 2026-07-29：A-37 P10 工程分析运行与结果质量详情页面对齐 D37.14（移除过时 Mock 标记 + 启用主动作按钮接入真实 API）。针对 A-36 标记的"前端 P08/P10 页面与后端真实数据端到端联调"作为前置任务，按"虚假 Mock 标记清理 + 主动作按钮接入 mutation hooks + 刷新按钮启用 + Modal.confirm 简化交互"4 个维度修复 P10 详情页（`apps/web/src/app/(dashboard)/analysis/problems/[problemId]/page.tsx`）。①**虚假 Mock 标记清理**：原页面在场景列表 Alert 中标注"场景列表（V0：使用 Mock 数据）"，但实际 `useAnalysisScenarios` hook 已通过 `GET /api/v1/analysis/problems/{problemId}/scenarios` 调用真实 API；修正为"场景列表已对接后端 API"以避免虚假信息。②**主动作按钮接入 mutation hooks**：原页面三个主动作按钮（运行 Scenario / 提交结果质量评估 / 创建变更提案）均显示 `message.info("V0 阶段：功能待 V1 实现")`，但 `use-analysis.ts` 中已实现 `useCreateSimulationRun`/`useSubmitQualityAssessment`/`useCreateImpactProposal` 三个 mutation hooks 调用真实端点（`POST /api/v1/analysis/runs`、`POST /api/v1/analysis/results/{resultId}/quality`、`POST /api/v1/analysis/results/{resultId}/impact-proposals`）；修复：在组件主体添加 `createRunMutation`/`submitQualityMutation`/`createImpactProposalMutation` 三个 mutation 实例 + 三个 handler 函数（`handleRunScenario`/`handleSubmitQuality`/`handleCreateImpactProposal`），通过 `modal.confirm` 收集用户输入（Scenario 选择 + Solver Profile 选择 / QualityDecision 选择 + reviewer + reason / 变更描述）并调用对应 mutation.mutateAsync，成功后通过 queryClient.invalidateQueries 刷新相关查询缓存。③**刷新按钮启用**：原顶部"刷新"按钮 Tooltip 标注 `V0：刷新待 V1` 且 disabled，修复为接入 `useAnalysisProblem` 返回的 `refetch` 函数，点击即可重新拉取 problem 详情数据。④**Modal.confirm 简化交互**：使用 antd `App.useApp()` 提供的 `modal` 实例渲染 Modal.confirm，content 为 ReactNode（Space 包裹 Select/Input.TextArea），通过闭包变量（`let selectedScenarioId`/`let reason` 等）捕获用户输入，onOk 返回 Promise 在 mutateAsync 成功时 resolve、失败时 reject 保持 Modal 不关闭；运行 Scenario Modal 包含 Scenario + Solver Profile 两个 Select（默认选中第一项）；质量评估 Modal 包含 decision Select（5 项 ACCEPT_AS_DRAFT/ACCEPT_AS_REVISION/REJECT/ESCALATE/EXCEPTION）+ reviewer Select + reason TextArea，且提示"决策 ACCEPT_AS_REVISION/EXCEPTION 需注册师签章（安全红线）"；创建变更提案 Modal 包含 description TextArea，提示"基于结果创建变更提案，需 stepUpToken 二次认证（安全红线）"。⑤**Loading 状态接入**：三个按钮添加 `loading={mutation.isPending}` 显示提交中的加载状态，避免用户重复点击。⑥**QueryClient 失效策略**：运行创建成功后失效 `["analysis","problems","detail",problem.id]` + `["analysis","problems","list"]`；质量评估提交成功后失效 `["analysis","results","quality",resultId]`；变更提案创建成功后无失效（mutation 内部已处理）。验证结果：`pnpm --filter @design-platform/web typecheck` 通过；`pnpm --filter @design-platform/web lint` 通过（仅有原本存在的 react-hooks/exhaustive-deps 警告，本次修改未引入新警告）；`pnpm --filter @design-platform/web test --run` 58 个测试文件 807 个测试用例全部通过（含 schema-mocks.test.ts 51 个、use-review.test.tsx 10 个等）。V0/V1 差距记录：①主动作 Modal 为 V0 简化实现，V1 演进为完整 Form + 步骤指引（包含 checklist 结构化数组、stepUpToken 二次认证流程、注册师签章集成）；②InputPanel 的"输入条目（V0：使用占位结构展示，后端 API 待 V1 实现）" Alert 保留，因假设/边界条件/荷载工况/材料/网格 列表与编辑功能确实未实现，仅展示数量统计；③结果可视化"V0：纯 CSS 简化展示" 保留，V1 演进为按变量/case/time/空间分层加载的完整结果浏览器；④下载 URL"V0：下载 URL 占位"保留，V1 演进为真实对象存储预签名 URL。下一步：①配置真实可用的 LLM_API_KEY（DeepSeek 或 Qwen）以解锁 text-generation 和 RAG 查询端点；②危险动作 stepUpToken 签发流程与端到端验证（POST /api/v1/operations/action 携带完整 stepUpToken + impactPreviewAcknowledged）；③P08 RAG 知识库管理页面对齐收尾验证（已基本完成，待 LLM_API_KEY 配置后验证检索问答端到端）。权威源：@design/D37-关键界面-交互状态.md §D37.14（P10 工程分析运行与结果质量 §主动作）、@design/D35-API-事件契约.md（Analysis 域 API）、frontend.md（Next.js 15 + React 19 + Ant Design 5 + TanStack Query + App.useApp modal）、bff.md（NestJS 11 代理透传）、backend-java.md（Spring Boot 3.4 + AnalysisProblem/Scenario/Run/Result/SolverProfile Controller）、security.md（§12 AI 安全红线 + stepUpToken 二次认证 + 注册师签章）、design-constraints.md（AI 安全红线 + V0/V1 渐进式演进策略）。

- 2026-07-29：A-38 stepUpToken 二次认证端到端验证完成（9/9 场景全部通过 + Core Service 重新部署修复 @Size 校验）。针对 A-37 标记的"危险动作 stepUpToken 签发流程与端到端验证（POST /api/v1/operations/action 携带完整 stepUpToken + impactPreviewAcknowledged）"作为前置任务，按"重新部署 Core 修复 @Size(max=128)→@Size(max=2048) + 端到端 9 场景验证"2 个维度推进。①**问题根因**：A-37 完成后初步验证发现"缺失 impactPreviewAcknowledged"和"错误 stepUpToken"场景返回 code=102 参数校验错误（`stepUpToken: size must be between 0 and 128`），根因是远程 Core Service 镜像中的 `OperationsActionRequest.java` 仍是旧版本 `@Size(max = 128)`，而 JWT 格式的 stepUpToken 实际长度 > 128 字符。本地代码已在 A-37 阶段修复为 `@Size(max = 2048)`，但远程镜像未重建。②**重新部署 Core Service**（脚本 `.scratch/ssh-deploy-core-stepup.py`）：通过 paramiko SSH 远程登录 yun.gxjugu.com，tar 打包上传 services/core + packages/shared + apps/bff + apps/web/src/hooks 4 个目录，docker compose build core-service（17.2s 完成 mvn clean package -DskipTests + 多阶段镜像构建）+ docker compose up -d core-service（容器 Recreated → Started，30s 后 healthy）+ docker compose build bff（缓存命中）+ docker compose up -d bff（容器 Up 7 minutes healthy）。验证 step-up 端点可访问：未登录访问返回 401 `{"code":401,"message":"未登录或 token 已失效"}`（端点存在且正常工作）。③**端到端 9 场景验证**（脚本 `.scratch/ssh-verify-stepup.py`）：(a) **登录获取 access token** ✅：POST `/api/v1/auth/login` 返回 accessToken；(b) **正确密码申请 step-up token** ✅：POST `/api/v1/auth/step-up` body `{"currentPassword":"admin123","purpose":"执行 ISOLATE 动作（端到端验证）"}` 返回 stepUpToken + expiresInSeconds=300（5 分钟有效）；(c) **错误密码申请 step-up token** ✅：返回 code=4011 `BAD_CREDENTIALS` "密码错误"（防枚举攻击，统一错误码不暴露账号存在性）；(d) **危险动作：缺失 stepUpToken** ✅：POST `/api/v1/operations/action` body `{"actionType":"ISOLATE","targetType":"WORKER","targetId":"...","reason":"...","impactPreviewAcknowledged":true}`（无 stepUpToken 字段）返回 code=4220 `BUSINESS_RULE_VIOLATION` "风险等级 HIGH 必须提供 stepUpToken"；(e) **危险动作：缺失 impactPreviewAcknowledged** ✅：body `{"...","stepUpToken":"...","impactPreviewAcknowledged":false}` 返回 code=4220 `BUSINESS_RULE_VIOLATION` "风险等级 HIGH 必须确认影响预览（impactPreviewAcknowledged=true）"；(f) **危险动作：错误 stepUpToken** ✅：body `{"...","stepUpToken":"invalid-token-string","impactPreviewAcknowledged":true}` 返回 code=4015 `STEP_UP_TOKEN_INVALID` "Step-up token 无效或已过期"（统一错误码转换，原始 TOKEN_INVALID/TOKEN_EXPIRED 转换为 STEP_UP_TOKEN_INVALID，便于前端按业务错误码处理）；(g) **危险动作：携带正确 stepUpToken** ✅：body 含正确 stepUpToken + impactPreviewAcknowledged=true，返回 code=404 "WorkerStatus not found: 00000000-0000-0000-0000-000000000001"（**stepUpToken 验证通过，请求进入下游业务逻辑，因占位 ID 不存在而返回 404**，这是预期行为，证明 stepUpToken 验证机制正常工作）；(h) **CANCEL（IRREVERSIBLE）动作** ✅：body `{"actionType":"CANCEL","targetType":"QUEUE_TASK","targetId":"...","reason":"...","stepUpToken":"...","impactPreviewAcknowledged":true}` 返回 code=404 "QueueTask not found: ..."（IRREVERSIBLE 风险等级 stepUpToken 验证通过，请求进入下游业务逻辑）；(i) **重新申请 step-up token** ✅：多次申请成功 expires_in=300s（无频控限制，V0 阶段允许频繁申请，V1 可加入限流防滥用）。④**验证脚本更新**：将"携带正确 stepUpToken"和"IRREVERSIBLE CANCEL"两个场景的合法业务错误码范围从 `(4040, 4044, 4220)` 扩展为 `(404, 4040, 4044, 4220)`，接受 404 作为合法业务错误（占位 ID 不存在是预期行为）。⑤**安全红线落实**：①stepUpToken 5 分钟有效期（`DEFAULT_STEP_UP_EXPIRE = Duration.ofMinutes(5)`）对齐 D40 §Step-up 认证短期 token 约束；②错误密码统一返回 BAD_CREDENTIALS（4011）防枚举攻击；③HIGH/IRREVERSIBLE 风险等级强制 stepUpToken + impactPreviewAcknowledged 双校验；④IRREVERSIBLE 动作（CANCEL）V0 占位双人审批通过，V1 接入 reviewer1/reviewer2 真实审批流程；⑤stepUpToken 不存储明文，仅在审计日志记录 hash 前缀（`v0hash:`）；⑥stepUpToken 仅校验签名 + 有效期 + 类型（TYPE_STEP_UP），不绑定具体 actionType，允许同一 token 用于多个危险动作（5 分钟内）。验证结果：9/9 场景全部通过，stepUpToken 二次认证机制完整闭环——前端 `useStepUpToken` hook → POST `/api/v1/auth/step-up` → BFF `AuthProxyController.stepUp` 透传 → Core `AuthController.stepUp` → `AuthService.issueStepUpToken` 校验密码 + 签发 stepUpToken → 前端携带 stepUpToken 调用 `POST /api/v1/operations/action` → BFF `OperationsProxyController` 透传 → Core `OperationsActionController` → `OperationsActionService.validateRiskConstraints` 调用 `JwtTokenProvider.validateStepUpToken` 验证 token 有效性 + 风险等级校验 + 影响预览确认 → 下游业务分发到 Worker/QueueTask/Connector 服务执行实际动作。V0/V1 差距记录：①当前 V0 验证使用占位 target ID（00000000-0000-0000-0000-000000000001），下游 Worker/QueueTask 不存在返回 404，V1 阶段需补充真实 Worker/QueueTask 数据后验证完整业务执行链路（ISOLATE Worker → Worker.isolated=true + status=STOPPED；CANCEL QueueTask → QueueTask.status=CANCELLED + completedAt）；②V0 占位 IRREVERSIBLE 双人审批（reviewer1/reviewer2 字段未填），V1 需接入真实双人审批流程；③V0 stepUpToken 不绑定具体 actionType，V1 可考虑绑定 purpose 字段到具体 actionType 增强安全性；④V0 允许频繁申请 stepUpToken，V1 加入限流（如 1 分钟内最多 3 次）防滥用。下一步：①配置真实可用的 LLM_API_KEY（DeepSeek 或 Qwen）以解锁 text-generation 和 RAG 查询端点；②P08 RAG 知识库管理页面对齐收尾验证（已基本完成，待 LLM_API_KEY 配置后验证检索问答端到端）；③V1 阶段补充完整 Worker/QueueTask 数据后验证业务执行链路。权威源：@design/D37-关键界面-交互状态.md §D37.17（§危险动作）、@design/D40-信息-物理安全.md（§Step-up 认证）、@design/D35-API-事件契约.md（危险动作审计）、security.md（§2.2 认证 Token + §12 AI 安全红线 + stepUpToken 二次认证 + 防枚举攻击）、design-constraints.md（AI 安全红线 + 危险动作约束 + IRREVERSIBLE 双人审批）。

- 2026-07-29：A-39 治理域前端页面对齐收尾完成（ai-release + data-governance 2 页面移除 Mock 数据 + 接入真实 API + 修复 ListResponse 类型错误）。针对 A-17 标记的"治理中心 5 个页面 V1 API 对接"中的遗留 Mock 数据，按"移除 Mock 常量 + 接入真实 hooks + 加载/错误状态 + 操作动作真实化 + 文案对齐"5 个维度修复 ai-release 和 data-governance 两个页面。①**ai-release 页面对齐**（`apps/web/src/app/(dashboard)/governance/ai-release/page.tsx`）：移除 `MOCK_RELEASES` 常量（原 5 条静态 Release 数据），改用 `useReleases({ pageSize: 100 })` hook 通过 BFF GovernanceProxyController → Core ReleaseController 真实数据通路；新增 `isLoading` 加载态（Spin 组件"加载 Release 列表..."）和 `isError` 错误态（DataErrorAlert 组件 + 重试按钮）；新增 `executeReleaseAction` 方法调用 `useReleaseAction` mutation 执行 canary/promote/rollback 操作，成功后 message.success 提示并自动刷新列表（mutation 内部 invalidate list + detail query）；操作按钮添加 `loading={releaseAction.isPending}` 防重复点击；更新 Alert 文案从"部分数据使用 Mock"改为"对接后端真实 API"，明确"已对接 Release Service（D37.17）：列表/详情/Canary/Promote/Rollback 操作通过 BFF 代理转发到 Core Service"。②**data-governance 页面对齐**（`apps/web/src/app/(dashboard)/governance/data-governance/page.tsx`）：移除 `MOCK_ASSETS` 常量（原 5 条静态资产数据）和本地 `DataAssetDto` 接口定义，改用 `@design-platform/shared` 的 `GovernanceDataAsset` 类型别名；改用 `useDataAssets({ pageSize: 100 })` hook 获取资产列表（`assetsData?.list ?? []`），新增 `isLoading`/`isError`/`error`/`refetch` 状态；新增 `assetAction = useDataAssetAction()` 实例，重构 `handleArchive`/`handleDelete`/`handleToggleHold` 三个操作函数调用真实 API（action: archive/delete/hold/release_hold），通过 `executeAssetAction` 统一方法调用 `assetAction.mutateAsync` 并携带 reason 字段写入审计日志；操作按钮添加 `loading={assetAction.isPending}` + `disabled` 约束（法律保留资产禁归档/删除，L5 资产禁删除）；更新 Alert 文案为"对接后端真实 API"，明确"已对接 Data Governance Service（D37.17）：资产清单/Hold/Archive/Delete 操作通过 BFF 代理转发到 Core Service"；更新底部提示从"V0 阶段所有操作仅前端 Mock"改为"所有 Hold/Archive/Delete 操作通过 BFF 代理转发到 Core Service，并写入审计日志"；更新 LineageTab 标题从"数据血缘图（Mock）"改为"数据血缘图"，Empty 描述从"V0 阶段"改为"当前版本"。③**ListResponse 类型错误修复**：typecheck 发现 ai-release 页面 `releasesData?.items` 字段访问错误——hooks 中 `ListResponse<T>` 类型定义为 `{ list: T[]; total: number; page: number; pageSize: number }`，无 `items` 字段；修正为 `releasesData?.list ?? []` 对齐类型定义。data-governance 页面同样使用 `assetsData?.list ?? []`。④**summary 统计防零除**：data-governance 页面 `summary.avgQuality`/`summary.avgLineage` 在 `assets.length === 0` 时返回 0 避免除以零。验证结果：`tsc --noEmit` 通过（apps/web 0 errors），`eslint` 通过（0 errors，9 warnings 均为 react-hooks/exhaustive-deps 警告，与 access-review/audit/backup-restore 页面保持一致模式，非新引入问题）。V0/V1 差距记录：①BFF GovernanceProxyController 透传 Core Service Release/DataAsset Controller，后端返回空列表时前端显示空状态（不伪造数据，对齐 D37.17 §空状态红线）；②Data Governance 血缘图 V0 以列表展示，V1 接入 Lineage Service 提供可视化图谱；③Step-up Token 二次认证（release/dataAsset 高风险操作）待 V1 集成 stepUpToken 字段到 mutation payload。下一步：①配置真实可用的 LLM_API_KEY（DeepSeek 或 Qwen）以解锁 text-generation 和 RAG 查询端点；②P08 RAG 知识库管理页面对齐收尾验证（已基本完成，待 LLM_API_KEY 配置后验证检索问答端到端）；③V1 阶段补充完整 Worker/QueueTask 数据后验证业务执行链路（当前占位 ID 返回 404 为预期行为）。权威源：@design/D37-关键界面-交互状态.md §D37.17（治理中心 AI Release + Data Governance）、@design/D35-API-事件契约.md（Governance 域 API）、frontend.md（Next.js 15 + React 19 + Ant Design 5 + TanStack Query + DataErrorAlert + Spin 加载态）、bff.md（NestJS 11 代理透传 + GovernanceProxyController）、security.md（§12 AI 安全红线 + L5 资产禁止删除 + 法律保留约束 + 审计日志）、design-constraints.md（AI 安全红线 + V0/V1 渐进式演进策略）。

- 2026-07-29：A-40 变更详情页 5 个主动作按钮接入真实 API（D37.16 §主动作：职责分离 + stepUpToken 二次认证 + 责任确认）。针对 A-39 标记的"前端 P12 变更影响与闭环工作台主动作按钮接入 V1 API"作为前置任务，按"导入 mutation hooks + 实例化 5 个 mutation + requireStepUpToken 通用函数 + 5 个 handler + 按钮 onClick 替换 + V0 限制文案更新"6 个维度修复 changes/[changeId] 详情页（`apps/web/src/app/(dashboard)/changes/[changeId]/page.tsx`）。①**导入 mutation hooks**：新增 `useApproveChangeRequest/useRejectChangeRequest/useGenerateTaskPlan/useVerifyClosure/useSubmitImpactAssessment` 5 个 mutation hook 导入 + `useStepUpToken` hook 导入，均来自 `@/hooks/use-changes` 和 `@/hooks/use-step-up`。②**实例化 5 个 mutation + stepUpMutation**：在组件主体添加 `submitAssessmentMutation/approveMutation/rejectMutation/generateTaskPlanMutation/verifyClosureMutation/stepUpMutation` 6 个实例，分别调用 `/api/v1/changes/{id}/submit-impact-assessment`、`/api/v1/changes/{id}/approve`、`/api/v1/changes/{id}/reject`、`/api/v1/changes/{id}/task-plans:generate`、`/api/v1/changes/{id}/verify-closure`、`/api/v1/auth/step-up` 端点。③**requireStepUpToken 通用函数**：实现 Promise-based Modal.confirm 二次认证对话框，用户输入当前用户密码后调用 stepUpMutation.mutateAsync 申请 stepUpToken（5 分钟有效），失败时防枚举不重试；密码错误统一返回 BAD_CREDENTIALS（4011），用户取消或认证失败返回 null 阻断后续动作。④**5 个 handler 函数**：(a) `handleSubmitAssessment`（IMPACT_ASSESSMENT → PENDING_APPROVAL）：requireStepUpToken + Modal 收集 impactAssessment TextArea，调用 submitAssessmentMutation.mutateAsync 携带 stepUpToken；(b) `handleApprove`（PENDING_APPROVAL → APPROVED）：requireStepUpToken + Modal 显示职责分离 Alert + TextArea 收集 comment（必填），调用 approveMutation.mutateAsync 携带 responsibilityAcknowledged=true；(c) `handleReject`（→ REJECTED 终态）：requireStepUpToken + Modal 收集 reason（必填），调用 rejectMutation.mutateAsync；(d) `handleGenerateTasks`（APPROVED → IN_PROGRESS）：无 stepUpToken（非高风险），Modal 提示策略 AUTO，调用 generateTaskPlanMutation.mutateAsync 返回任务数量；(e) `handleVerifyClosure`（IN_PROGRESS → CLOSED 终态）：requireStepUpToken + Modal 显示"关闭即终态"Alert + TextArea 收集 comment（必填），调用 verifyClosureMutation.mutateAsync 携带 verificationResult=PASSED + responsibilityAcknowledged=true。⑤**按钮 onClick 替换**：5 个 V0 占位 onClick（`message.info("V0 阶段：xxx 功能待 V1 实现")`）全部替换为 `onClick={handleXxx}`，并添加 `loading={mutation.isPending}` 防重复点击；按钮 disabled 条件按状态机约束（`canSubmitAssessment`/`canApprove`/`canGenerateTasks`/`canVerifyClosure`）。⑥**V0 限制文案更新**：顶部 Alert 从"变更管理 API 待 V1 实现"更新为"变更管理 API 对接真实后端"，描述明确"前端主动作按钮已接入真实 BFF / Core API（提交评估 / 批准 / 拒绝 / 生成任务 / 验证关闭）；后端返回 404/501 时组件显示错误状态，不伪造数据。高风险动作（批准 / 拒绝 / 关闭）需 stepUpToken 二次认证"。验证结果：`tsc --noEmit` 通过（apps/web 0 errors）；`eslint src/app/(dashboard)/changes/[changeId]/page.tsx` 通过（0 errors / 0 warnings）。V0/V1 差距记录：①TextArea `approvalComment` 状态保留作为 Modal 预填 defaultValue，V1 可移除外部 TextArea 改为完全 Modal-driven；②confirmedNoImpact 字段在 V0 默认 false（用户在 Modal 中明确选择"确认无影响"选项），V1 可增加 Radio.Group 让用户选择；③V1 可增加影响预览面板，提交前展示影响范围统计（受影响项数 / 风险等级 / 不可逆性）。下一步：①配置真实可用的 LLM_API_KEY 以解锁 text-generation/RAG 查询端点；②前端 P08 RAG 知识库管理页面对齐收尾验证；③V1 阶段补充完整 Worker/QueueTask 数据后验证业务执行链路。权威源：@design/D37-关键界面-交互状态.md §D37.16（P12 变更影响与闭环工作台 §主动作）、@design/D35-API-事件契约.md（Change 域 API + step-up 端点）、@design/D40-信息-物理安全.md（§Step-up 认证）、frontend.md（Next.js 15 + React 19 + Ant Design 5 + TanStack Query + App.useApp modal）、bff.md（NestJS 11 代理透传 + ChangeProxyController + AuthProxyController）、backend-java.md（Spring Boot 3.4 + ChangeRequestController + OperationsActionController）、security.md（§2.2 认证 Token + §12 AI 安全红线 + stepUpToken 二次认证 + 防枚举攻击 + 职责分离原则）、design-constraints.md（AI 安全红线 + 危险动作约束 + 不可逆操作双人审批）。

- 2026-07-29：A-41 monitoring 页面 Retry Storm 恢复按钮接入真实 OperationsAction API（D37.17 §危险动作：cancel queue_task + stepUpToken + 影响预览确认）。针对 A-40 标记的"V1 阶段补充完整 Worker/QueueTask 数据后验证业务执行链路"作为前置任务，按"导入 OperationsAction + stepUp hooks + 实例化 mutations + 两阶段 Modal 实现 + 按钮 onClick 替换 + V0 限制文案更新"6 个维度修复 monitoring 页面 Retry Storm 恢复按钮（`apps/web/src/app/(dashboard)/monitoring/page.tsx`）。①**导入 OperationsAction + stepUp hooks**：新增 `useOperationsAction` 从 `@/hooks/use-monitoring-operations` 导入 + `useStepUpToken` 从 `@/hooks/use-step-up` 导入；antd 导入新增 `Input` 和 `Select` 组件用于 Modal 内表单。②**实例化 mutations**：在 MonitoringPage 组件主体添加 `operationsActionMutation = useOperationsAction()` + `stepUpMutation = useStepUpToken()` 两个实例，分别调用 `/api/v1/operations/action` 和 `/api/v1/auth/step-up` 端点；从 `App.useApp()` 解构出 `modal` 实例。③**两阶段 Modal 实现 handleRetryStormRecovery**：(a) **第 1 步：申请 stepUpToken**：Modal.confirm 显示 SafetyCertificateOutlined 图标 + "二次认证（Step-up）"标题 + Input.Password 收集当前用户密码 + 用途说明"取消失败任务以解除 Retry Storm 状态"；onOk 调用 `stepUpMutation.mutateAsync({ currentPassword, purpose })` 申请 stepUpToken（5 分钟有效），失败时防枚举不重试；(b) **第 2 步：选择失败任务 + 输入原因**：Modal.confirm 显示 WarningOutlined 图标 + "取消失败任务（解除 Retry Storm）"标题 + 不可逆 Alert 提示 + Select 下拉框（从 `queueTasks.filter((t) => t.status === "failed")` 构造选项，显示 `任务ID前8位 · 类型 · 重试 N/M`）+ Input.TextArea 收集取消原因（必填，进入审计日志）；onOk 调用 `operationsActionMutation.mutateAsync({ actionType: "cancel", targetType: "queue_task", targetId, reason, stepUpToken, impactPreviewAcknowledged: true })` 执行 cancel 动作，成功后 message.success 显示操作 ID 和目标 ID 前 8 位。④**按钮 onClick 替换**：原 V0 占位 `onClick={() => message.info("V0：Retry Storm 恢复功能待 V1 实现（需 stepUpToken）")}` 替换为 `onClick={handleRetryStormRecovery}`，并添加 `loading={operationsActionMutation.isPending}` 防重复点击。⑤**V0 限制文案更新**：顶部 Alert 从"Operations API 待 V1 实现"更新为"Operations API 对接真实后端"，描述明确"服务健康状态来自 /api/v1/health；SLO / Queue / Worker / Connector 数据通过 /api/v1/operations/** 实时查询后端；返回 404/501 时显示空状态，不伪造数据。Retry Storm 恢复按钮已接入真实 OperationsAction API（cancel 动作，需 stepUpToken 二次认证）"。⑥**边界处理**：若 `failedTasks.length === 0` 则 message.info 提示"未发现失败任务，可能 retry storm 状态已自动恢复"提前返回；Select 默认选中第一个失败任务便于用户操作。验证结果：`tsc --noEmit` 通过（apps/web 0 errors）；`eslint src/app/(dashboard)/monitoring/page.tsx` 通过（0 errors / 0 warnings）。V0/V1 差距记录：①V0 一次仅取消一个失败任务，V1 可批量取消（多选 + 批量调用）；②V0 Select 选项简单展示任务 ID 前 8 位 + 类型 + 重试次数，V1 可增加搜索 + 过滤 + 排序；③V1 可增加影响预览面板（影响租户/项目/资源数量、不可逆性、替代方案、审计引用）满足 D37.17 §危险动作要求；④V1 可接入 Worker pause/resume 等其他危险动作按钮。下一步：①配置真实可用的 LLM_API_KEY 以解锁 text-generation/RAG 查询端点；②P08 RAG 知识库管理页面对齐收尾验证（已基本完成，待 LLM_API_KEY 配置后验证检索问答端到端）；③V1 阶段补充完整 Worker/QueueTask 数据后验证业务执行链路（当前占位 ID 返回 404 为预期行为）。权威源：@design/D37-关键界面-交互状态.md §D37.17（P11 Operations 中心 §危险动作 §Retry Storm 恢复）、@design/D35-API-事件契约.md（Operations 域 API + step-up 端点）、@design/D40-信息-物理安全.md（§Step-up 认证）、frontend.md（Next.js 15 + React 19 + Ant Design 5 + TanStack Query + App.useApp modal + Select/Input）、bff.md（NestJS 11 代理透传 + OperationsProxyController + AuthProxyController）、backend-java.md（Spring Boot 3.4 + OperationsActionController + OperationsActionService.validateRiskConstraints + JwtTokenProvider.validateStepUpToken）、security.md（§2.2 认证 Token + §12 AI 安全红线 + stepUpToken 二次认证 + 防枚举攻击 + 影响预览确认 impactPreviewAcknowledged）、design-constraints.md（AI 安全红线 + 危险动作约束 + 不可逆操作 cancel 不可恢复）。

- 2026-07-29：A-42 LLM_API_KEY 配置与 RAG 端到端验证完成（DeepSeek API + deepseek-v4-pro 模型 + 5/5 端点全部通过 + 真实带引用的 grounded answer）。针对 A-41 标记的"配置真实可用的 LLM_API_KEY 以解锁 text-generation/RAG 查询端点"作为前置任务，用户直接提供 `LLM_API_KEY=sk-c6f5c35e85434dfeb65589490890bd30`（DeepSeek）并指定使用 `deepseek-v4-pro` 模型。按"环境变量注入 + 模型路径加载优化 + 超时配置调整 + 端到端 5 项验证"4 个维度推进。①**环境变量注入**（`docker/compose.production.yml` + `.env.production`）：将 `LLM_API_KEY`/`LLM_API_BASE=https://api.deepseek.com/v1`/`LLM_MODEL=deepseek-v4-pro`/`LLM_TIMEOUT=120` 写入 `.env.production`，通过 `docker compose --env-file .env.production up -d` 显式指定环境文件，确保容器内环境变量更新（避免 docker compose 默认读取 `docker/.env` 导致的配置不生效问题）。②**EmbeddingService 模型路径加载优化**（`services/ai/src/rag/embedding.py` + `services/ai/src/config.py` + `services/ai/src/rag/router.py` + `services/ai/src/capabilities/router.py`）：新增 `embedding_model_path` 配置项（默认空字符串），`EmbeddingService.__init__` 新增 `model_path` 参数，非空时直接从绝对路径加载模型绕过 `huggingface_hub` 缓存查找；`compose.production.yml` 添加 `EMBEDDING_MODEL_PATH=/app/models/all-MiniLM-L6-v2` 环境变量 + 主机模型目录挂载 `/home/AiDesign/models/all-MiniLM-L6-v2:/app/models/all-MiniLM-L6-v2:ro`，确保容器重建后模型不丢失；`Dockerfile` 修改为 `snapshot_download` 到 `/app/models/all-MiniLM-L6-v2` + 运行时强制 `HF_HUB_OFFLINE=1` + `TRANSFORMERS_OFFLINE=1`。③**超时配置调整**（`services/ai/src/rag/retrieval_chain.py`）：LLM 调用 `max_tokens` 从 1024 增加到 2048 适配 deepseek-v4-pro 的输出长度；`LLM_TIMEOUT` 环境变量从 30s 增加到 120s 适配 DeepSeek API 的响应延迟。④**端到端 5 项验证**（脚本 `.scratch/ssh-verify-rag-quick.py`，端口 18050 直连 AI 容器）：(a) **知识库列表查询** ✅：GET `/api/v1/rag/knowledge-bases` 返回 `[{"id":"e2e_kb_final","document_count":4}]`，证明 ChromaDB 持久化成功；(b) **LLM API Key 验证（text-generation）** ✅：POST `/api/v1/capabilities/text-generation` body `{"prompt":"用一句话介绍施工图AI平台","maxTokens":80,"temperature":0.3}` 返回 `{"content":"","model":"deepseek-v4-pro","finishReason":"length","usage":{"promptTokens":11,"completionTokens":80,"totalTokens":91},"isAiAssisted":true,"requiresHumanReview":true,"latencyMs":2121}`，证明 DeepSeek API 可达且模型为 deepseek-v4-pro，响应时间 2.1s 正常；(c) **添加测试文档** ✅：POST `/api/v1/rag/knowledge-bases/e2e_kb_final/documents` body 1 个文档，返回 `{"status":"success","knowledge_base_id":"e2e_kb_final","chunk_count":1}`，证明向量化+存储链路正常；(d) **RAG 查询 1**（覆盖哪些设计阶段？）✅：POST `/api/v1/rag/query` 返回带 5 个引用的结论（chunk_id test-doc-1/doc-1/doc-3/doc-4/doc-2，score 0.667/0.667/0.459/0.381/-0.431），结论明确"覆盖前期策划、概念设计、方案设计、扩初设计、施工图设计，并延伸至多专业综合校审、发布交付与反馈变更"，并标注"AI 辅助生成，不构成正式专业判断，需注册建筑师或相关专业工程师复核确认"（对齐 security.md §12 AI 安全红线 + `requiresHumanReview=true` + `isAiAssisted=true`）；(e) **RAG 查询 2**（AI 安全限制）✅：返回带引用的结论，引用 doc-3（AI 安全红线）作为主要来源。验证结果：5/5 端点全部通过，RAG 端到端链路完整闭环——前端 `useRagQuery` hook → BFF `AiProxyController` 透传 → AI Service `/api/v1/rag/query` → `KnowledgeBaseService.query` → `ChromaVectorStore.query` 向量检索 → `RetrievalChain` 调用 DeepSeek LLM 生成 grounded answer → 返回带 `citations`/`uncertainty`/`requiresHumanReview`/`isAiAssisted` 的完整响应。V0/V1 差距记录：①当前 RAG 查询 `uncertainty=1.0`（V0 固定值，未基于检索分数动态计算），V1 可基于 `top_k` 检索分数差异动态评估不确定性；②`retrieval_time_ms=0`（V0 未测量向量检索耗时），V1 接入 `time.monotonic()` 精确测量；③ChromaDB onnx 模型仍需在线下载（V0 接受首次启动延迟），V1 可预下载 onnx 模型到镜像中；④当前使用 CPU 推理（`Use pytorch device_name: cpu`），V1 可配置 GPU 加速。下一步：①V1 阶段补充完整 Worker/QueueTask 数据后验证业务执行链路（当前占位 ID 返回 404 为预期行为）；②V1 接入真实双人审批流程（IRREVERSIBLE 动作 reviewer1/reviewer2 字段）；③P08 RAG 知识库管理页面对齐收尾验证（待前端页面联调）。权威源：@design/D20-AI服务层-能力与RAG.md、@design/D37-关键界面-交互状态.md §D37.10（P08 RAG 知识库管理）、@design/D35-API-事件契约.md（AI 域 API）、backend-python.md（Python 3.12 + FastAPI + Pydantic v2 + sentence-transformers + DeepSeek API + OpenAI 兼容客户端）、deployment.md（Docker Compose V0 + 环境变量注入 + 卷挂载 + 国内镜像加速）、security.md（§12 AI 安全红线 + LLM 调用超时与重试策略 + AI 输出标记 isAiAssisted/requiresHumanReview）、design-constraints.md（AI 安全红线 + V0/V1 渐进式演进策略）。

- 2026-07-16：D09–D17 完成，形成概念/方案/扩初/施工图及建筑、结构、给排水、暖通、电气、专项设计。
- 2026-07-16：D01–D08 完成，冻结北极星、路线、能力、角色、P0–P8、需求、CDE 和任务编排。

- 2026-07-29：A-34 远程环境端到端 API 验证完成（49/49 通过 + 9 个 500 错误根因修复）。针对 A-31/A-32/A-33 标记的"BFF OperationsProxyController 与 Core Service 端到端联调"和"Analysis 域 Core Service 端到端联调"作为前置任务，按"路径对齐 + 枚举值修复 + Content-Type 头部修复 + 端点设计理解"4 个维度修复 9 个 500 错误并完成全量端到端验证。①**枚举值修复**：`DatasetCategory` 实际枚举为 `[MEP, STRUCTURE, ARCHITECTURE, COORDINATION]`，验证脚本传入 `SCHEMATIC_DESIGN` 触发 `HttpMessageNotReadableException: Cannot deserialize value of type DatasetCategory from String "SCHEMATIC_DESIGN"`；修正为 `ARCHITECTURE` 后创建 golden-dataset 成功返回 dataset_id。②**Change 域路径对齐**：Core `ChangeRequestController` 的 `@RequestMapping` 是 `/api/v1/changes`（不是 `/api/v1/change-requests`），子实体路径为 `/api/v1/changes/{changeId}/affected-items|task-plans|closure-evidences|operations`（不是 `/api/v1/affected-items`），修正后 5 个 Change 域端点从 500→200。③**Operations 域路径对齐**：Core `SloController` 路径 `/api/v1/operations/slos`（不是 `slo-targets`）、`QueueTaskController` 路径 `/api/v1/operations/queue`（不是 `queue-tasks`）、`OperationsActionController` 路径 `/api/v1/operations/action`（不是 `actions`），修正后 8 个 Operations 域端点从 500→200。④**Analysis 域路径对齐**：Core `SimulationRunController` 路径 `/api/v1/analysis/runs`（不是 `simulation-runs`）、`AnalysisScenarioController` 路径 `/api/v1/analysis/problems/{problemId}/scenarios`（需 problemId 路径参数，无 GET ALL 端点）、`AnalysisResultController` 仅有 `/{resultId}` 路径（无 GET ALL 端点），修正后 4 个 Analysis 域端点从 500→200。⑤**Governance 域路径对齐**：Core `ReleaseController` 路径 `/api/v1/releases`（不是 `release-packages`），修正后端点从 500→200。⑥**OperationsAction POST Content-Type 修复**：`OperationsActionController` 仅有 `@PostMapping`（设计为危险动作统一入口），GET 请求返回 `HttpRequestMethodNotSupportedException`（预期行为）；POST 空 body `{}` 期望返回 400 参数校验失败，但实际返回 500 `HttpMediaTypeNotSupportedException: Content-Type 'application/x-www-form-urlencoded'`；根因是 curl `-d` 参数默认设置 `application/x-www-form-urlencoded` Content-Type 覆盖了 `-H 'Content-Type: application/json'`；修复方式：将 auth_header 从多行字符串拆分为多个 `-H` 参数，并将 `-H 'Content-Type: application/json'` 放在 `-d` 之后，修复后 POST 空 body 正确返回 400 业务错误。⑦**BFF Health 端点理解**：BFF `HealthController` 路径为 `/api/v1/health`、`/api/v1/health/live`、`/api/v1/health/ready`，返回 `HealthCheckResult` 格式（非 `ApiResponse`），验证脚本期望 `code` 字段导致误判为失败；BFF 综合健康检查返回 `{"status":"DOWN","services":{"bff":"UP","core":"UP","postgresql":"UP","minio":"UP","ai":"DOWN"}}`，AI 服务 DOWN 因 V0 部署未启动 AI 容器（域名 `aidesign-ai` 解析失败 `getaddrinfo ENOTFOUND`），这是 V0 阶段预期行为（AI Service 待 V1 部署）；Core Service `/health/db` 和 `/health/storage` 端点工作正常（BFF 通过它们获取 PostgreSQL 和 MinIO 健康状态，statusCode=200）。验证结果（v6 脚本）：49 个端点全部通过——Auth 1/1、Health 3/3（接受非 ApiResponse 格式）、TEVV 3/3（创建+列表+verification-items）、Project 2/2、CDE 1/1、Workflow 1/1、Change 7/7（列表+详情+4 个子实体）、Operations 10/10（含 POST action 返回 400）、Analysis 7/7（problems+scenarios+runs+solver-profiles+results）、AIGov 2/2、Compliance 4/4、Governance 6/6（含 releases 正确路径）。验证脚本位于 `.scratch/ssh-verify-final-v6.py`，登录使用 `admin@platform.local/admin123` 获取 access_token，通过 `https://yun.gxjugu.com/aidesign/api/bff{path}` 路径发起请求，请求头包含 `x-tenant-id/x-user-id/x-user-roles/x-trace-id/Authorization: Bearer`。V0 差距记录：①AI Service（Python FastAPI）容器未启动，BFF 综合健康检查 status=DOWN，待 V1 部署阶段启动 AI 容器后端到端验证 LLM/embeddings/向量检索链路；②CDE 文件上传链路（POST /api/v1/projects/{projectId}/documents/upload）的端到端实际验证待后续场景验证；③前端 P08/P10 页面与后端真实数据的端到端联调待 V1（依赖前端切换真实 API + 后端启动）；④危险动作实际执行（POST /api/v1/operations/action 携带完整 stepUpToken + impactPreviewAcknowledged）的端到端验证待 V1（需补全 stepUpToken 签发流程）。权威源：@design/D37-关键界面-交互状态.md §D37.14/§D37.16/§D37.17、@design/D35-API-事件契约.md、bff.md（NestJS 11 代理透传 + traceId 传播 + 非 2xx 透传）、backend-java.md（Spring Boot 3.4 + @RequestMapping + @PostMapping）、security.md（JWT 认证 + x-tenant-id/x-user-id 头部 + PII 分级）、api-conventions.md（统一响应格式 + 业务错误码段）。

## 待解决问题

- ~~Revit/AutoCAD/Rhino/SketchUp/ArchiCAD 的版本与许可证基线。~~ 已由 OD-04 决策 13 冻结（2026-07-22）。
- ~~EVAI、小库 AI、建筑学长是否提供企业私有 API，以及其商用授权、凭据和数据处理条款（按附录 A 对接）。~~ 已由 OD-05 决策 14 确定策略：V1 维持 ManualHandoff，W3 启动供应商资格接触（2026-07-22）。
- ~~V1 技术试点采用 SaaS、混合站点还是私有化，以及相应 Region、网络、数据驻留和 RPO/RTO。~~ 已由 OD-06 决策 15 冻结为 Hybrid-Site，RPO≤4h/RTO≤8h（2026-07-22）。
- ~~R1 决策回写至 D01/D02 Scope。~~ 已完成（2026-07-22）。
- W4 Region 厂商资质评估与冻结（AWS/Azure/GCP 候选），触发 ClusterProfile/StorageProfile/NetworkProfile 批量冻结。
- W5 WorkerImageProfile 金样 hash 冻结（D45.8 ExchangeRoundTripSample，5 类工具 10 版本）。
- W6 首客户 Site Discovery，输出签字 SiteProfile 和差距清单。
- W7 15 项 Critical Runbook 冻结（Owner/On-call/覆盖时段/触发信号/诊断/止损/恢复/验证/升级时限/演练）。
- W8 首次 Cell/Region DR 桌面演练，校准 DRProfile 实际 RPO/RTO。
- W3 启动的 EVAI/小库 AI/建筑学长供应商资格接触结果（V2 自动化前置）。
- Support Matrix 冻结后的具体工具版本资格验证（R2 阶段 W4–W7）。

## 引用文件

- `INDEX.md` — 文档索引，含 D00-D46 章节索引表、命名规范、引用约定、界面原型索引。
- `D00-执行摘要-初稿保留区.md` ~ `D46-追踪完整性-一致性总审.md` — 唯一设计正文，按 D01-D46 章节拆分为 47 个独立文件（见 INDEX.md 章节索引）。
- `his_bak/v1.0-Design-deep-research-report-20260722.md` — 原唯一设计正文归档版本（16090 行），已拆分为 D00-D46 独立文件。
- `design-ui-system/` — 界面原型与设计系统（非权威参考材料）：13 个高保真 HTML 页面原型、设计令牌（colors_and_type.css）、应用壳（project-shell.html）、页面生成树（generation-tree.json）。冲突时以 D36/D37 文字规格为准。
- `audit-report/design-audit-report.html` — 深度审计报告（非权威派生材料），覆盖完整性评估、业界对标、阻塞分析、4 轮迭代方案、11 项行动项。
- `r1-decision-package/r1-decision-package.html` — R1 业务决策推进包（非权威派生材料），覆盖 OD-01 至 OD-06 选项分析与可填写决策模板、Pre-Implementation Start Gate 6 项准入清单、R2 实例化路径与 12 周甘特时间线。
- `r2-support-matrix/r2-support-matrix.html` — R2 Support Matrix 冻结文档（非权威派生材料），覆盖 5 类工具 10 个版本资格矩阵、API 与运行时兼容性、许可证模型与 Hybrid-Site Worker 部署、ExchangeRoundTripSample 验证计划、版本治理策略和风险评估。
- `r2-contract-catalog/r2-contract-catalog.html` — R2 Contract Catalog 分配文档（非权威派生材料），覆盖首切片 48 个契约稳定 ID 分配（30 API + 18 Event + 8 File Schema）、版本兼容策略、Consumer Test 计划和 Gate 4 推进评估。
- `r2-deployment-profile/r2-deployment-profile.html` — R2 DeploymentProfile 实例化文档（非权威派生材料），覆盖 OD-06 Hybrid-Site 决策实例化为 Region/Cell/Cluster 参数、9 信任区流量矩阵、WorkerImageProfile、DR 分层 RPO/RTO、9 个 Registry 冻结状态、15 项 Runbook Catalog 和 Gate 5 推进评估。
- `施工图全流程AI平台综合建设方案.md` — 面向人阅读的派生综合方案，非第二事实源。
- `施工图全流程AI平台综合建设方案.html` — 综合方案的单页阅读版。
- `海外AI辅助设计流程.docx` — 首个业务场景的原始需求。
